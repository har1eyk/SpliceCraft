# Invariants — sweep history and feature-area rules

Companion to `CLAUDE.md`. The 10 SACRED biology-correctness invariants live in `CLAUDE.md`; this file holds **everything else** — the 35 known pitfalls, the sweep-history entries [INV-36]…[INV-64], persistent settings rules, architecture pointers, conventions, the sister-project note, and the new-feature playbook ("Borrow before respinning").

## How agents should use this file

**Before editing any subsystem, grep this file for the tag.** Each entry carries a `[PIT-NN]` / `[INV-NN]` / `[PREFS]` / `[ARCH]` / `[CONV]` / `[SISTER]` / `[RECIPE]` anchor plus topic keywords so a single `grep` finds it fast.

```bash
grep -n '\[INV-50\]' docs/invariants.md           # by number
grep -n '\[PIT-17\]' docs/invariants.md           # known-pitfall lookup
grep -n '\[RECIPE\]' docs/invariants.md           # new-feature playbook
grep -ni 'master.delete\|wipe' docs/invariants.md # by topic
grep -ni 'synthesis\|protein' docs/invariants.md  # by feature area
```

## Tag index (grep table)

| Tag | Topic keywords |
|---|---|
| `[PIT-01]`…`[PIT-39]` | Known pitfalls (bare-except, wrap features, `_re_highlight` schema, Primer3 linear-only, cache contracts, trademark scrub, agent-endpoint rules, natural-sort row mapping, CommercialSaaS writer packet inventory, rename LOCUS sanitisation, trad-cloning lane rebuild, Textual `prevent` for programmatic widget writes, Select.BLANK vs Select.NULL…) |
| `[INV-73]` | INV-72 follow-up sweep (2026-05-25) — `_flush_active_alignments` `_cache_lock` RMW; `_extract_variants_from_alignment` cap + truncated sentinel; `_kmer_set` strong-match minimum; rotation-picker enum validation; `_coverage_pct_from_result` helper; `events_summary.json` in F9 bundle; terminal/locale capture; bulk-align summary event; exception-context args; `_action_log` diagnostic; pyright cleanup (workspace 0 errors) |
| `[INV-95]` | Ctrl+E `EditSeqDialog` rework — Insert **left**/**right** + Replace + Delete radios (Copy dropped — five overflowed the dialog), colour-coded live "You are about to `<action>` `<N>` bp `<position>`" warning, region-prefilled textbox (read-only preview for delete, capped at `_PREFILL_MAX_BP`), a bare cursor = a 1-bp region (`seq[cursor_pos]` exact — no off-by-one); Ctrl+E default is non-destructive (Replace/Insert), the **Delete key** (no feature) → this dialog in Delete mode |
| `[INV-94]` | Ctrl+F sequence search (fuzzy / both-strand / circular matcher, `n`/`N` step) + alignment-lane click → seq-panel navigation (shared `focus_span` jump-to-bp; add-feature remapped Ctrl+F→Alt+Shift+F — NOT Alt+F, which decodes as `ctrl+right`) |
| `[INV-93]` | Construction history for EVERY generated plasmid (origin + edit-append history at the save chokepoints, bloat cap, data-safe legacy backfill, Constructor enzyme-gate fix, move/copy excluded) |
| `[PREFS]` | Persistent user preferences (`settings.json`, the 4-step add-a-toggle recipe, full toggle list) |
| `[ARCH]` | Architecture pointers (top-to-bottom `splicecraft.py` layout, `grep` recipe for live map) |
| `[CONV]` | Conventions (worker pattern, JSON-library envelope, modal subclass rule, test rules, regression-guard dating) |
| `[SISTER]` | Sister project (ScriptoScope) — perf-pattern crib source, single-file-merge question |
| `[RECIPE]` | "Borrow before respinning" — new modal / JSON file / save action / `action_*` / worker / agent endpoint / picker / feature iter / HTTP fetch / XML parse / logging / file traversal |
| `[INV-36]` | future-proofing scaffolding; migrations; SPLICECRAFT_PYPI_URL; data-version stamp; plugins |
| `[INV-37]` | robustness 0.7.6; data-dir lock; threading excepthook; settings schema; clipboard fallback |
| `[INV-38]` | diagnostic logging; UI snapshot Alt+D; logs bundle; sequence-leak prohibition |
| `[INV-39]` | splicecraft update pre-update snapshot; restore; sibling backup dir; four restore checks |
| `[INV-40]` | overhang pair; part classification; `_classify_part_from_plasmid` |
| `[INV-41]` | sweeps #2-#6; `_feat_bounds`; `_smallest_enclosing_feature`; worker pattern; `_blocks_undo`; `_cache_lock`; symlink refusal; .dna sidecar collision |
| `[INV-42]` | structured event logging; `_log_event`; `@_action_log`; `@_timed`; sequence redaction |
| `[INV-43]` | sweep #9; cache-bust on restore; project-switch atomicity; tag regex; Windows zip separators; clipboard megapixel cap |
| `[INV-44]` | every save is a full plasmid; Domesticator library mirror; clone simulation colours |
| `[INV-45]` | cross-platform contract; UTF-8 required; OSC 52 OS-specific; subprocess list-form |
| `[INV-47]` | entry-vector auto-detection; `_detect_entry_vector_role`; α/Ω acceptors; EntryVectorsModal |
| `[INV-48]` | Master Delete; sentinel; typed-YES; cool-down; residual sweep; cache reset |
| `[INV-49]` | load-time collision resolution; ExactCopyConfirmModal; NameCollisionModal; parts-bin Counter delete |
| `[INV-50]` | sweep #10; batch-collision flow; modal double-fire; cache-bust enumeration; save-chain lock; history stack overflow; rotated state cache key |
| `[INV-51]` | sweep #11; active-pointer flush-sync; cross-group worker RMW; auto-bind worker; `_find_library_entry_by_id`; agent API hardening |
| `[INV-52]` | sweep #12; aspect early-exit; `_blocks_undo` 12 modals; reactive hygiene baseline |
| `[INV-53]` | Synthesis composer; SynthesisEditor; document-model save; IUPAC typing; feature-shift |
| `[INV-54]` | Synthesis DNA/Protein tabs; ProteinEditor; codon-table dropdown; Alt+T toggle; protein motifs; CDS round-trip |
| `[INV-55]` | sweep #14; `_make_unique_entry_id`; Save As single prompt; Clone Fragment handoff; Domesticator prefill |
| `[INV-56]` | sweep #15; AA features; motif insert colored; protein_motifs.json persistence; Edit buttons |
| `[INV-57]` | sweep #16; dithered protein lane art; distinct motif colors; release.py lint gate |
| `[INV-58]` | sweep #17; `extend_adjacent_feats` kwarg; boundary insert semantics |
| `[INV-59]` | sweep #18; protein-load filter; misc_feature only; codon-alignment guard; CI trimmed; release.py bundles |
| `[INV-60]` | sweep #19; protein lane art perf; pyright `_apply_record` |
| `[INV-61]` | sweep #20; 21 new agent endpoints; cache attrs coverage; `_blocks_undo` retrofit; collision events; `@_action_log` retrofit; bare-except narrowing |
| `[INV-62]` | sweep #21; modal-staleness audit; `_guard_callback`; feature/primer/annotate-edit wrap |
| `[INV-63]` | sweep #22; `_safe_save_json` ancestor symlink walk; `_iupac_pattern` case-fold; migration framework tests |
| `[INV-64]` | sweep #23; new file formats (EMBL/AB1/FASTQ/GFF3); bulk export; 18 new agent endpoints; backup-label parity; readonly cache contract; rejected seed-from-validate cache pattern |
| `[INV-65]` | sweep #24; custom enzymes + enzyme collections hardening; `_AGENT_BACKUP_LABELS` parity for new files; `_dismiss_once` retrofit; `_save_or_status` modal helper; `_all_enzymes()` routing across scan/cloning paths; redundant exception-tuple narrowing; 12 new agent endpoints; enzyme-save cache invalidation |
| `[INV-66]` | sweep #25; full adversarial audit (4 parallel agents); cache-bust drift fix (`_MASTER_DELETE_CACHE_ATTRS` is now the single source of truth for both restore paths); `_iter_library_readonly` + `_iter_parts_bin_readonly` + `_find_library_entry_by_name` helpers; custom-enzyme agent endpoint RMW under `_cache_lock`; App-level Ctrl+S worker under `_cache_lock`; 5 disk-writer workers wrap `_safe_save_json` in `_cache_lock`; `_SCAN_CATALOG` atomic rebuild; bearer token required on ALL agent endpoints; agent server `timeout=30`; uniform-400 collapse for `_h_load_file` / Plasmidsaurus errors (FS-oracle reduction); `_get_setting` reads cache directly; `_rc` hoist in annotation-transfer loop; GFF3 silent-drop logging; `_safe_identifier` validator on `active_*` settings; snapshot-dir restore size cap; agent token via `secrets.token_urlsafe(32)`; `_VECTOR_MATCH_CACHE` hash key; CLI lstat token file; lockfile unlink on graceful exit |
| `[INV-67]` | Plasmidsaurus align flow refactor — target-as-canvas + rotation picker (2026-05-24); see `[SUB-plasmidsaurus]` |
| `[INV-68]` | `_pick_best_rotation` helper — tries plain + query-rot + target-rot, picks highest overall identity, shifts aq/at to canvas-axis frame; used by both `_align_worker` (canvas_axis="target") and `_diff_align_worker` (canvas_axis="query"). 80% identity threshold skips the 2x C-loop when plain is already good. Toast surfaces aligned-bp/target-bp + coverage% + overall identity + matched-region identity (pre-fix only matched-region was shown — misleading for length-mismatched plasmids). MultiAlignPickerModal filters stale ids against current library at dismiss. PrimerDesignScreen warns before silently clearing star-marks on collection switch. Sanger AB1 "Add to library" disables button after add (prevents silent `_2`/`_3` duplicate-add) and refreshes LibraryPanel. |
| `[INV-69]` | Bulk-align + Verification Report + Library "Seq" badges (2026-05-24). LibraryPanel `_lib-table` gained a "Seq" column showing per-entry sequencing-status badges (✓ verified / ⚠ near-match / ~ partial / ✗ divergent / — none) computed by `_library_entry_alignment_summary` from stored alignments. `LibraryPanel.refresh_seq_cell(entry_id)` does incremental cell update without full repopulate (triggered by `_flush_active_alignments` + AlignmentManagerModal saves). `_match_samples_to_library` does fuzzy name match + k-mer Jaccard fallback. `BulkAlignConfirmModal` lets user rotate per-row action (align/add/skip) before commit. `_bulk_align_worker` batches alignments + new-entry adds through `_pick_best_rotation`, persists each onto its target's library entry, stamps `source: plasmidsaurus:<run>:<sample>` provenance for add-as-new entries. `VerificationReportModal` collects every stored alignment across the active library, sorted by status priority (worst first), click-row jumps to canvas with cursor at first variant. `_extract_variants_from_alignment` walks aq/at into `{snp, insertion, deletion}` records (gap runs merged into single indel records). `_normalize_for_match` strips Plasmidsaurus run+order prefix + extension + punctuation for name comparison. |
| `[INV-70]` | Bulk-align matcher: sequence-first ranking + memory cap (2026-05-24). Pre-fix the matcher ran name-substring first and only fell back to k-mer when name match failed — a coincidental substring (e.g. `cam2` ⊂ `pcambia1300`) could outrank a 99%-identical entry by sequence. Post-fix `_match_samples_to_library` always computes both name and k-mer scores for every (sample, library entry) pair; decision tree: **strong-kmer (Jaccard ≥ 0.5)** wins outright, then **near-exact name (≥ 0.85)** as fallback, then **weak-kmer (≥ 0.2)** as last resort, else propose add-as-new. Top-3 alternatives surfaced on every match for user visibility. `_kmer_set(seq, cap=50_000)` truncates sequences > 50 kb to bound memory — a 154 kb chloroplast genome × 89 library entries exploded RAM to >5 GB pre-fix. `BulkAlignConfirmModal` now shows k-mer and name as separate colour-coded columns (green ≥ 50%, yellow ≥ 20%, red below) so weak matches stand out. |
| `[INV-71]` | Strand-agnostic alignment (canonical k-mers + RC trials) (2026-05-24). Pre-fix a Plasmidsaurus consensus that assembled in the opposite orientation to its library entry scored 0% k-mer overlap in `_match_samples_to_library` and ~0% identity in `_pick_best_rotation` — the matcher then picked a coincidental backbone match and the aligner produced garbage. Post-fix `_kmer_set(canonical=True)` (default) normalises each k-mer to the lexicographically smaller of (k-mer, RC(k-mer)) so RC-of-target samples now score 100% Jaccard. `_pick_best_rotation` tries forward AND RC of the query at the plain tier, then runs rotation candidates in BOTH orientations when neither plain crosses the 80% threshold (4 rotation trials max). Result carries new `query_rc: bool` field. Early-stop at `_ROTATION_EARLY_STOP_PCT = 99.5` avoids wasting C-loop time once a near-perfect candidate lands. Verified end-to-end on user case: CAM-2/3/5/6/7 → MAV 39/38/41/42/43 at 100% k-mer / 100% identity (pre-fix: all → pCAMBIA1300 at 40% / 47%). |
| `[INV-72]` | Alignment audit sweep (2026-05-25). Five-part hardening that closes gaps the `[INV-67]`–`[INV-71]` sweeps left behind. **(1) `_normalize_dna_for_align` helper**: every `_pairwise_align` input runs through a single normaliser that strips FASTA header lines (`>name\n` via `_FASTA_HEADER_PATTERN`, line-anchored), digits, whitespace, then uppercases + IUPAC-validates against `_IUPAC_NUC_CHARS = frozenset("ACGTUMRWSYKVHDBN")`. Pre-sweep the docstring claimed "raises on non-IUPAC" but no such check existed — a pasted protein sequence or FASTA-as-is body either crashed deep inside Biopython or produced silent garbage. `_pick_best_rotation` pre-normalises at entry so its frame-shift helpers (`_rotate_aligned_to_original_target_frame` / `_rotate_aligned_to_original_query_frame`) use the cleaned length — pre-fix a raw-FASTA input would mean `len(target_seq)` in the shift differed from what `_pairwise_align` consumed, off-by-N depending on how many header/whitespace chars got scrubbed. **(2) Agent endpoint picker parity**: `_h_diff_plasmid` and `_h_align_plasmidsaurus_zip` now route through `_pick_best_rotation` (canvas_axis="query" and "target" respectively) instead of a bare `_find_circular_alignment_offset` + `_pairwise_align`. Pre-sweep agent callers missed the RC-orientation auto-detection + multi-rotation best-of-N pick the UI gained in `[INV-71]` — an RC-orientation Plasmidsaurus consensus scored 0% via the agent while the UI scored 100%. Response payload gains `picked_rotation` / `query_rotation` / `target_rotation` / `query_rc` fields so the agent can replicate the alignment; `rotation_offset` kept as a back-compat alias for `target_rotation`. **(3) Bulk-align failure surfacing**: `_bulk_align_worker`'s add-as-new path tracks success vs failure in a shared `add_counters = {"ok", "err"}` dict; the OSError/ValueError/RuntimeError catch increments `counters["err"]` rather than letting the count of "tried to add" pass for "actually saved". Summary toast now shows `aligned N · added M new · add failed K · failed L` with severity bumped to "warning" when any failure column is non-zero. Mid-flight cancel readout reads from the same counter so it agrees with the final summary. **(4) `n_failed` log diagnostic**: the no-gbk-member skip in bulk-align now logs `BulkAlign: skipping sample %r — no .gbk member field` so a malformed Plasmidsaurus manifest leaves a trail in the diagnostic bundle (pre-fix the skip was silent and "failed N" gave no clue why). **(5) Coverage clamp**: `_align_worker`'s toast and `VerificationReportModal._collect_rows` clamp `coverage_pct` to `min(100.0, …)` so a corrupted result dict can't render ">100% coverage" to the user. Underlying numbers in storage are untouched — display-only defense. Tests: `TestNormalizeDnaForAlign` (12 cases), `TestPickBestRotationNormalises` (3), `TestAgentDiffPlasmidUsesPicker` (2), `TestAgentAlignPlasmidsaurusUsesPicker` (1), `TestPairwiseAlignCoverageClampInToast` (3), `TestBulkAlignNoGbkLogs` (1). |
| `[INV-73]` | Alignment + F9 diagnostic follow-up sweep (2026-05-25). Closes gaps the `[INV-72]` sweep left behind after a parallel-agent audit. **Alignment (5):** (1) **`_flush_active_alignments` thread-safety** — the load-merge-save sequence now holds `_cache_lock` for the entire RMW. `_diff_align_worker`, `_align_worker` (Plasmidsaurus), `_multi_align_worker` run in different `@work` groups, so two concurrent flushes against the same library entry would clobber each other's writes (silent data-loss path). RLock allows the inner `_load_library`/`_save_library` to re-enter without deadlock. (2) **`_extract_variants_from_alignment` result cap** — `_MAX_VARIANTS_PER_ALIGNMENT = 10_000` bounds memory on a fully-divergent alignment (200 kb random vs 200 kb random ≈ 200k variant dicts ≈ 40 MB). Capped runs emit a `{"type":"truncated"}` sentinel so callers can surface "10k+" rather than silently underreport; type-filter callers (`sum(1 for v in variants if v["type"]=="snp")`) naturally skip it. (3) **`_kmer_set` minimum for strong-match** — `_MIN_KMER_SET_FOR_STRONG_MATCH = 50` gates the `kmer-strong` path in `_match_samples_to_library` so a 25 bp sample (~6 canonical k-mers) can no longer score a coincidental 1.0 Jaccard against a library entry that happens to contain a primer-length match region. Below the threshold the sample falls through to name / weak-kmer / add-as-new. (4) **Rotation-picker enum validation** — `_deserialize_stored_alignment_args` validates `picked_rotation` ∈ {none, query, target}, `query_rotation`/`target_rotation` non-negative int, `query_rc` bool; corrupted values coerce to safe defaults with a warning log rather than crashing or silently misframing downstream segments. (5) **`_coverage_pct_from_result` helper** — centralises the clamp + zero-target guard so toast (`_align_worker`) and `VerificationReportModal._collect_rows` can't drift in their display logic; future call sites get the same defensive math. **F9 / diagnostic bundle (2):** (6) **`events_summary.json`** in the diagnostic bundle — `_extract_recent_events` parses the last 200 structured `_log_event` lines out of the rotating log (walks rotated backups too) and ships them as a JSON-parseable array; a bug-reader (or an AI scanning the bundle) gets the causal chain without regex against unstructured text. (7) **Terminal/locale env capture** in `_build_system_info` — narrow allowlist (`TERM`, `COLORTERM`, `TERM_PROGRAM`, `LANG`, `LC_*`, `SHELL`, `WSL_DISTRO_NAME`, `XDG_SESSION_TYPE`) + TTY flags so rendering bugs on `TERM=dumb` / `LANG=C` / non-TTY pipes are visible in triage. **Event logger (3):** (8) `_bulk_align_worker` emits `alignment.bulk.summary` event (n_aligned/n_added/n_add_failed/n_failed/n_committed) so the diagnostic bundle captures the bulk-align outcome even after the toast is dismissed. (9) `_align_worker` and `_diff_align_worker` exception logs now name query/target ids + lengths + topology — pre-fix "alignment failed" bug reports lacked enough context to reproduce. (10) `_action_log` decorator's silent-except now leaves a `_log.debug` breadcrumb when `_log_event` itself fails — pre-fix a malformed payload silently swallowed the entry with no trace. **Pyright cleanup:** `pyrightconfig.json` adds `extraPaths: ["."]` (lets tests resolve `import splicecraft`), suppresses `reportUnreachable` + `reportUnnecessaryComparison` globally (defensive code at module + API boundaries is intentional), and adds an `executionEnvironments` block for `tests/` that suppresses Textual-Widget generic-type narrowing issues (`reportAttributeAccessIssue`, `reportOptionalMemberAccess`, `reportArgumentType`, …) — keeps main source strict while documenting tests' looser typing as intentional. Real bugs fixed in pass: `test_smoke.py::_timed` unbound `result`, duplicate method name in `test_genbank_io.py`. Workspace pyright now reports `0 errors`. Tests: `TestVariantExtractionCap` (4), `TestExtractVariantsMixedAndDivergent` (2), `TestPickedRotationEnumValidation` (6), `TestCoveragePctHelper` (6), `TestKmerSetForStrongMatchThreshold` (2), `TestAlignmentQualityStatusBoundaries` (5), `TestFlushAlignmentsLocked` (2). Existing test `TestMatchSamplesToLibrary::test_sequence_match_beats_name_substring` updated to use a non-repetitive 1500 bp seed (was 27 bp tandem repeat, below the new strong-match k-mer threshold). `test_sweep22.py::test_every_load_helper_uses_safe_load_json` updated to walk one level of call-graph indirection (`_load_library` now routes through `_ensure_library_cache_populated_and_migrated` post-INV-72 cache rewrite). |
| `[INV-88]` | Bulk-align confirm window: real per-sample alignment quality, aligned ONCE (2026-05-31). Follow-up to `[INV-87]` after the user noted the BulkAlignConfirmModal's "k-mer" column reads "100%" for a read with a known 1-bp mismatch. Cause is conceptual + cosmetic: the k-mer column is a **matching** score (canonical-k-mer Jaccard — "which library entry is this read?"), not alignment identity, and for a circular plasmid the origin-junction k-mers alone drop a *perfect* read to ~99.8% Jaccard (measured on JP4W9V: MAV40-1/2/3 perfect reads = 99.78% Jaccard; MAV40-4 with one SNP = 99.55%) — all of which the cell's `f"{kmer:.0%}"` then rounds to "100%". Massaging the k-mer would mislead the *other* way (perfect reads → "99.8%"). Fix surfaces the TRUE quality: **Ident / Mism / Gaps** columns showing each read's real `_pick_best_rotation(..., is_circular=<target topology>, mode="global", canvas_axis="target")` against its matched plasmid. **Aligned exactly once** — `_compute_bulk_quality` runs on the `_match_samples_worker` button-press thread (right after `_match_samples_to_library`, progress ticks the bar), aligns each target-bearing match, and stashes BOTH a display summary (`_aln = {ident, mism, gaps}`) and the full result (`_aln_result`) on the match dict; `BulkAlignConfirmModal` then only RENDERS `_aln` (no in-modal worker), and `_bulk_align_worker` REUSES `_aln_result` (verbatim if it carries `aligned_q`/`aligned_t`, else falls back to a fresh align) so the commit doesn't re-run the ~18 kb pairwise. **The first build of this feature aligned the same read THREE times** (modal-open preview worker `_quality_align_worker` + commit) — the modal "hung" re-aligning on open; the user flagged the double work, so it was reworked to align-once-up-front before shipping (the in-modal `_quality_align_worker`/`_apply_quality_row` + the modal's `zip_path` param were removed). Display routes through the pure `_bulk_quality_cells(aln, *, action, has_target)`: em-dash for non-`align` / no-target rows, "…" while pending (unreached now — `_aln` is always set before the modal opens), "?" on align failure, else honest `_format_identity_pct` (never a false "100%") coloured by `_identity_pct_color` + yellow mismatch / red gap counts. `_aln` is stripped from the confirm payload (`action_run`); `_aln_result` rides through for the commit's reuse. Verified read-only on JP4W9V: CAM 4 (MAV40-4) renders `99.99% · 1 · 0`, the three perfect reads `100% · 0 · 0`. Tests: `TestBulkQualityCells` (8), `TestBulkAlignConfirmModalQualityColumns` (2). |
| `[INV-95]` | Ctrl+E `EditSeqDialog` rework (2026-06-01, user request). The sweep-#39 modal kept its hardened bones (RadioSet + `_InstantPressButton` OK/Cancel, one-shot `dismiss` guard, `_sanitize_pasted_sequence`, IUPAC validation, `_MAX_INPUT_BP = 200_000` cap) — the user explicitly chose to KEEP the radios over a button rewrite ("keep the radios if those are more hardened"). Changes: **(1) Insert split into Insert LEFT / Insert RIGHT.** New internal modes `insert_left` / `insert_right`; `_MODE_INSERT = "insert"` retained as a back-compat alias `__init__` maps to insert_left (old callers + `test_modal_boundaries` pass `"insert"`). The DISMISS payload still speaks the App's language — insert_left → `(bases, "insert", start, start)`, insert_right → `(bases, "insert", end, end)` — so `_edit_dialog_result` + `_rebuild_record_with_edit` (feature-coord shift, sacred invariant #9) are unchanged; insert-right is just an insert at the region END, which the existing `_shift_range` handles. **(2) Colour-coded live warning** (`_build_preview_line`): "You are about to `[b <color>]<action>[/]` `[b <color>]<N> bp[/]` `[<color>]<position>[/]`" — green insert / red delete / yellow replace; position is "to the left of cursor" / "to the right of cursor" / "at cursor"; `<N>` is the TEXTBOX count for insert/replace (live as the user types) and the REGION count for delete. Rebuilt on every mode-change + `TextArea.Changed`. **(3) Region-prefilled textbox, always visible.** `on_mount` seeds the box with the region's bases; `_apply_mode_visibility` no longer HIDES the box for delete — instead it sets `TextArea.read_only=True` so it shows a read-only preview of the affected bases (label: "Bases that will be deleted (read-only preview):"). `_on_mode_changed` manages content: delete → show region; insert → clear an untouched prefill (or a preview carried over from delete) so it starts blank; replace → seed with the region when empty / arriving from a preview. **(4) Cursor = 1-bp region.** `action_edit_seq` opens a bare cursor on `[pos, min(pos+1, n))` (was an empty `[pos, pos)` insert point) — a single base edits like a selection; past the last base it falls back to a bare insert point. **`_cursor_pos` is the EXACT base under the cursor, no off-by-one:** a plain click sets `_cursor_pos = bp` (the clicked base; the shift-select path uses `e = max(anchor, bp) + 1`, half-open) and the renderer reverse-videos the same base (`is_cur = (cursor_pos == i)` in `_build_seq_text`), so `seq[pos:pos+1]` is precisely what the user sees highlighted — locked by a boundary-covering test. **(5) Ctrl+E default is NON-destructive; Delete key routes to Delete.** `action_edit_seq` (Ctrl+E) opens **Replace** for a selection / **Insert-left** for a bare cursor — NEVER Delete (user: "ctrl+e SHOULD NOT DEFAULT TO DELETE!!! huge hand slip risk"). The shared opener `_open_seq_edit_dialog(prefer=…)` takes `"context"` (Ctrl+E) or `"delete"`. `action_delete_feature` (the **Delete key**): with a feature selected it deletes the feature as before; with NO feature it calls `_open_seq_edit_dialog(prefer="delete")` → this dialog in Delete mode with the cursor bp / selection prefilled + Delete radio pre-selected + OK focused (a modal confirm — never an instant delete; undoable via `_edit_dialog_result`'s `_push_undo`), replacing the old "No feature selected" notify. Insert modes open with an EMPTY box (`on_mount` — a prefilled region would insert a duplicate). **Hardening — `_PREFILL_MAX_BP = 20_000`:** a region larger than the cap opens with an EMPTY box. Critical for Replace (whose submit READS the textbox) — a truncated prefill would silently commit truncated bases; and it avoids rendering a whole plasmid into the `TextArea`. Delete acts on the region (`[s,e]`), never the textbox, so nothing is lost; the label reads "(too large to preview — see the count above)". **Declutter (user: "ui crowded and lil messy"):** five horizontal radios overflowed the 66-col dialog content — **Copy was dropped** (redundant with the seq panel's Ctrl+C, and the user's original ask was four ops) and the redundant "Mode:" label removed, leaving four radios (64 cols) that fit; `min-width: 76` on `#edit-dlg` overrides a ~72-col render cap for ~6 cols of spare. **Radio highlight sync:** Textual's `RadioSet._on_mount` runs `action_next_button` → highlights index 0 regardless of which radio is `value=True`, so a non-first default (Delete via the Delete key, Replace for a selection) showed the `-selected` highlight box on Insert-left while a different radio was filled (user: "the delete radio is selected but the first radio is highlighted"). `on_mount` re-syncs via `rs._selected = rs.pressed_index` (deferred with `call_after_refresh`, guarded — private attr). **Post-edit highlight + scroll-into-view (2026-06-01):** on submit, `_edit_dialog_result` highlights the modified region (inserted / replaced bases, expanded to the new length) and centers it via `focus_span(s, s+len(new_bases), center=True, select=True)`; a pure deletion (`len == 0`) just parks the cursor at the splice point. **Critical interaction:** the async restriction-scan re-paint (`_dispatch_restr_scan` → `_apply`, `call_from_thread`) calls `update_seq`, which resets cursor + selection — it would WIPE the just-set edit highlight a beat later. `_apply` now snapshots + restores `_cursor_pos` / `_user_sel` / `_sel_range` / `_sel_anchor` across the overlay re-render (the sequence is unchanged — only the restriction overlay faded in). This also fixes a LATENT bug: every scan completion (on load, `r`-toggle, edit) used to silently clear the user's cursor + selection. Tests: `test_smoke.py::TestEditSeqDialog` (30 — alias→insert_left, insert_right preserved, left/right dispatch positions, warning action/count/position/colour, prefill + read-only, Ctrl+E selection → **replace** (not delete), Ctrl+E cursor → **insert-left** empty box, Delete-key-no-feature → Delete mode, **cursor-bp read is exact across boundaries (no off-by-one)**, **four radios fit within the dialog (no overflow)**, **radio highlight matches the checked mode**, **post-edit insert/replace highlights the modified region + delete parks the cursor**, huge-region empty box, plus the retained sanitize / delete-as-replace-empty / idempotent-dismiss cases). Help table + CHANGELOG updated. |
| `[INV-94]` | Ctrl+F sequence search + alignment-lane click navigation (2026-06-01). Two user requests. **(A) Alignment-lane click → seq-panel jump.** Pre-change, clicking an alignment row on the linear map pushed the `AlignmentScreen` pairwise-detail modal; the user wanted the click to instead jump the sequence panel to the clicked region (centered + highlighted) so a misaligned stretch / the exact base to re-edit is one click away. `PlasmidMap.on_click`'s alignment branch (`_align_at_click(x,y) >= 0`) now computes the canvas-bp span via `_linear_click_bp_span(x)` and posts `PlasmidMap.AlignmentLaneClicked(ai, bp_lo, bp_hi)` instead of pushing a screen; it deliberately DROPS the old `_selected_align_idx` reverse-video lane highlight (the 2026-05-22 gray-bar regression — and with no modal to close, nothing would clear it). App handler `_map_alignment_lane_clicked` drives the seq panel. The full per-base detail view stays reachable on **Alt+L → Enter** (Alignment Manager). **Column↔bp inverse:** `_linear_x_to_bp(x)` inverts `_draw_linear_flag`'s `bp_to_col` (`margin_l + (bp-view_s)*usable_w//visible_bp`), reusing the SAME float form `_feat_at_linear` already used (refactored to share it) so an alignment-row click resolves to the SAME bp as a feature-row click in that column; returns ABSOLUTE bp (the linear axis is absolute, NOT rotated by `origin_bp`), guarded to linear-mode + the usable band (margins → -1). `_linear_click_bp_span(x)` widens to the column's bp chunk (1 bp in letter mode, many in bar mode) so a zoomed-out mismatch fleck selects its whole bp range. **(B) Shared jump-to-bp primitive.** `SequencePanel.focus_span(start, end, *, center=True, select=True)` parks `_cursor_pos`, optionally marks a copyable `_user_sel` selection (only when span > 1, mirroring `action_add_feature`'s gate — so a clicked chunk / search match is Alt+Shift+F-annotatable AND Ctrl+C-copyable), and `center_on_bp` (vertical center) or `_ensure_cursor_visible`. Closes the "No public jump-to-bp helper" gap flagged in `_jump_to_library_entry_at_pos` (verification-report jump refactored onto it). ALL coords ABSOLUTE; the panel converts at render via `_abs_to_disp` (so a non-zero rotation still lands the right base — catastrophic-class for an editor people use to fix exact bp). **(C) Ctrl+F fuzzy sequence search.** `_search_subsequence(seq, query, *, max_mismatches, circular, both_strands, max_hits=_SEQ_SEARCH_MAX_HITS=10_000)` — Hamming (substitutions only, NO indels), IUPAC-aware via `_iupac_compatible`, both inputs scrubbed/validated through `_normalize_dna_for_align` (U→T, FASTA/whitespace/digit strip, `ValueError` on a foreign char). FORWARD (top-strand) coords for BOTH strands — a `-` hit means RC(query) sits on the top strand there (query anneals to the bottom strand), the same convention as sacred invariant #2's bottom-strand cuts. Circular scans `seq + seq[:m-1]` (a wrap hit's `end` may exceed `n`; `start` always in `[0,n)`); a palindromic query (`rc == q`) is skipped on the reverse pass and any same-span `-`/`+` pair de-dups to `+` so `n`/`N` never visits a bp twice. `SeqSearchModal` collects query + mismatch budget + both-strands, computes hits ONCE on submit, and dismisses with `{query, mismatches, both_strands, hits}` (a 0-hit / bad-char search STAYS OPEN with a status hint rather than dismissing). App: `action_find_sequence` (Ctrl+F) → `_on_seq_search` (stores `_seq_search`, jumps to the first hit at/after the cursor, wrapping) → `_goto_search_hit` (focus_span center+select + toast `idx+1/N · bp · strand · n/N`); `action_find_next`/`action_find_prev` step `n`/`N` with wrap. Each hit lands as a `_user_sel` selection so **Alt+Shift+F** annotates it on the spot (the find-a-repeat → annotate → find-next workflow the user described). **(D) Keybinding remap.** Ctrl+F `add_feature` → **`find_sequence`**; `add_feature` → **Alt+Shift+F** (on BOTH `PlasmidApp` and `SynthesisScreen`; the SynthesisEditor/ProteinEditor key-forwarders gained `alt+shift+f`). **NOT Alt+F** — Textual decodes the Alt+F byte sequence (`ESC f`, readline forward-word) as `ctrl+right`, so an `alt+f` binding never fires and the keystroke just nudges the seq cursor 1 bp right (user bug report 2026-06-01: "alt+f seems to just offset highlighted bases by 1bp to the right"); `ESC F` (Shift) decodes cleanly as `alt+shift+f`. Same `ESC`-prefix trap silently broke the latent `alt+b` BLAST-menu shortcut (`ESC b` → `ctrl+left`); that dead binding was REMOVED (user request) — BLAST is Ctrl+B (modal) + menu-bar click only. The File menu's `alt+f` was retired for the SAME reason (undeliverable, not because add-feature took it) — File stays mouse-only (its actions keep Ctrl+O/S/N). `find_sequence` is `priority=True` (sibling of `Ctrl+/` `find_annotation`, which searches feature ANNOTATIONS, not bases) with a self-screen guard so a second Ctrl+F can't stack another modal. `n`/`N` are NON-priority so they don't fire inside Inputs (you can type `n` in the query box). **Shift+n delivery quirk (verified empirically):** a standard terminal delivers Shift+n as the bare uppercase `"N"` (what Textual fires), NOT `"shift+n"` — only kitty / enhanced-keyboard protocols emit `"shift+n"`; bound BOTH so find-previous works on every terminal. **(E) Hardening:** mode-guard on the column→bp helper (circular → no nav, defense-in-depth over the `_align_at_click` gate); stale-search guards — `_goto_search_hit` drops the search if the sequence LENGTH changed mid-search, and `_apply_record(clear_undo=True)` clears `_seq_search` on a fresh canvas swap (catches a same-length plasmid swap the length-guard would miss); modal-stacking guard; matcher rejects empty / oversize query, negative `k`, and (via the normaliser) foreign chars. **Test-harness note:** monkeypatching live `post_message` / `push_screen` on a running app DEADLOCKS `run_test` teardown (broke the message pump) — assert click-nav via the screen stack + cursor instead. Tests: `test_seq_search_nav.py` (42 — matcher exact/RC/k-mismatch/IUPAC/circular-wrap/linear-no-wrap/palindrome-dedup/foreign-raise/RNA/FASTA/sort/cap; `focus_span` cursor/selection/single-bp/clamp/empty; click-nav span-helper / mode+margin reject / handler / missing-panel / on_click-navigates-not-drills-in; find-flow store+jump / next+prev wrap / no-search-safe / stale-guard / cursor-start / `n`+`N` keystrokes-fire / record-swap-clears / no-stack; binding remap incl. `N`). README (Features + Sequencing) + Help shortcut table + CHANGELOG updated. |
| `[INV-93]` | Construction history for every generated plasmid + bloat cap + legacy backfill (2026-06-01). Closes the gap where several plasmid-generation paths wrote no `history_xml` (reported: a Golden Braid clone "demo311" with an empty History tab — its stored `source` was `id:` = the generic canvas Save, NOT `constructor:`). **Origin fallback:** `_ensure_entry_origin_history(entry)` stamps a minimal `<HistoryTree>` root (`_build_origin_history_xml` → operation/manipulation from `_origin_history_descriptor(source)`: synthesizeDNA / pasteSequence / amplifyPCR / importFile / sequencingRead / createDocument) when an entry lands without history. Wired into the TWO save chokepoints — `LibraryPanel.add_entry` (generic Save / Synthesis / paste / file import) and `_commit_library_entry_to_collection` (PCR amplicon + universal workbench safety net). The rich workbench builders (`_build_history_for_assembly` / `_build_history_for_product` / `_build_history_for_gibson`) set `history_xml` BEFORE these, so the fallback no-ops for them. Root topology comes from `_gb_text_is_circular(gb_text)` (LOCUS-line read). **Move/copy-to-collection is EXCLUDED** — `_move_copy_commit` saves via `_save_collections` with per-entry deepcopy, never through the chokepoints, so a copy keeps the source's history and never mints a spurious root (user: "aside from moving the plasmid file to another location"). **Constructor enzyme-gate fix:** `_build_history_for_assembly` no longer returns None when `_enzyme_for_level_up` can't resolve an enzyme (it dropped ALL history over a missing detail — the demo311-class bug); the enzyme is now just an optional `RegeneratedSite` marker. **In-place edit append (user choice "append on actual change"):** `_maybe_append_edit_history` adds an `editSequence` node chaining the pre-edit version ONLY when a same-id re-save's SEQUENCE actually changed (cheap `len` check first; same-length → parse-and-compare the prior record); a no-op / features-only re-save adds nothing. **Bloat cap (history lives in BOTH plasmid_library.json AND collections.json; the edit chain re-nests the whole prior tree per edit):** `_truncate_history_to_recent(root, _HISTORY_TREE_MAX_NODES=256)` BFS-keeps the recent window + one `(earlier history truncated)` leaf; a prior history > `_HISTORY_XML_MAX_BYTES` (256 KB) collapses to a leaf instead of being re-parsed; a final byte ceiling drops to the bare edit node. Imported `.dna` history is NOT capped here — its own 32 MB `_COMMERCIALSAAS_HISTORY_MAX_XML` ceiling governs it. **Legacy backfill (CRITICAL data-safety):** `_backfill_origin_history(entries)` is ADDITIVE-ONLY — only ADDS `history_xml` where absent, NEVER removes/reorders/rewrites any entry or other field; one corrupt row is skipped (per-entry try/except), not dropped; idempotent. Wired into BOTH `_ensure_library_cache_populated_and_migrated` and `_ensure_collections_cache_populated_and_migrated` (embedded plasmids) under separate flags (`_origin_history_backfill_done`, `_collections_origin_history_backfill_done`; conftest force-sets both True so tests opt in). Each pass asserts the entry COUNT is invariant before persisting and ABORTS the whole save on any change — belt on top of `_save_*`'s L3 catastrophic-shrink guard + `.bak`/atomic chain; save failure is non-fatal (cache migrated, disk catches up next save). SnapGene/.dna history import verified intact. **History-viewer protocol clarity (2026-06-01 follow-up):** `_history_step_from_node` collects ALL regenerated-site enzymes (was `sites[0]` only — a KpnI+XbaI double digest showed just "✂ KpnI"; verified on real BATCH 55-C data), deduping + dropping the `(reverse insert)` sentinel. `_history_protocol_lines` now reads left → right like a recipe — `<op>  <ingredients> into <backbone>  →  <product>  ✂ <enzymes>` — with the forward arrow pointing AT the product (replacing the unclear backward `⟵`); an in-place edit (input == product) collapses to one labelled product. `_history_protocol_text` prepends a one-line symbol legend (shown in both viewers). `_HISTORY_OP_FRIENDLY` gained `amplifyFragment→PCR` + `editSequence→edit`. Tests: `test_origin_history.py` (41 — helpers, descriptor map, enzyme-gate, edit-append, bloat cap over 400 edits, additive backfill + count-invariant + idempotent + collections-embedded); `test_commercialsaas_io.py` protocol tests (all-enzymes, dedup/sentinel, legend present). **Protocol readability + harmonised history (2026-06-01 follow-ups):** the protocol pane renders a Rich `Group(legend, 2-col table)` via `_history_protocol_renderable` (`_history_protocol_step_cells` splits number from content) so a wrapped step HANGS-INDENTS under the content, not the next step's number. Amplify (PCR) detail: `_CommercialSaaSHistoryNode.oligos` parses `<Oligo>` primer children + `add_oligo` writes them; `_history_detail_lines` shows a **Primers** block (name + sequence) and the amplified **region** (`val1/val2`) instead of a bare "amplify ? ↔ ?" (SnapGene's amplify `InputSummary` carries no name1/name2 — the detail was in the `.dna` `<Oligo>`/`Parameter` children all along). **Uniform history regardless of origin:** `_build_amplicon_history_xml` makes a de-novo SpliceCraft amplicon record the SAME element set a `.dna` import does — an `amplifyFragment` node + `amplify` region `InputSummary` + primer `<Oligo>`s — wired into `SimulatorScreen._build_amplicon_library_entry` (was a bare `simulator:pcr` origin root). This propagates: when an amplicon is a part in a GB/Gibson/traditional assembly, `_parent_node_for_entry` nests its oligo-bearing subtree, so primers survive through multi-step builds. (Not yet generated: the cosmetic `.dna` `HistoryColors` / redundant `Features` / PCR `Parameter` elements.) |
| `[INV-92]` | Nuanced bar-mode alignment overlay shading (2026-06-01). Replaces the flat worst-state collapse (`[INV-87]`'s `_alignment_bar_columns`) for the zoomed-out (< 1 col/bp) linear overlay with a per-column composition SHADE so the blue/red/gray patchwork survives at full zoom-out (user: "see where it binds and where it doesnt … that granularity at max zoom too"). `_alignment_bar_column_shades(segments, view_s, view_e, bp_to_col, lo, hi)` accumulates per-column bp COUNTS `{col: (n_match, n_mismatch, n_gap)}` — walked per-bp over the visible window, so a lone mismatch bp is counted in its column alongside the surrounding match majority (no degenerate-range drop). `_alignment_shade_cell(n_m, n_x, n_g)` → `(glyph, rich_style)`: gap-dominant (`n_g > n_m + n_x`) → gray `░`; pure match → blue `█`; else a red shade glyph on a blue bg whose density tracks the mismatch fraction among ALIGNED bp via `_ALIGN_SHADE_TIERS` (`░` < 0.125, `▒` < 0.375, `▓` < 0.625), tipping to solid red `█` ≥ 0.625. So a single SNP in a ~164 bp cell → faint `░` red-on-blue fleck (visible, yet clearly mostly-binding); a half-mismatch cell → `▓`; a divergent read → solid red; a perfect read → solid blue. `_draw_alignment_band`'s bar branch derives the name-overlay `col_state` (gap-dominant→`gap` / any-mismatch→`mismatch` / else `match`) from the same counts. `_alignment_bar_columns` (worst-state) is retained for its tests but no longer drives the paint. Verified on real MAV 40 stored alignments: MAV40-4 (1 SNP) → mostly-blue bar with one `░` at the SNP column; the three 100% reads → solid blue; the 52% read → uniform `▒`. Tests: `TestAlignmentShadeCell` (11), `TestAlignmentBarColumnShades` (6). |
| `[INV-91]` | edlib fast-aligner engine (2026-05-31). `_pairwise_align`'s **global**-mode engine is now edlib (bit-parallel Myers edit distance) when available, falling back to Biopython otherwise. ~90–600× faster on the long near-identical sequences that drive QC: `_pick_best_rotation` on a real 18 kb Plasmidsaurus read drops from ~16–48 s to **~0.33 s**, with IDENTICAL identity / mismatch / indel counts (validated base-for-base on JP4W9V; edlib diverges from Biopython ONLY on low-identity alignments, which the rotation picker discards — and where the exact count is immaterial since the read is flagged divergent either way). `_edlib_align_global(q, t)` returns the gapped `(query, target)` strings; `_pairwise_align` keeps its IUPAC-aware counting (`_iupac_compatible`) AND hands the same IUPAC pairs to edlib as `additionalEqualities` (`_EDLIB_IUPAC_EQUALITIES` — 88 pairs built once from `_iupac_compatible`) so the alignment engine and the match counting agree on ambiguity (N vs A, R vs G, …). `score` is read off the Biopython row when that path runs, else reconstructed from the counts with the same scalar params (display-only — the picker ranks by identity, `[INV-76]`). edlib is OPTIONAL (`_EDLIB_AVAILABLE`, `# type: ignore[import-not-found]`) with a `sys_platform != 'win32'` marker in pyproject (mirrors pyhmmer — edlib's Windows wheels are unreliable, and the Biopython fallback covers it) plus a dev-dep so CI exercises the edlib path while `release.py`'s system-python run exercises the fallback. **Local mode stays on Biopython** (edlib has no local-SW equivalent; no caller passes `mode="local"` in practice). This makes the bulk-align process-parallelism idea moot — per-read align is now ~sub-second, so a whole run is seconds. **Hardening (rock-solid):** the edlib path is wrapped per-call — any edlib exception OR a **round-trip-guard** failure (the ungapped aligned rows MUST reconstruct the normalised inputs `q`/`t`; if not, edlib mis-aligned) logs a debug breadcrumb and falls back to Biopython for that single call, so a corrupt / mis-ordered edlib result can never reach the QC counts (Biopython is the universal safety net, reached via `if aligned_q is None`). `_log_startup_banner` records `edlib : <ver> (active …)` / `not installed — Biopython fallback` for the diagnostic bundle; the reconstructed score clamps its gap term (`max(0, …)`). Tests: `TestPairwiseAlignEngine` (8) — fallback-when-disabled, round-trip-guard-falls-back, edlib-exception-falls-back, round-trip-reconstructs-inputs, IUPAC-ambiguity-as-match, empty/oversized-rejected (all engine-agnostic via monkeypatch), + edlib≡Biopython near-identical equivalence and the `_edlib_align_global` round-trip (skip without edlib). All existing `_pairwise_align` / `_pick_best_rotation` suites pass under BOTH engines (4620 full-suite + 389 alignment-file under edlib; 387+2-skip under Biopython). |
| `[INV-90]` | Bulk-commit read-reuse + gap/indel count consistency (2026-05-31). **(1) Commit optimisation:** `_bulk_align_worker` now extracts + parses each read's ~38 KB consensus GenBank ONLY when it needs the sequence — to align (cache miss) or to add-as-new. When the matcher-phase `_compute_bulk_quality` already produced the alignment (`_aln_result` carries `aligned_q`/`aligned_t`), the commit reuses it and skips the re-extract/re-parse (an `align_reuse` flag is computed before the now-lazy extraction; the resulting `query_rec` Optional is guarded with unreachable `is None → n_failed; continue` clauses in the align-else and add branches to keep pyright at 0 errors). **(2) Gap/indel count consistency:** new pure `_alignment_indel_events(result)` returns indel EVENTS = `n_gap_opens_q + n_gap_opens_t` (gap runs per strand — a query gap run is a deletion, a target gap run an insertion; falls back to walking `aligned_q`/`aligned_t` for alignments stored before the gap-open fields existed, pre-2026-05-27). The AlignmentManager "Gaps" column and the bulk-confirm `_aln["gaps"]` now use it instead of `n_gap_cols` (gapped bp), so all three QC surfaces agree: a 5 bp deletion reads "1" in the Manager "Gaps", the bulk "Gaps", AND the VerificationReportModal "Indels" (which counts the same gap runs via `_extract_variants_from_alignment`). Mism/SNPs already agreed (each mismatch column is one SNP). Tests: `TestAlignmentIndelEvents` (7, incl. a `_extract_variants_from_alignment` equivalence lock). |
| `[INV-89]` | Honesty-pass completion + bulk-prealign cancellation + k-mer relabel (2026-05-31). Follow-up to `[INV-87]`/`[INV-88]`. **(1) Honest %, remaining display sites:** the three identity displays `[INV-87]` missed — `_align_worker`'s post-align toast ("X% identity (Y% in matched region)"), `_diff_align_worker`'s Alt+\\ toast ("X% identity (aligned region)"), and `AlignmentScreen._summary_md`'s detail line (was `.2f`) — now route through `_format_identity_pct` (toasts at the default 1 dp, the detail panel at `decimals=2` to keep its precision) so a 1-bp-off read never shows "100.0%"/"100.00%". **(2) Bulk-prealign cancellation:** `_compute_bulk_quality` polls `get_current_worker().is_cancelled` each iteration (a 30-sample run is 30 × ~18 kb pairwise — pre-fix an exclusive restart or screen-unmount couldn't stop the C-loop until it ground through every remaining sample), and `_match_samples_worker` re-checks `is_cancelled` before pushing the confirm modal so a cancelled worker can't stack a SECOND `BulkAlignConfirmModal` over a fresh one (a real double-click bug now that the matcher phase runs minutes of alignment, not just k-mer matching). **(3) k-mer column relabel:** `BulkAlignConfirmModal`'s "k-mer" header → "k-mer match" so a strong-but-imperfect read's "100%" k-mer score reads as pairing confidence, not alignment quality (the Ident/Mism/Gaps columns carry quality — the confusion that prompted `[INV-88]`). **(4) Narrow-terminal regression lock:** `TestWidenedModalsAtNarrowWidth` mounts the widened bulk-confirm (10 cols) + Alignment Manager (9 cols) tables at 80×24 and asserts the dialog stays within the viewport — CSS already clamps (bulk-dlg `min-width:80`/`max-width:140`, alnmgr-dlg `width:100`/`max-width:95%`); the over-wide table scrolls horizontally rather than pushing the modal off-screen. Tests: `TestWidenedModalsAtNarrowWidth` (2); existing format / manager / bulk suites green. |
| `[INV-87]` | Alignment honesty + zoomed-out mismatch visibility + manager mismatch/gap columns (2026-05-31). User report: an alignment that registered at 100.0% identity rendered GREEN in the AlignmentManagerModal — a one-bp mismatch in an 18 kb plasmid whose `result['identity_pct']` was 99.994% but displayed via `f"{x:.1f}%"` = "100.0%", while `_identity_pct_color` (strict `>= 100.0` → light-blue) correctly kept the cell green. The number and the colour disagreed about perfection. **(1) `_format_identity_pct(pct, *, decimals=1)`** — new pure formatter used by `AlignmentManagerModal._repopulate` + `VerificationReportModal._add_row`: escalates decimal places (1→4) until the rendered number is strictly `< 100` so a sub-100 identity never reads "100%" (e.g. "99.99%"); a genuine 100.0 renders the clean "100%" (no decimals — true-perfect is visually distinct from near-perfect); a value rounding to 100 even at 4 dp → "<100%"; None / non-numeric → "—". Shares the strict `>= 100.0` boundary with `_identity_pct_color` so number and colour can never contradict. **(2) `_alignment_bar_columns(segments, view_s, view_e, bp_to_col, lo, hi)`** + `_ALIGN_STATE_PRIORITY` — bar-mode (zoomed-out, < 1 col/bp) linear overlay (`PlasmidMap._draw_alignment_band`) now collapses segments to per-column WORST state (mismatch > gap > match) instead of painting each segment's `range(bp_to_col(s), bp_to_col(e))` in paint order. Pre-fix a lone 1-bp mismatch was invisible at high zoom: its column range was degenerate (empty `range`) AND the following match run repainted the same column blue. Now every in-view segment claims ≥1 column and priority (not paint order) decides the colour, so a single mismatched base shows RED even when one terminal column spans hundreds of bp (user: "show red character in region where mismatch occurred even if one bp"). Letter mode (≥ 1 col/bp) paths unchanged — each bp already owns a column there. **(3) AlignmentManagerModal "Mism" / "Gaps" columns** — quantify mismatched bases (`n_mismatches`) + gapped bp (`n_gap_cols`, legacy `n_gaps` fallback) per stored alignment for a fast "how bad is it" read without drilling in; nonzero cells draw the eye (yellow / red, mirroring the VerificationReportModal SNP/indel columns), a clean "0" recedes dim, hidden rows dim all. Tests: `TestFormatIdentityPct` (12), `TestAlignmentBarColumns` (10) in `test_alignment_overlay.py`. |
| `[INV-86]` | Sweep #30 — final pre-1.0 deep audit (10 parallel read-only agents, 2026-05-28). **Data integrity (the crown jewels):** (1) `_safe_save_json` Step-3 inline `os.fsync` now RE-RAISES (was `except OSError: pass`) — the last save path that swallowed fsync failures; mirrors the audit-2 H2 hardening in `_atomic_write_text`/`_atomic_write_bytes`. On EIO/ENOSPC at fsync the rename used to proceed and the call returned success over a non-durable write (UI showed "saved"). (2) `_safe_save_json` Step-1: an `OSError` reading an EXISTING prior file now REFUSES the save (raises) rather than warn-and-proceed — overwriting an unreadable-but-present file destroys un-backed-up data AND the shrink guard can't fire (existing_count stays 0). (3) `_authoritative_library_snapshot` helper: the LibraryPanel disk-writer workers (`_add_save_to_disk`/`_delete_save_to_disk`/`_rename_save_to_disk`) now persist the live `_library_cache` (always ≥ any in-flight dispatch-time payload, since every dispatch site reseats the cache synchronously first) instead of the captured payload — closes the documented cross-group out-of-order-write race that could resurrect a just-deleted/renamed entry on disk (cache stayed correct; disk diverged until next mutation). The collection mirror (`_sync_active_collection_plasmids`, which clones its arg) uses the same snapshot. (4) Agent library RMW handlers `_h_delete_from_library`/`_h_set_plasmid_status`/`_h_add_current_to_library` (+ `LibraryPanel.add_entry`'s id-match / no-collision / `_on_resolved` commit paths) now hold `_cache_lock` across load→mutate→save — UI-thread serialisation does NOT exclude worker-thread `_cache_lock` holders (e.g. `_h_gibson_assemble`), so a lockless `_load_library()` could be clobbered. Unfixed [INV-74] instances. **Privacy [INV-38]:** `_extract_recent_events` scrubs every top-level string field value via `_scrub_path`, so the F9 bundle's `events_summary.json` can't leak home/data-dir paths (the human-readable log copies were already scrubbed; this is the JSON-summary chokepoint). `save.ok`/`update.snapshot.ok` emit sites scrubbed too. **Robustness:** app-wide `@on(Worker.StateChanged)` net on `PlasmidApp` — an exception escaping any `@work` body (Textual logs only to its devtools logger; `threading.excepthook` doesn't cover pool workers) now toasts + logs (`worker.error` event) instead of a silently frozen panel. `_user_data_dir` mkdir + `Path.home()` wrapped — a misconfigured `$SPLICECRAFT_DATA_DIR` (a file / non-dir path) or unset `$HOME` no longer hard-crashes at IMPORT; falls back to a temp dir with an stderr notice. `main()` rejects a leading-dash positional (`splicecraft --bogus`) with exit(2) instead of handing it to `fetch_genbank` (confusing "Fetch failed"). `_enzyme_cuts_impl` try/excepts `_iupac_pattern(site_u)` per enzyme — a corrupt `custom_enzymes.json` site no longer crashes trad-cloning Simulate / MoClo `@work` digests (parity with [INV-85]'s `_rebuild_scan_catalog` guard). `_gibson_normalize_fragments` maps RNA `U`→`T`. `_parse_pypi_version` requires pure-ASCII-digit segments (rejects `"+5"`/`"1_0"`/Unicode digits/empty). `_resolve_pypi_url` redacts override-URL credentials in warning logs. **Security:** `_hmmer_web_parse_json` strips control bytes from `clan`/`type`/`link` (the gap acc/name/desc strips left); `_online_render_blast`/`_online_render_hmm` wrap remote text cells in `Text(...)` so DataTable renders them LITERALLY (Rich-markup-injection defense); `_online_http` now routes through `_build_hardened_url_opener` (https-only, bounded redirects, https→http downgrade refusal) — was the default global opener; `_fetch_hmm_db_remote_version` bounds gzip decompression to `_HMM_DB_VERSION_MAX_BYTES` via streamed `GzipFile.read(cap+1)` (gzip-bomb defense; pre-fix one-shot `gzip.decompress`); `_normalise_experiment_entry` strips `body_md` (`_NOTE_CTRL_RE`) + tags (`_sanitize_label`); `_normalise_gel_entry` routes notes through `_sanitize_note`. **Genetic codes (wrong-biology fix):** `_codon_table_for(table_id)` resolves the codon→AA map for any NCBI genetic-code id (Biopython `unambiguous_dna_by_id`, cached; table 1 returns the hand-rolled `_CODON_TABLE` fast-path; unknown id → standard code + warning, never crash). `_cds_aa_list` reads `f['transl_table']` — so the seq-panel protein AND the `_premature_stops` count it feeds both honour it — and `_translate_cds` gained a `transl_table=` arg (passed by the AA-copy path); `PlasmidMap._parse` stamps `/transl_table` on CDS features (only when present and != 1). A vertebrate-mito / Mycoplasma / Spiroplasma CDS now translates correctly and a reassigned stop (e.g. `TGA`→Trp in tables 2/4/5) no longer false-flags a premature-stop ⚠. `_find_orfs` + `_mut_translate` deliberately stay table-1 (ORFs are de-novo; the mutagenesis WT-check is a niche-of-niche). Tests: `TestTranslTableNonStandard` (6). **Completeness:** "Transfer annotations from…" restored to the File menu (the working `action_transfer_annotations`/`AnnotationTransferModal` feature was un-wired when commit `bac27c2` dropped the menu line; unit tests exercised it in isolation so the suite stayed green and masked the gap). **UI whimsy (user request):** MenuBar "Mutagenize" → "Mutato" (label + `on_click` dispatch + test + README/keybindings; internal `action_open_mutagenize`/`MutagenizeModal`/worker-group names unchanged). **Doc:** `_h_search_library` stale "Edit → Find plasmid…" → "File → Find plasmid (all collections)…". Tests: `TestSweep30SafeSaveJson` (fsync-failure raises; unreadable-prior refuses), `TestAuthoritativeLibrarySnapshot` (2); existing suites green. **Verified-and-DECLINED (not regressions — recorded so future audits don't re-chase):** (a) SynthesisScreen featlib/motif per-keystroke `_load_features()` is NOT a real freeze: `_typed_clone` shares immutable sequence strings by reference, so the clone copies small dicts, not the kb blobs — the UI-perf agent overstated it. (b) Several "orphaned" `action_*` wrappers (`toggle_feature_tooltips`/`toggle_constructor_filter`/`open_entry_vectors`/`restore_from_backup`/`set_min_primer_binding`) are unreachable from the UI but ARE exercised by tests as programmatic entry points — left intact rather than break the suite. (Note: the non-standard `/transl_table` item this list originally deferred was IMPLEMENTED in this sweep — see **Genetic codes** above.) (c) `_h_set_active_primer_collection` mirror gap, `_h_restore_backup`/`_h_set_active_collection` multi-lock-window, HMMER `job_id` regex, lockfile-unlink TOCTOU — low-severity, noted for a future pass. |
| `[INV-85]` | Sweep #29 — final pre-1.0 hardening + completeness (2026-05-28). **Correctness:** `_mut_design_modified_outer` must SPAN the mutant codon (takes `nt_start`, returns None → SOE fallback) — pre-fix a near-end mutation folded into an outer primer could omit the change (silent wild-type product); `VerificationReportModal._collect_rows` numbers variants in the QUERY frame for `axis="query"` diffs; `_alignment_quality_status` clamps coverage via `_coverage_pct_from_result` (no >100% → false ✓); `_commit_product_to_library` keeps origin-wrap features as `CompoundLocation` ([INV-44]); `_rebuild_scan_catalog` try/excepts each enzyme so a corrupt `custom_enzymes.json` site can't crash import; forward-strand restriction scan + `_enzyme_cuts` (both strands) got the negative-cut linear-wrap guard the restriction-overlay reverse strand already had (the `_enzyme_cuts` reverse-strand path was completed in the pre-1.0 verification follow-up — a reverse-bound Type IIS site whose cut fell off a LINEAR molecule's 5' end was emitting a phantom 3'-end cut via `_emit`'s `% n`); `_gibson_validate_body_lengths` validates fragment-0's combined arms (circular n>1); mutagenesis stop-codon respects the selected codon table. **Data integrity:** `_h_delete_experiment_project` + `_h_bulk_import_folder` do their full RMW under `_cache_lock` (in-lock collision re-check); `_delete_experiment_attach_dir` + `_cleanup_pressed_files` routed through `_refuse_unauthorized_delete`. **Security:** `_CONTROL_CHARS_RE` strip on remote BLAST/HMM hit text (`_ncbi_blast_parse_xml` / `_hmmer_web_parse_json`) + agent gel/experiment/grammar/part/primer/feature fields; `_hmm_db_build_url_opener` → `_build_hardened_url_opener` (shared by the PyPI update fetch + HMM-DB downloader) overrides `redirect_request` to refuse https→http downgrade; NCBI BLAST poll tolerates transient 5xx (consecutive-fail) and deletes the RID on ANY abort; `_fetch_hmm_db_remote_version` GET applies the content-type guard. **UI:** `--no-splash` runs the post-splash gate via the main()-only `_run_post_splash_when_skipped` flag (What's New + `last_seen_version` bump no longer skipped); settings-flush broad-except now `_bg_notify_save_failure`s; 5 modal-open `_load_library`/`_load_collections` deepcopies → readonly iterators (`BlastModal`, `MultiAlignPickerModal`, `MoveCopyToCollectionModal`, Mutagenize select, `AddFeatureModal._import`). **Completeness (new/wired):** ORF finder reachable via File → "Find ORFs in this sequence…"; manual feature grouping wired (FeatureEditModal "Group with…"/"Ungroup" → `GroupFeaturePickerModal` / `UngroupScopeModal` → `_apply_feature_group_with` / `_apply_feature_ungroup`); protein-motif editor New/Delete (`NewMotifModal` + shared `_protein_motif_upsert` / `_protein_motif_delete`); codon-table TSV paste import (`CodonTsvImportModal` + `_parse_codon_tsv`); agent endpoints `list`/`set-active-parts-bin` (mirror via `_safe_save_json_mirror`, [INV-83]) + `list`/`get`/`set-active`/`delete-hmm-database`. **Cleanup:** removed dead `action_edit_custom_enzyme_list` / `action_toggle_custom_enzyme_list`; fixed stale File-menu `[q]`→`[^Q]` hint + the dead "File > Codon tables" toast pointer. Tests: `test_codon` (15 TSV-parser), `test_synthesis` (6 motif-editor), `test_new_agent_endpoints` (11 parts-bin/HMM), + mutagenize/alignment/gibson/trad-cloning updates. |
| `[INV-84]` | Sweep #28 HMM database registry + download manager (2026-05-25). Adds `hmm_db_catalog.json` + per-DB dirs under `<DATA_DIR>/hmm_databases/<id>/{db.hmm,db.hmm.gz,db.hmm.h3*,meta.json}`. Two builtins ship out of the box (Pfam-A from EBI, NCBIfam from NCBI); user can add custom URLs via `HmmDbAddEditModal`. **Sacred network discipline:** every internet-facing helper (`_fetch_hmm_db_remote_version`, `_stream_download_to_path`) goes through (a) `_hmm_db_url_scheme_ok` for HTTPS-only-by-default + opt-in `hmm_db_allow_http`; (b) `_hmm_db_build_url_opener` for bounded redirects (5 max) + explicit `ssl.create_default_context`; (c) `_hmm_db_assert_content_type_ok` to refuse `text/html` / `application/json` / `application/xml` (CDN error pages); (d) gzip magic + HMMER3 magic verification on first chunk so a wrong-file download fails fast; (e) `_hmm_db_check_disk_space` pre-check requiring ≥ 2.5× the Content-Length free; (f) one retry with 250ms backoff per `[INV-37]`; (g) cancel-aware (`cancel_check_cb` polled between chunks; modal close = cancellation); (h) `_redact_url_credentials` on every logged URL. **Atomic state:** download → `db.hmm.gz.download_tmp` → fsync → `os.replace`; decompress → `db.hmm.gz_tmp` → fsync → `os.replace`; hmmpress failure cleans the `.h3*` siblings via `_cleanup_pressed_files` so `_hmm_db_pressed` doesn't falsely report ready. **n_profiles == 0 → failure** (post-press validation refuses to record success for an empty index). **Cross-modal slot:** `_HMM_DB_DOWNLOAD_INFLIGHT` set keyed by id stops a second modal instance from starting a parallel download of the same DB. **L2 chokepoint covers** `_stream_download_to_path` + `_decompress_gz_to_path` + `_save_hmm_db_local_meta` (writes) and `_delete_hmm_db_files` (deletes). **Catalog hygiene:** `_normalise_hmm_db_entry` is run on every load+save, so a hand-edited `hmm_db_catalog.json` can't smuggle un-sanitised ids or URLs; builtins are re-injected if removed (defensive — UI doesn't allow deletion). `_sanitize_hmm_db_id` is ASCII-only (a-z, A-Z, 0-9, `_`, `-`; ≤64 chars; no `..`/NUL/path separators) because the id doubles as a filesystem path component. `_sanitize_hmm_db_url` rejects whitespace/control chars in the RAW input (pre-strip) so a pasted URL with hidden newlines surfaces an error rather than being silently scrubbed. **Settings:** `hmm_db_active_id` (picked DB), `hmm_db_allow_http` (opt-in http), `hmm_db_path` (legacy; the modal still writes through for back-compat). **Auto update-check:** on `BlastModal` mount + on picker change, `_hmm_db_should_check_remote` gates a 24h-cached network poll; manual "Check now" button in `HmmDbCatalogModal` forces a re-poll. Tests: `tests/test_hmm_db_catalog.py` (57 cases). |
| `[INV-83]` | Sweep #27 mirror-write helper `_safe_save_json_mirror` + AST regression scanner (2026-05-25). LIVE INCIDENT: a user switched parts bin from "Eden Parts" (26 parts) → "FFE Parts" (0 parts); the L3 catastrophic-shrink guard saw 26 → 0 entries and refused the write — user's data was safe (the L3 spillover dumped to `lost_entries/`, the Eden parts intact in `parts_bin_collections.json`) but the bin-switch failed with no UI recovery path. Root cause: the `parts_bin.json` write is a MIRROR of the selected bin inside `parts_bin_collections.json` (the bin file is the source-of-truth); a "shrink" here is a legitimate state transition, not data loss. Sweep introduces `_safe_save_json_mirror(path, entries, label)` which wraps `_safe_save_json` in `_allow_catastrophic_shrink()`. Four mirror-swap sites converted: `_restore_library_from_active_collection` (startup collection mirror), `_restore_primers_from_active_primer_collection` (startup primer mirror), `PartsBinPickerModal._open` (UI bin switch — the incident), `ExperimentProjectsPickerModal._open` + `._do_delete` (UI project switch + delete-with-auto-promote), `_h_set_active_experiment_project` + `_h_delete_experiment_project` (agent project switch + delete), `LibraryPanel._collection_switch_save_to_disk` (UI collection switch). Regression test `TestMirrorWriteHelper::test_no_bare_safe_save_json_against_mirror_files` does an AST walk of `splicecraft.py` and asserts no `_safe_save_json(<MIRROR_FILE>, ...)` call sits outside an allowlist of canonical writers (`_save_library`, `_save_parts_bin`, `_save_primers`, `_save_experiments`, `_safe_save_json_mirror`) or audited in-place save workers (`_add_save_to_disk`, `_delete_save_to_disk`, `_dom_primers_save_to_disk`, `_trad_save_to_disk`, `_primer_status_save_to_disk`, `_rename_save_to_disk`, `_delete`). A new mirror-swap site that forgets the helper trips the test at the next run. |
| `[INV-82]` | `@work` decorator must carry `thread=True` (sweep #27). Textual's `@work` defaults to `thread=False` (async coroutine on UI thread); a bare `@work` or `@work(exclusive=True)` silently runs heavy work on the UI thread and defeats the worker contract. `TestWorkDecoratorAlwaysThread` does an AST walk over `splicecraft.py` and asserts every `@work(...)` decorator has `thread=True` as a literal kwarg. Catches the next misconfigured worker mechanically. |
| `[INV-81]` | `_safe_data_repr(value, *, max_len=32)` returns type/length only, never raw value (sweep #27). Use in user-facing error messages (toasts, modal Static.update) instead of f-string interpolating sequences, names, or other content that could carry embedded ANSI/terminal control codes from a malicious paste. Returns `"<type(len=N)>"` for sized values; `"<type>"` for scalars; `"None"` for None. Tested by `TestSafeDataRepr` (7 cases). |
| `[INV-80]` | Agent API idempotency-key support (sweep #27). Optional `X-Idempotency-Key` header on write endpoints; valid key format `[A-Za-z0-9_-]{1,128}` (validated by `_agent_validate_idempotency_key`). On hit, replay cached `(payload, status)` for 60 s without re-invoking the handler; replays tagged with `_idempotent_replay: true`. Cache capped at 1024 entries with oldest-first eviction. Read endpoints deliberately NOT cached. Cleared on `_stop_agent_api`. Tested by `TestAgentIdempotency` (5 cases). |
| `[INV-79]` | Golden-file regression for persisted JSON envelopes (sweep #27). `TestGoldenFileEnvelope` pins (a) envelope keys = `{"_schema_version", "entries"}`; (b) `_schema_version == 1`; (c) on-disk text is multi-line indented for `git diff` legibility; (d) round-trip preserves entries verbatim; (e) legacy bare-list format still loads. A regression in the writer (key sort drift, schema bump without migration, accidental compact serialisation) gets caught mechanically. |
| `[INV-78]` | Single time source `_now()` / `_monotonic()` (sweep #27). Every wall-clock and monotonic-clock read SHOULD route through these helpers. `_now()` returns tz-aware `datetime`; `_monotonic()` returns `float` (seconds). Existing 18 direct `datetime.now()` callsites can migrate incrementally; new callsites MUST use the helpers. Side benefit: tests monkeypatch one symbol instead of chasing every callsite. `_save_ui_snapshot` migrated as the first example. |
| `[INV-77]` | Concurrency fuzz test (sweep #27). `TestConcurrencyFuzz::test_fuzz_save_load_round_trip` spawns 8 threads running random `_load_library` / `_save_library` / `_iter_library_readonly` for 1.5 s with a fixed seed, then asserts the post-run invariants: every entry is dict, every id is non-empty str, no duplicate ids, JSON on disk parses cleanly. Would have caught the 20 RMW races sweep #26 fixed one-at-a-time. |
| `[INV-76]` | Crash-injection regression for `_safe_save_json` (sweep #27, POSIX only). `TestCrashInjectionSafeSaveJson::test_recovery_after_sigkill` writes a small payload, then SIGKILLs a child process mid-write of a large payload; asserts the post-crash state recoverable via `_safe_load_json`'s `.bak` chain is ONE OF the two known good payloads, never torn. Codifies the atomic-write contract from `[INV-37]`. Skips on Windows (no `os.kill(pid, SIGKILL)`). |
| `[INV-75]` | L2 chokepoint extended to delete operations (sweep #27). New `_refuse_unauthorized_delete(path, label)` mirrors `_refuse_unauthorized_write`; shares the `_SAVES_AUTHORIZED` flag (one is implied by the other). Routed from `_delete_dna_original`, `_clear_autosave`, `_check_crash_recovery`'s stale/over-cap pruning. Future delete helpers added under `_DATA_DIR` MUST route through this gate. Same threat model as the write chokepoint: an unsandboxed probe script that calls a delete helper would otherwise destroy user data without the sandbox guard. |
| `[INV-74]` | Sweep #26 — full adversarial audit (4 parallel agents, 2026-05-25). Meta-finding: sweep #25 (`[INV-66]`) hardened a representative subset but didn't propagate uniformly to sibling code paths. This sweep closes the propagation gap. **Data integrity (5):** (1) **L2 chokepoint extended** to `_save_dna_original`, `_delete_dna_original`, `_save_experiment_image`, `_do_autosave`. Pre-fix the chokepoint covered 13 JSON subsystems but not `.dna` sidecar bytes, experiment image attachments, or crash-recovery autosaves — an unsandboxed probe could still overwrite user sidecars by id collision. (2) **20 agent endpoint RMW sequences** now wrap load+mutate+save in `with _cache_lock:` — gels (create/update/delete), grammars (create/update/delete), primers (create/delete), parts (create/update/delete), feature-libraries (create/update/delete), experiments (create/update/delete), experiment-projects (create/rename/delete), protein-motifs (set/delete), codon-tables (delete), collections (delete/rename), gibson-assemble, set-active-experiment-project. Sweep #25 fixed `_h_create_custom_enzyme` to this template but skipped the rest. Concurrent agent calls could otherwise drop entries silently (load same snapshot → both append → second save wins). Name-collision checks moved INSIDE the lock so two concurrent creates with the same name can't both pass. (3) **10 `_load_*` first-load populates** under `_cache_lock` — `_load_gels`, `_load_custom_enzymes`, `_load_enzyme_collections`, `_load_features`, `_load_protein_motifs`, `_load_feature_colors`, `_load_settings`, `_load_custom_grammars`, `_load_experiment_projects`. Pre-fix a cold-load could read pre-save disk state and overwrite a concurrent `_save_*` worker's freshly-locked cache. (4) **`_set_setting` global cache assignment** under `_cache_lock` so two concurrent agent toggles can't drop each other's keys (last-writer-wins on the global global rebind). (5) **`_rename_library_entry` cache reseats** (`_library_cache` + `_parts_bin_cache`) under `_cache_lock` so the UI-thread reseat can't clobber a worker's freshly-locked write. **TOCTOU fixes (3):** (6) **`_h_replace_sequence`, `_h_apply_gff3`, `_h_transfer_annotations`** capture `(rec, counter)` atomically in a single `call_from_thread` UI-tick — pre-fix the counter was read on the worker thread on a separate tick, so a UI load between the rec snapshot and the counter capture made `_apply`'s counter check pass against a stale `rec` and commit OLD-derived edits onto the NEW canvas. **Agent API hardening (6):** (7) **Path-info scrub** — `_h_status`, `_h_save`, all `OSError` export-failed branches route `source_path` / `exc.strerror` through `_scrub_path` (defeats the path-shape oracle). (8) **`_h_apply_gff3`** gains `_check_agent_read_path_ancestors` (mirrors `[INV-66]` plasmidsaurus path-ancestor refusal) and collapses 404 to opaque "file rejected" (closes 404-vs-400 path-existence oracle). (9) **`_h_hmmscan` `FileNotFoundError`** collapsed to opaque "hmm file not acceptable" (closes TOCTOU race-window 404 oracle that pre-existed `[INV-66]` upstream check). (10) **`_h_load_entry` 404** drops the `available` enumeration list (pre-fix any miss-key returned the user's 50 most-recent library names — a token-holder local attacker enumeration oracle). (11) **`_h_classify_part`** caps `features` list at 10,000 entries (pre-fix only the 1 MiB body cap upstream; a payload of micro-dicts could saturate the classifier). (12) **`_h_set_protein_motif`** routes `name` / `feature_type` / `description` / `color` through `_sanitize_label` so an agent can't smuggle terminal escape codes into the on-disk JSON. **Performance (6):** (13) **`_find_collection` / `_collection_name_taken`** walk `_iter_collections_readonly()` instead of deep-cloning the full 160 MB collections.json on every call (13 call sites; agent `bulk-import-folder` paid 2× per call). (14) **`LibraryPanel._repopulate`** + the two `_repopulate_*` subroutines switched from `_load_library() + _load_collections()` (full deepcopy) to the readonly views — every search-Enter on a 156 MB lib + 160 MB collections was a 2–4 s freeze; now near-instant. (15) **`_hydrate_alignments_for_active`** uses `_iter_library_readonly()` to build `library_by_id` and `_typed_clone(matched_entry)` only for the target — pre-fix every record load (library row click, fetch, undo, redo) paid the full 156 MB deepcopy before the canvas painted. (16) **9 find-by-id callsites** swapped to `_find_library_entry_by_id` — history viewer, CommercialSaaS export, annotation transfer, rename collision check, `_h_export_commercialsaas`, `LibraryPanel` rename, `LibraryPanel` cascade-rename, etc. Pre-fix each call deep-cloned the entire library to read one entry's fields. (17) **`action_spellcheck`** moved off UI thread via `@work(thread=True, exclusive=True, group="spellcheck")` — pure-Python pyspellchecker over a 500 kB body froze the UI 5–15 s with no progress indicator (users assumed crash). Worker pattern shows immediate "Spellchecking…" toast + posts modal from `call_from_thread`. (18) **`FeatureLibraryScreen._repopulate_collections/plasmids`** fallback paths also routed through readonly views. **Correctness (1):** (19) **`_h_list_library`** projection bug fixed — pre-fix the handler read non-existent JSON fields (`sequence`, `features`, `topology`, `source_path`) and returned `{length: 0, n_features: 0, topology: ""}` for every entry. Now projects the real fields (`size`, `n_feats`, `source`) and pulls topology out of the GenBank LOCUS line when available. **UI hardening (15):** (20) **`_blocks_undo`** added to 9 modals (BlastModal, LibrarySearchModal, LoadPartSourceModal, PartsBinPickerModal, ORFFinderModal, EditGrammarConfirmModal, _ConfirmDeleteGrammarModal, LargeFileConfirmModal, MultiAlignPickerModal, AnnotationTransferModal, BulkAlignConfirmModal, DropdownScreen) — pre-fix Ctrl+Z in modal Input/TextArea leaked to the canvas underneath, reverting the record instead of undoing the typo. (21) **`_dismiss_once`** added to 4 modals (MutagenizeModal, ExperimentRenameModal, UpdateAvailableModal, OpenFileModal) — pre-fix double-click on a button (especially with a worker mid-flight on the same modal) could fire two `dismiss()` calls and trip Textual's screen-stack underflow. (22) **`FeatureLibraryScreen.action_remove`** now prompts via `LibraryDeleteConfirmModal` — pre-fix plain Delete key wiped the row with no confirmation while every other delete path prompts. (23) **`SettingsModal._on_check_updates` + `_on_click_debug`** route through the canonical `action_toggle_*` methods so the informational toasts fire and the click-debug counters reset on toggle (pre-fix the orphan `action_toggle_check_updates` was unreachable from the UI). Tests: existing test suites pass — `tests/test_dna_sanity.py` (135 cases), `tests/test_agent_api.py + test_data_safety.py + test_collections.py` (358 cases). |

---

## [INV-36] Future-proofing scaffolding
Six additive mechanisms:
* `_ENTRY_MIGRATIONS` per-label `(from_v, to_v) → Callable[[dict], dict]`. `_extract_entries` runs every load through `_migrate_entries`. Failed migrators preserve entry + warn (never drop data).
* `$SPLICECRAFT_PYPI_URL` env override (http/https only, ≤2048 chars). No caching.
* Pre-update snapshots record `from_python_version` + `from_platform`. **`_RUNTIME_PLATFORM` cached at import** because `platform.platform()` shells out via subprocess.
* `--dry-run` exercises detection/PyPI/snapshot then bails. Mutex with `--check`.
* `<DATA_DIR>/.splicecraft-data-version` stamp; `_check_and_stamp_data_version()` warns to stderr on downgrade. Atomic write via `_atomic_write_text`, read capped at 128 bytes.
* `_PLUGINS_DIR = _DATA_DIR / "plugins"` + `_RESERVED_ENTRY_FIELDS = ("_plugin_data",)`. Tested by `TestFutureProofingFeatures`.

## [INV-37] Robustness pass (0.7.6)
Tested by `TestRobustnessHardening`:
* `_acquire_data_dir_lock` (POSIX `fcntl.flock` / Win `msvcrt.locking`) at `<DATA_DIR>/splicecraft.lock`; PID-carrying; `$SPLICECRAFT_SKIP_LOCK=1` bypass.
* `threading.excepthook` → `_log.error`. `_chmod_user_only` 0o600 on logs/bundles. `_drain_in_flight_workers(timeout_s=2.0)` in `main()` finally.
* `_SETTINGS_SCHEMA` + `_validate_settings` — **strict bool-vs-int** (`True` does NOT coerce into `int`). Unknown keys pass through.
* Network retry: 1 try + 250 ms backoff on `_fetch_latest_pypi_version` + `fetch_genbank`. 4-tier `_copy_to_clipboard_with_fallback`: Textual → OSC 52 → `<DATA_DIR>/clipboard/<ts>-<label>.txt` → log.
* `_MODAL_STACK_SOFT_CAP = 12` on `push_screen` with `callback(None)` fallback. `_apply_record` notifies > `_LARGE_PLASMID_BP = 5_000_000`. `_snapshot_data_files` skips > `_SNAPSHOT_FILE_SIZE_CAP = 50 MB`.

## [INV-38] Diagnostic logging + UI snapshot + bundle
Three surfaces:
* Rotating log `<DATA_DIR>/logs/splicecraft.log` (`RotatingFileHandler`, 5 MB × 4 backups, 8-char `_SESSION_ID` prefix). **NEVER log sequence content** — `_repr_for_log` truncates/summarises.
* **`Alt+D`** App-priority → `action_capture_ui_snapshot` → `<DATA_DIR>/ui_snapshots/ui-snapshot-<ts>.md` (version, Python, platform, screen stack, focused widget, terminal size, record metadata excl. sequence, settings, 200-line log tail with `/home/<user>` → `~`). `_collect_ui_snapshot` defensive. Retention `_UI_SNAPSHOT_RETENTION = 20`. Old hover-debug moved to `alt+shift+d`.
* `splicecraft logs --bundle [--out PATH]` zips logs + last 5 snapshots + sanitized settings + system info + README. `_scrub_path` handles `/home/<user>`, `/Users/<user>`, `C:\Users\<user>`, `Path.home()`. **Sacred privacy invariant: sequence content MUST never leak.**

## [INV-39] `splicecraft update` snapshots user data before any install subprocess
All upgrade paths (pipx/uv-tool/uv-venv/pixi-global/pip-user/pip-venv) call `_create_pre_update_snapshot(__version__)` AFTER user confirm BEFORE `subprocess.run`. Covers `_USER_DATA_FILE_ATTRS` (10 files: library, collections, parts_bin, primers, features, feature_colors, grammars, entry_vectors, codon_tables, settings) + `_USER_DATA_DIR_ATTRS` (crash_recovery, dna_originals). **Atomic**: built in `<backup_dir>/.tmp-<rand>/`, fsynced, sealed by `os.replace`. Failure → staging removed + `OSError`/`shutil.Error` raised → exit 1. Location is **sibling** `<DATA_DIR>/../<DATA_DIR.name>-update-backups/` (override `$SPLICECRAFT_UPDATE_BACKUP_DIR`) so recursive-wipe can't kill recovery. Refuses when `_data_dir_inside_install_path()`. Restore: `splicecraft update --restore-pre-update [<id>|latest]` takes pre-restore snapshot first. Retention 5; rmtree restricted to `_PRE_UPDATE_NAME_RE`. **Sacred four restore checks**: `schema_version` ≤ `_PRE_UPDATE_SCHEMA_VERSION`, `attr` in whitelist, `name` rejects separators/`..`, SHA-256 re-verified before `os.replace`. Refusal paths + `--check` MUST NOT snapshot.

## [INV-40] Overhang pair is sacred source of truth for part classification
`_classify_part_from_plasmid` resolves part type/level/position **purely** from the `(oh5, oh3)` pair released by each grammar's primary/secondary Type IIS enzyme. Feature labels, plasmid name, source filename NEVER consulted. Returns `None` if pair doesn't match any position (with "couldn't classify — use New Part" notify). Grammar position-table changes affect "this pair now/no longer classifies"; never re-route via heuristics. Adding GB 2.0 expanded grammar was a position-table change, not a classifier change.

## [INV-41] Robustness sweeps #2–#6 (0.7.5→0.8.10)
Key invariants:
* **`_feat_bounds(feat, total) → (start, end, strand)`** is the canonical wrap-aware extractor. Use instead of raw `int(loc.start)/.end`.
* **`_smallest_enclosing_feature(bp)`** (bisect on `_feats_starts_sorted` + wrap pass) replaces O(N) `_feat_at` for bp-lookup.
* **Worker pattern:** heavy ops use `@work(exclusive=True, group=...)`. UI thread pre-captures inputs (`_collect_*_inputs`); worker emits `_on_*_failed` + `_apply_*_result`; never touches `widget.update`. Workers capture `_record_load_counter` at entry (extends invariant #28).
* **Modal Ctrl+Z:** `_blocks_undo: bool = True` opts modal out of app-level undo. **Attr must come AFTER docstring** or first-statement detection breaks. Applied to Constructor/Domesticator/Mutagenize/PrimerDuplicates/PrimerDesignScreen/PartsBinModal/FeatureLibraryScreen.
* **Primer dedup modal** defaults to KEEP (focus+Esc → Keep); `_skip_primer_dedupe_check` test flag.
* **Atomic writes:** `_atomic_write_bytes(path, data)` is byte-mode counterpart to `_atomic_write_text`; all `.bak`/`.bak.<ts>`/daily-snapshot copies route through it. `.dna`/bundle/token writers call `_fsync_parent_dir` after `os.replace`. Same-second collision protector bumps `.bak.<ts>.{N}.json`.
* **Symlink refusal:** `_safe_save_json` via `path.is_symlink()`; `_check_agent_read_dir` via `lstat`+`S_ISDIR`; `_check_agent_write_path` walks FULL ancestor chain via `resolve()` divergence + per-segment `is_symlink()`.
* **`.dna` sidecar case-collision fix (HIGH).** `_dna_sidecar_path` case-folds basename + appends 8-char SHA-1 prefix of raw `entry_id`. `_dna_sidecar_legacy_path` migrates on first read. `_DNA_SIDECAR_BASENAME_MAX = 200`.
* **Pre-update restore.** `_restore_pre_update_snapshot` REFUSES when manifest `sha256` missing/empty. Manifest reads capped at `_PRE_UPDATE_MANIFEST_MAX_BYTES = 4 MB`. `_restore_from_backup` staging uses `tempfile.mkstemp`.
* **Agent-API save uniformity.** `_LIVE_APP_REF` soft pointer set in `on_mount` + `_agent_dispatch`, cleared in `finally`. `_agent_save_or_500(save_fn, label)` wraps every agent-endpoint `_save_*`; `_bg_notify_save_failure(label, exc)` daemon-thread counterpart.
* **Concurrency.** Module-level `_cache_lock = threading.RLock()` wraps `_safe_save_json` + cache-reassignment in every JSON save helper. RLock because chains nest (`_save_library` ⇒ `_sync_active_collection_plasmids` ⇒ `_save_collections`). Reads don't take lock (`_typed_clone`-on-return + GIL). `_settings_flush_worker` try/finally so exception can't wedge `_settings_flush_running=True`. HMMscan/BLAST run/BLAST build split into distinct `@work(group=...)`.
* **Defence-in-depth size caps.** `_gb_text_to_record` > `_GB_TEXT_MAX_BYTES = 64 MB`. `_h_hmmscan` via `_safe_file_size_check` (2 GB). `_backup_info` + `_restore_from_backup` + `.bak` fallback via 1 GB cap. `_h_diff_plasmid` pre-caps both seqs at `_PAIRWISE_MAX_LEN`. Export endpoints whitelist via `_check_export_extension`. `_sanitize_path` refuses `~user`. `splicecraft_cli.py` caps body (50 MB) + token (1 KB).
* **Hygiene.** `_sweep_orphan_tmp_files` collects `.tmp`/`.migrating`/`.restoring` >1 h old. Lockfile uses `O_EXCL`. PID-alive recheck on stale lock.
* **`MultiAlignPickerModal`** ships `(entry_id, gb_text)` tuples so multi-Mb dismiss doesn't block UI.
* `tests/test_sweep5.py` (21 cases) regression coverage.

## [INV-42] Structured event logging (0.8.7+)
Design: **user pastes log → AI parses → patch shipped same loop**. `_log_event(event, *, _stacklevel=2, **fields)` emits `event <namespaced.name> {JSON}`. Strings >200 chars truncated with `…[+N]`; `SeqRecord`/`Seq`/`MutableSeq`/`bytes`/`bytearray` rendered as opaque tags (`<SeqRecord id=X len=Y>`) via class-name match (no BioPython import) — accidental `_log_event('e', rec=record)` cannot leak BioPython's `__repr__`-embedded bases. `seq.chunk_dump` only logs via structured event; `seq.hover_copy` logs `text_len` not `text`; UI snapshot routes settings through `_scrub_path`. `@_action_log(event_name)` decorates every `action_*` (exceptions swallowed — **logging must never break the underlying action**). `@_timed(path, threshold_ms=0)` wraps heavy ops; emits `op.timed`. Decorators pass `_stacklevel=3` so `funcName:lineno` lands on wrapped method. Names: `app.<area>.<verb>` (user actions), `op.<area>.<verb>` (ops), `<noun>.<verb>` (state — `save.ok`/`save.failed`, `undo.*`, `record.loaded`, `collection.switched`, `settings.changed`, `migration.step/failed`, `net.retry`, `lock.acquired/contended/stale/released`, `shutdown.drain.ok/timeout`). `SPLICECRAFT_DEBUG=1` bumps to DEBUG.

## [INV-43] Adversarial sweep #9 (2026-05-19, pre-v1.0.0)
11 HIGH + 10 MEDIUM fixes; `tests/test_sweep9.py` (50 cases). Key invariants:
* **Cache-bust on pre-update restore** (`_h_restore_pre_update_snapshot`) must enumerate **every** persisted-state cache including `_experiments_cache`, `_experiment_projects_cache`, `_gels_cache`. `RestoreFromBackupModal._TARGETS` mirrors.
* **Project-switch atomicity.** `ExperimentProjectsPickerModal._open` AND `_do_delete` active-promotion MUST: (a) update active-pointer via `_set_active_project_name`, (b) FORCE sync flush via `_settings_flush_sync()`, (c) write `experiments.json` for promoted project. Pre-fix left power-loss window where settings said OLD and experiments held NEW.
* **App-level Ctrl+Z routes through public `action_undo`** (NOT private `_action_undo`) so modal `_blocks_undo` actually fires.
* **`_save_experiments` mirror inside `_cache_lock`.** `_sync_active_project_experiments(entries)` MUST be inside `with _cache_lock:` block — RLock allows nested `_save_experiment_projects` re-acquire.
* **`_check_data_files` covers every persisted file** (sweep #9 added experiments/experiment_projects/gels/parts_bin_collections).
* **`_sweep_orphan_tmp_files` walks `_EXPERIMENTS_DIR` per-entry subdirs** one level deep with symlink refusal at each entry.
* **All new modals over editor/text input carry `_blocks_undo: bool = True`** (attr AFTER docstring). Sweep #9 added to `ExperimentProjectsPickerModal`, `ExperimentDeleteConfirmModal`, `ExperimentUnsavedChangesModal`, `ExperimentRenameModal`, `GelLibraryModal`, `ActionsPickerModal`, `ImageAttachModal`.
* **Tag regex atomic-group hardening.** `_PLASMID_REF_RE`/`_ACTIONS_REF_RE`/`_GEL_REF_RE` use `(?=([A-Za-z][\w.\-]{0,63}))\1(?![;=])` lookahead+backref to prevent backtracking (Python 3.10 lacks possessive / atomic group). Without it, `&amp;`/URL params would partial-match.
* **Windows-zip separator resilience.** `_zf_get_member_info(zf, name)` tries exact, falls back to normalised scan of `zf.infolist()` (Plasmidsaurus zips on Windows ship `category\\sample.gbk`).
* **SequencingScreen same-path cache** invalidates on content drift via `_zip_signature: tuple[mtime_ns, size]`.
* **Lockfile staleness via argv.** `_pid_is_splicecraft(pid)` reads `/proc/<pid>/cmdline` on POSIX to detect PID-recycle. Returns None where unimplementable. Stale-lock log carries `reason="no_live_process"`/`"pid_recycled_to_unrelated"`.
* **Clipboard image megapixel cap.** `_EXPERIMENT_CLIP_MAX_PIXELS = 50_000_000` checked BEFORE `img.save`. Save in try/finally so failure unlinks tmpfile.
* **`_summarize_perbase_tsv` float-aware** — `float()` not `int()`; extraction `int(float(cols[2]))`.
* **Legacy tag migration runs on every save** (`_normalise_experiment_entry` triggers `_migrate_legacy_tag_format` when `@plasmid:`/`@actions:` substrings present).
* **`_SETTINGS_SCHEMA` covers every persisted key** (sweep #9 added `active_parts_bin`, `active_project`, `experiments_custom_dict`).
* **Highlight-map per-line caps.** `_HIGHLIGHT_LINE_LEN_CAP = 50_000` / `_HIGHLIGHT_PER_LINE_TAG_CAP = 500` on `_ExperimentMarkdownTextArea._build_highlight_map`.
* **`_confirm_delete` (Experiments) resolves cursor row FIRST**, falls back to loaded entry only when no cursor. Arrow keys don't fire `_on_entry_selected`.
* **Plasmid cross-ref click prompts on unsaved canvas edits.** `_open_plasmid_ref` wraps switch+load with `UnsavedNavigateModal` when `app._unsaved`.

## [INV-44] Every save is a full plasmid (2026-05-19)
User contract: every assembly path — L0 Domesticator, Constructor TU / MOD / L3+, Traditional cloning, Gibson, MoClo — lands as a library entry whose `gb_text` is a single complete circular plasmid carrying **payload + overhangs + backbone** AND every parent-fragment feature as its own annotation. Coverage closed in `TestEverySaveIsAFullPlasmid` (11 cases):
* **L0 Domesticator → library mirror.** `PartsBinModal._new_part._on_result` runs the parts-bin save sync, then dispatches `_domesticator_library_mirror_worker` (`@work(thread=True, exclusive=True, group="dom_library_mirror")`) which calls `_part_to_cloned_seqrecord(part_dict)` and saves the full part-in-entry-vector plasmid as a library entry. **Off-thread** so a 100+ MB library doesn't freeze the UI between bin save and the success notify; mirror is best-effort (clone / save failure logs + notifies but does NOT roll back the bin row).
* **Parts Bin Save-to-Collection for L1+.** `_save_to_collection` filter accepts rows with `sequence` OR `gb_text` (was: `sequence` only — rejected every TU / MOD silently). `_part_to_cloned_seqrecord` short-circuits at **tier 0** for L1+: `gb_text` IS the cloned plasmid, parse and return it directly. Tier 0 validates `_seq_len(rec) > 0` and falls through on a zero-length record (corrupt data) so the explicit "no sequence" ValueError surfaces instead of a degenerate 0-bp library row. `_diagnose_part_cloning` returns None for every L1+ row (with OR without gb_text) since the IIS-vector diagnostics don't apply.
* **Colour preservation in clone simulation.** `_clone_part_marshal_vec_features` reads `ApEinfo_fwdcolor`/`ApEinfo_revcolor`/`color` from vector features (was hardcoded `"white"`). `_clone_part_build_seqrecord` writes `ApEinfo_fwdcolor`/`ApEinfo_revcolor` qualifiers when the carried colour is non-white. `_clone_part_build_part_feature` sources L0 part colour from `_GB_TYPE_COLORS[part_type]` (was hardcoded `"white"`) so Domesticator → library entries are visually typed.
* **Wrap-feature `CompoundLocation` in `_clone_part_build_seqrecord`.** Features with `end < start` render as `CompoundLocation([(s, n_seq), (0, e)])` — origin-spanning backbone features (e.g. an Ori straddling the relegated join) survive instead of being silently dropped by the old `if e <= s: continue` guard.
* **Structured save events for parity with Gibson.** `constructor.save.ok` / `constructor.save.failed` fired from `_save_to_library_worker` (fields: `gid`, `source_level`, `target_level`, `name`, `id`, `bp`, `n_feats`, `parts`, `backbone` / `stage`, `error`). `traditional.save.ok` / `traditional.save.failed` fired from `_save_product_to_library`. `domesticator.library_mirror.ok` / `domesticator.library_mirror.failed` fired from the worker. Naming follows the `<noun>.save.<verb>` convention `gibson.save.ok` established.

## [INV-45] Cross-platform contract (2026-05-19)
SpliceCraft runs on every modern OS / terminal pairing listed in `docs/PLATFORMS.md`. Audit sweep: `tests/test_cross_platform.py` (11 cases) covers every `sys.platform` branch via monkeypatch. Key invariants:
* **UTF-8 stdout encoding is blocking.** `_check_terminal_capabilities()` refuses to launch on non-UTF-8 / non-Unicode terminals (the braille map U+2800-U+28FF would render as gibberish) with an actionable error pointing at `PYTHONIOENCODING=utf-8` / `LANG=C.UTF-8`. Aliases `utf8`, `cp65001` (Windows Terminal in `chcp 65001`), `utf-16`, `utf-32` all pass. Empty encoding (piped) is silently skipped.
* **Optional Python deps degrade, never block.** `PIL` (clipboard image), `primer3` (Tm), `pyspellchecker` (F7) absence emits warnings via `_log.warning` + `_log_event("startup.terminal_capabilities", warning_count=N)` but launch proceeds. Per-feature gates handle the missing-import case at the call site.
* **OSC 52 clipboard target is OS-specific.** `_copy_to_clipboard_osc52` writes to `/dev/tty` on POSIX, `CONOUT$` on Windows. Either failure (no controlling terminal, no console output device, encoding error) returns False so the multi-tier wrapper escalates to the file-based tier-3 fallback.
* **Signal handlers gated by `hasattr` / `getattr`.** `SIGUSR1` (POSIX-only) faulthandler install: `if hasattr(_signal, "SIGUSR1")`. `SIGTERM` / `SIGHUP` translation: `getattr(_signal, name, None)`. Windows missing `SIGHUP` is fine.
* **Subprocess calls always list-form, never `shell=True`.** Cross-platform quoting parity for the update install path + every other shell-out.
* **`_RUNTIME_PLATFORM` cached at import** because `platform.platform()` shells out via subprocess on some OSes (conflicts with tests monkeypatching `subprocess.run`). Same cache pattern as `[INV-36]`.
* **Stale lock detection has POSIX-specific path.** `_pid_is_splicecraft(pid)` reads `/proc/<pid>/cmdline` on Linux to detect PID-recycle to unrelated processes. Returns None on macOS / Windows (no `/proc`); the conservative "assume live" stance preserves correctness.
* **`/dev/tty` / `CONOUT$` writes (OSC 52) wrapped in `try / except (OSError, UnicodeEncodeError)`** so a broken SSH session / WSL-without-TTY / Windows legacy `conhost.exe` never crashes the clipboard helper.

## [INV-47] Entry-vector auto-detection (2026-05-20)
`_detect_entry_vector_role(record, grammar)` identifies which entry-vector role a plasmid plays via Type IIS digest fingerprinting — NOT name/label heuristics. Strategy: digest with both grammar enzymes, identify stuffer via `_fragment_has_backbone_marker` exclusion (NEVER size — `feedback_never_assume_smaller_frag_is_payload`), match `(inner enzyme, outer overhang pair)` → role via Golden Braid binary-assembly architecture. Returns `(role, "strict")` for α/Ω acceptors whose inner digest matches the canonical TU boundary AND outer digest matches a recognised pair; `("", "weak")` for UPD-style L0 donors with non-canonical stuffers; `None` for L0 parts, TUs, MODs, or anything else. `_resolve_acceptor_role(grammar, inner_enz, outer_pair)` carries the gb_l0 mapping (`Alpha1=Esp3I-outer-(GGAG,GTCA)`, `Alpha2=Esp3I-outer-(GTCA,CGCT)`, `Omega1=BsaI-outer-(GGAG,GTCA)`, `Omega2=BsaI-outer-(GTCA,CGCT)`). `_auto_bind_entry_vectors_from_entries(entries)` runs detection across every grammar for a batch of library entries, binds new roles via `_set_entry_vector` (NEVER clobbers existing user bindings; strict > weak on collisions). Surfaced two ways:
* **Auto on collection import** — `_bulk_import_folder` callers append the auto-bind summary onto the import success notification. Zero-click happy path: user imports their EDEN/FFE folder, classification works immediately.
* **Settings → Entry Vectors…** — `EntryVectorsModal` (`_blocks_undo=True`) shows every role for the active grammar in one table with Pick/Clear/Auto-detect controls. Replaces the single-slot widget in `GrammarEditorModal` (which only exposed the empty-role binding). Grammar selector at top lets the user reconfigure for any grammar. Default focus on the table so arrow keys + Enter drive the picks. Esc → close.
* Tested by `tests/test_entry_vectors_auto.py` (17 cases) covering α1/α2/Ω1/Ω2 strict detection, UPD weak match, rejection of L0 parts / TUs / linear records / marker-less plasmids, auto-bind precedence (strict > weak), no-clobber guarantee, modal smoke test.

## [INV-48] Master Delete (2026-05-20)
File menu → "⚠ Master Delete (wipe all user data)…" wipes every persistent SpliceCraft file the user can produce — library, collections, parts bin, parts-bin collections, primers, features, feature colours, custom grammars, entry vectors, codon tables, settings, experiments, experiment projects, gels, agent token, data-version stamp, all `.bak` / `.bak.<ts>` siblings, daily snapshots, lost-entries spillover, UI snapshots, clipboard tmpfiles, crash-recovery autosaves, `.dna` sidecars, plugins dir, rotated log backups, `.migrated` legacy marker, AND the sibling pre-update-backups directory. True clean slate — no built-in restore path. Tested by `tests/test_master_delete.py` (33 cases). Sacred — two independent blockers against accidental fire:
* **No agent endpoint.** `_AGENT_HANDLERS` has zero handlers whose name contains `master`/`wipe`/`nuke`/`delete_all`/`clean_slate`/`purge`/`destroy`. Enforced by `test_no_agent_endpoint_exposes_wipe`.
* **Module-local sentinel.** `_perform_master_delete(app, *, sentinel)` raises RuntimeError BEFORE any disk op unless `sentinel is _MASTER_DELETE_SENTINEL` (a bare `object()` only the GUI flow holds a reference to). Enforced by `test_sentinel_mismatch_refused_before_any_disk_op`.
* **Typed-YES gate (`MasterDeleteModal`).** Stage 1 Input must read exactly `"YES"` — case-sensitive, no `.strip()` / `.casefold()`. Cancel default-focused; Esc → cancel. Delete button starts disabled and only enables on the exact-match `Input.Changed`. `_blocks_undo = True`.
* **3-second cool-down (`MasterDeleteConfirmModal`).** Stage 2 confirm button starts disabled with a countdown label (`set_interval(0.1, …)` ticks `_cooldown_remaining`) and only enables after `_MASTER_DELETE_CONFIRM_COOLDOWN_S` (3 s) elapses. Default focus on `No, keep my data`. `_blocks_undo = True`.
* **No keyboard binding.** `action_master_delete` is reachable from the File menu only. Enforced by `test_no_keyboard_binding_for_master_delete` (greps the source for `Binding(... master_delete ...)`).
* **Re-entrancy guard.** `PlasmidApp._master_delete_in_progress` flips True at action entry, False in every terminal callback (stage-1 cancel, stage-2 cancel, result-modal dismiss, wipe error). A menu double-click finds the flag set and notifies "already running"; no second flow stacks. Enforced by `test_action_master_delete_reentrancy_guard`.
* **Lockfile + active log preserved.** `_master_delete_residual_paths` resolves both `_DATA_DIR / "splicecraft.lock"` and `_DATA_DIR / "logs"` and excludes them from the residual sweep. The process holding the lock survives the wipe; the `RotatingFileHandler` writes survive to a still-open file. `_master_delete_log_files` separately removes rotated log backups (`splicecraft.log.1`, `.2`, …) while skipping `_LOG_PATH`.
* **Residual sweep (defense in depth).** After the named-target pass, `_master_delete_residual_paths` walks `_DATA_DIR.iterdir()` and unlink/rmtrees every file/dir that isn't the lockfile or logs/. Any future persistence file added without a corresponding `_USER_DATA_FILE_ATTRS` entry still gets wiped. `summary["residual_files"]` / `residual_dirs` counts surface in the result modal.
* **Cache reset under `_cache_lock`.** Every name in `_MASTER_DELETE_CACHE_ATTRS` (14 entries — every `_*_cache` global) reset to None inside the lock, after the file-level deletion but before the lock releases. A concurrent `_save_*` queued behind us thus finds the cache emptied and writes the empty-shape payload onto a never-existing path. `_settings_flush_pending` cleared FIRST so the daemon flush worker doesn't beat the wipe to disk.
* **Agent server stopped first.** `_stop_agent_api(app._agent_api_server)` before the wipe so an authenticated agent session can't re-populate data into the wiped tree. Token file unlinked by `_stop_agent_api` as a side effect.
* **Pre-wipe scope count on main thread.** `action_master_delete` enumerates file/dir targets BEFORE pushing the first modal so both modals + the post-wipe summary quote the same numbers. Scope count failures degrade to zeros (the wipe itself still proceeds).

## [INV-49] Load-time collision resolution (2026-05-20)
Every load path that lands user-named items in a persistent list — parts bin (single + bulk Load Part, Domesticator), plasmid library (`LibraryPanel.add_entry`), primer library (`PrimerDesignScreen._save_primers_btn`) — routes through `_resolve_load_collisions(app, entity, new_items, existing_items, *, name_key, content_fn, on_resolved, on_cancelled)`. Two collision categories, two modals:
* **Exact copy** (same name + same content) → `ExactCopyConfirmModal[bool]`. Default focus on Skip; "Keep as COPY" renames via `_ensure_unique_copy_name` (" COPY" / " COPY 2" / …) so the duplicate can coexist with its twin. Returns True to keep, False/None to skip.
* **Name collision** (same name, different content) → `NameCollisionModal[str]`. Three-way: keep original (default), overwrite, cancel. Cancel aborts the ENTIRE load (caller does NOTHING) — surfaced via `on_cancelled` callback. Keep drops the colliding new items; Overwrite removes existing entries whose name matches and lets the new ones land.
* **Counter-based parts-bin delete** — `PartsBinModal._delete_selected` switched from set-membership on `(name, sequence)` to `Counter`; selecting one of two duplicates removes one, not both. Pre-fix bug: legacy duplicates collapsed into one set element and got nuked together. Sacred regression guard.
* **Modals both carry `_blocks_undo: bool = True`** (attr AFTER docstring per sweep #6); ID-replace path in `LibraryPanel.add_entry` (same `record.id`) is NOT a collision — treated as "editing the same record" — and bypasses the modal flow. Tested by `tests/test_parts_bin_dedupe.py` (24 cases) + `tests/test_collision_flows.py` (5 cases).

## [INV-50] Adversarial sweep #10 (2026-05-20, pre-v1.0.0 follow-up to sweep #9)
11 HIGH fixes, `tests/test_sweep10.py` (25 cases). Targets bugs introduced by sweep #9 / `[INV-49]` + classes the earlier sweeps missed:
* **Batch-collision flow (`LibraryPanel.add_entries_batch`).** `_save_to_collection` pre-fix called `lib.add_entry(rec)` in a loop, each colliding rec stacking a `NameCollisionModal` while the post-loop toast claimed success before any modal resolved (and the modal stack soft cap would silently default-resolve at N=12). New batch API pre-classifies the whole batch, routes ONE collision flow, and fires `on_done(saved_names, replaced_names, cancelled)` once after the user resolves both modals. Toast emits from `on_done`, not from the loop. Diagnostics (stub-backbone warnings) also surface from `on_done` so they only fire for parts that actually landed.
* **Modal double-fire guard.** `ExactCopyConfirmModal` + `NameCollisionModal` carry a `_dismissed: bool` flag and a `_dismiss_once(payload)` helper. Pre-fix rapid double-clicks between the button-press handler and the async `pop_screen` completion could fire the same callback twice with potentially different payloads (Textual's `dismiss()` returns an AwaitComplete; the result-callback runs before the pop finishes). Every exit path (`_skip`/`_keep`/`_overwrite`/`_cancel_btn`/`action_cancel`) goes through `_dismiss_once`.
* **`_blocks_undo` retrofit.** 6 modals gained the flag: `EditSeqDialog`, `AddFeatureModal`, `FeatureEditModal`, `NewPlasmidModal`, `GrammarEditorModal`, `RestoreFromBackupModal`. Every one mutates canvas / record / persistent grammars / persistent library — app-level Ctrl+Z under them could race the in-progress edit (the bug class `[INV-41]` catches).
* **`RestoreFromBackupModal` cache-bust enumeration drift.** Pre-fix only reset 4 of 14 persisted-state caches. Sweep #9 added experiments / experiment projects / gels persisted files but the restore modal was never extended; restoring `experiments.json` from .bak left `_experiments_cache` stale and the next UI mutation silently overwrote the freshly-restored disk state. Now mirrors the full 13-cache enumeration from `_h_restore_pre_update_snapshot` (settings deliberately omitted — restoring settings would clobber the live app's pointer state mid-session).
* **Save-chain lock-release gap.** `_save_library` + `_save_parts_bin` pre-fix released `_cache_lock` BEFORE calling their mirror helper (`_sync_active_collection_plasmids` / `_sync_active_parts_bin_parts`), so Thread A's write + Thread B's write + Thread B's mirror + Thread A's stale-snapshot mirror could leave the mirror file out of sync with the live file. Mirror calls moved INSIDE the `with _cache_lock:` block. RLock allows re-entry from the mirror's own `_save_collections` / `_save_parts_bin_collections` chain. `_commit_library_entries` (post-modal commit path) similarly wraps its cache reseat in the lock.
* **Entry-vector RMW under lock.** `_set_entry_vector` + `_clear_entry_vectors_for_grammar` did load-mutate-save without `_cache_lock`. Two concurrent calls both read pre-state, both wrote, last writer lost the other's binding. The auto-bind flow's per-role loop amplified the race (TOCTOU on every iteration's `_get_entry_vector`). Now the full RMW runs under `_cache_lock`; RLock allows the nested `_save_entry_vectors` re-entry.
* **`_history_node_to_dict` stack-overflow fix.** Pre-fix recursed through `node.parents`, blowing Python's default recursion limit (1000) on hostile `.dna` imports carrying deeply-nested `<HistoryTree>` (32 MB cap can yield 3M+ nodes). Sibling helpers `walk`, `_history_node_count`, `HistoryScreen.populate` were already iterative for this reason; `_history_node_to_dict` was overlooked. Now iterative DFS with explicit `_HISTORY_NODE_MAX_DEPTH = 500` + `_HISTORY_NODE_MAX_NODES = 100_000` caps. Truncation surfaces via `_truncated` markers in the output dict.
* **`_bulk_import_folder` symlink refusal.** Pre-fix `path.is_file()` + `path.stat()` both FOLLOW symlinks — a hostile `.gb` symlinked to `/dev/zero` (devices report `st_size=0`) passed the size cap and `load_genbank` then read until OOM. Symlinks to `~/.ssh/id_rsa` or similar would also expose private data to any subsequent agent fetch of the imported entry. Now uses `path.lstat()` + `stat.S_ISREG(st.st_mode)` to refuse symlinks outright; the per-file check fires AGAIN inside the import loop (defence-in-depth against TOCTOU between the children-scan and the load).
* **`_get_rotated_state` cache key.** Pre-fix keyed on `(id(self._seq), id(self._feats), o)`. Python's small-string interning + allocator reuse means two distinct strings can share the same id after one is GC'd, surfacing a stale rotated cache after an in-place edit that happened to reuse the previous string's address. `_build_seq_inputs` (line 6564) had been fixed for this same reason; rotated-state was overlooked. Now uses `(hash(self._seq), id(self._feats), o)`.
* **`except (AttributeError, Exception)` collapse.** `_reset_app_state_after_master_delete` carried the misleading tuple form — `Exception` subsumes `AttributeError`, so the clause was just `except Exception`. The pitfall-#1 "no bare except" sweep would have flagged this. Narrowed to `except (TypeError, AttributeError)` for the `.clear()` site (read-only mapping defence) and `except AttributeError` for the simple attribute access.

## [INV-51] Adversarial sweep #11 (2026-05-20, deferred items from sweep #10)
6 areas, `tests/test_sweep11.py` (18 cases). Closes the architecture-larger items sweep #10 deferred:
* **Active-pointer flush-sync.** Every active-pointer flip that's followed by a sync file write now calls `_settings_flush_sync()` between them — mirrors the sweep #9 fix for project switch. Applied to 4 sites: `LibraryPanel._on_select` (collection switch), `LibraryPanel._request_collection_delete` (delete-promote), `PartsBinPickerModal._open` (parts-bin switch), `PartsBinPickerModal._delete` (parts-bin delete-promote). Pre-fix a power loss between the async daemon-flush and the sync file write left settings.json saying OLD while the dependent file held NEW; next mutation mirrored NEW into OLD, silently overwriting OLD's data.
* **Cross-group worker RMW under `_cache_lock`.** Every worker that load-modify-saves the plasmid library — Constructor `_persist_assembly`, Traditional `_save_product_to_library`, Gibson `_gibson_save_worker`, Domesticator `_domesticator_library_mirror_worker` — now wraps the read-modify-save in `_cache_lock`. Pre-fix `exclusive=True` only serialised within ONE `@work` group; two workers from DIFFERENT groups could both read the same pre-state, both insert a new entry, both save — the second writer silently dropped the first's entry. RLock allows re-entry from the inner `_save_library` lock. Constructor + Gibson + Traditional ALSO re-disambiguate the unique `name` / `id` inside the lock to defend against a racing worker that took the originally-unique slot.
* **Settings flush worker drain on Master Delete.** `_perform_master_delete` now BUSY-WAITS up to 2s under `_settings_flush_lock` for `_settings_flush_running == False` before proceeding with the file unlinks. Pre-fix the daemon worker only held the lock long enough to swap out the pending payload; it then released the lock and called `_safe_save_json` on its own — so a wipe that cleared `_settings_flush_pending` mid-write would find settings.json resurrected by the worker's atomic `os.replace` AFTER the wipe pass.
* **UI freeze on bulk-import auto-bind closed via `@work` + batched writes.** `_auto_bind_entry_vectors_from_entries` previously ran N × M (entries × grammars) GenBank parses AND N synchronous `_set_entry_vector` writes on the UI thread immediately after `_bulk_import_folder` returned, freezing the UI for several seconds on a 100-entry × 3-grammar import. Now (a) gb_text is parsed ONCE per entry (not N × M), (b) all auto-detected bindings accumulate into a single `_set_entry_vectors_batch` call (one `_save_entry_vectors` write instead of N), (c) the whole detection runs in `LibraryPanel._auto_bind_worker` (`@work(thread=True, exclusive=True, group="auto_bind_evs")`); the import-success toast fires immediately + a follow-up notify lands when the worker finishes.
* **`_find_library_entry_by_id` helper.** New helper that takes `_cache_lock`, walks `_library_cache` directly, and clones only the matched entry — O(N) walk with O(1) cloned bytes instead of `_load_library()`'s O(N) cloned bytes per single-entry lookup. Migrated 4 hot-path callsites: `LibraryPanel._refresh_active_row` (status badge refresh), `LibraryPanel._on_delete` (delete confirmation), `SequencingScreen._do_align` (target plasmid resolve), `PlasmidApp._action_discard_changes` (Esc-from-canvas reload). Pre-fix each click paid a full library deepcopy.
* **Agent API hardening.** `_h_create_collection` now does an ATOMIC name-taken re-check inside `_cache_lock` so a GUI user (or second agent client) creating a same-named collection during the multi-second `_bulk_import_folder` window surfaces 409 instead of silently clobbering. `_h_hmmscan` collapses its "not found" / "rejected" / "too large" responses to a single generic 400 + log line — pre-fix an unauthenticated local process could probe arbitrary filesystem paths via the error-message differential. `_build_commercialsaas_features_packet_from_record` strips ASCII control characters from `<V text=>` qualifier attributes before serialising; pre-fix hostile values containing `\x01` etc. produced `.dna` files that BioPython accepted on write but `_safe_xml_parse` choked on during round-trip, silently dropping the colour + label overrides.

## [INV-52] UI audit sweep #12 (2026-05-20, four-phase render/modal/reactive pass)
`tests/test_sweep12.py` (23 cases). Audit ran each phase independently then consolidated:
* **Phase 1 — `action_aspect_inc/dec` early-exit at limit.** Holding `,` (widen) at the 5.0 cap or `.` (heighten) at the 0.5 floor previously fired `self.notify(...)` AND assigned `self._aspect = round(...)` on every key tick — Toast widgets queued up at the screen edge and the reactive churn fired Textual watchers redundantly. Post-fix: `new = round(...)`, `if new == self._aspect: return`, then assign + notify. Same equality-check pattern as the `tooltip` cache at `PlasmidMap._tooltip_for` (line 11197) which already documented "Avoid spamming reactive churn with identical writes."
* **Phase 2 — `_blocks_undo` retrofit onto 12 input-bearing modals.** Modals carrying user-facing `Input` / `TextArea` widgets (or committing persistent mutations) must opt in to the `_undo_blocked_by_modal` guard so app-level Ctrl+Z falls through to the Input's built-in undo instead of mauling the canvas underneath. Added to: `MultiRecordFastaModal`, `CustomEnzymeListModal`, `PrimerEditModal`, `ColorPickerModal`, `PartEditModal`, `SpeciesPickerModal`, `CollectionsModal`, `CollectionNameModal`, `NewCollectionModal`, `NamePlasmidModal`, `RenamePlasmidModal`, `MinPrimerBindingModal`. Same after-docstring placement as sweep #10 (`[INV-41]`) — `TestBlocksUndoAttrPlacement` white-box checks that the attr lands AFTER both `"""` triples.
* **Phase 3 — reactive & widget hygiene (no source changes; documented baseline).** Audit found ONE `watch_*` method (`PlasmidMap.watch_origin_bp`), ZERO `self.query(...)` callsites (everything routes through `query_one`), debounced search via `_SearchInput._on_input_changed` + `Input.Submitted`-only repopulate. `TestReactiveHygieneInvariants` regression-locks these as soft caps (≤3 watch methods total; `query_one` outnumbers `query` by 20×) so a future feature can't drift the baseline silently.
* **What this sweep did NOT do.** The render-perf audit produced exactly ONE fixable finding (aspect notify spam); the Explore agent's HIGH-tagged "id() on rotated state" finding was a false positive — `_get_rotated_state` returns CACHED `_rotated_seq` / `_rotated_feats` whose identity IS stable across calls with the same `_rotated_cache_key`, so the outer `_view_cache_key` keying on `id(disp_seq)` works correctly. Documented here so a future reader doesn't re-investigate.

## [INV-53] Synthesis composer (2026-05-20)
Full-screen gene-synthesis workbench reachable via the **Synthesis** menubar entry (sits between Mutagenize and Parts — the natural workflow position upstream of Parts → Constructor). `SynthesisScreen` hosts the `SynthesisEditor` widget (linear horizontal-scroll DNA editor that renders via `_build_seq_text(seq, feats, line_width=len(seq)+1, ...)` — one chunk = one row group, no wrap, all per-base overlays from the SequencePanel renderer: feature stripes + restriction overlay + AA translation). Tested by `tests/test_synthesis.py` (34 cases). Key invariants:
* **Sacred — document-model save.** `_loaded_id` carries the library entry id under edit. `_commit_save` builds a SeqRecord with `topology="linear"`, `molecule_type="DNA"`, and pushes through `LibraryPanel.add_entry`; the id-match branch silently replaces the existing entry (the id-match branch's "editing-in-place" path, not the collision modal flow). Save As resets `_loaded_id = None`, prompts `NamePlasmidModal` for a fresh name, and re-targets. Rename updates `_loaded_name` only; the user still hits Save to persist.
* **Sacred — cap + IUPAC-only typing.** `_SYNTHESIS_MAX_BP = 50_000` (generous headroom above the ~30 kb commercial-synthesis ceiling so `_build_seq_text` stays interactive on a single chunk). `_SYNTHESIS_TYPEABLE_BASES = frozenset("ACGTRYWSMKBDHVN")` mirrors `AddFeatureModal`'s validation set so a fragment composed here round-trips through the feature library without surprises. `insert_at_cursor` truncates at the cap with a notify; non-IUPAC chars dropped with a notify.
* **Sacred — feature-shift on insert/delete.** Insert at cursor: bases right of cursor shift right; features whose `start >= cur` shift; features whose `end >= cur` shift, with the carve-out that `end == cur && start == cur` (zero-length features parked at the cursor) DON'T extend. A feature whose `end == cur && start < cur` extends (the typical "append to upstream feature" case). Delete: features entirely left of deletion unchanged; entirely right shift by `n_del`; overlapping clipped; zero-survival drops. `_delete_range` is the canonical mutation primitive — `delete_at_cursor` (Backspace / Delete / selection-delete) routes through it.
* **Two message classes.** `SynthesisEditor.Changed` fires on mutation (sets `_dirty = True`, refreshes status); `SynthesisEditor.CursorMoved` fires on cursor / selection change (refreshes status only, NO dirty flag). Screen listeners use `@on(SynthesisEditor.Changed)` / `@on(SynthesisEditor.CursorMoved)` — NEVER `@on(Message)` (catches every bubbling message, polluting handler scope).
* **Feature library side panel.** Right-pane DataTable populated from `_load_features()` with two action modes: *Insert* splices the entry's sequence at the cursor AND annotates it (reverse-strand entries get `_rc`'d so the annotation matches the template strand); *Annotate* overlays the entry's type / colour / qualifiers onto the current selection without changing DNA (notifies if no selection). `_featlib_rows` cache is `list[(row_key, entry_dict)]` where row_key is `f"{name}|{ftype}|{hash(seq):06x}"` so dup names with different sequences resolve uniquely.
* **Restriction-site picker.** `RestrictionInsertModal` is a searchable picker over `_NEB_ENZYMES`; dismiss payload is the enzyme name (caller resolves the recognition site via `_site_for_enzyme` and `insert_at_cursor`s it at the cursor — IUPAC degeneracy preserved, not expanded, so a vendor handles ambiguous bases per their own policy).
* **AddFeatureModal `total_len` kwarg.** New optional keyword (default None) lets callers running outside the main app (like SynthesisScreen, which doesn't host `#seq-panel`) supply the length used by the CDS divisibility-by-3 check. None falls back to the live `#seq-panel` query — backwards compatible with the on-canvas Ctrl+F path.
* **`_blocks_undo: bool = True`** on `SynthesisScreen`, `SynthesisLoadModal`, `RestrictionInsertModal`, `SynthesisUnsavedChangesModal` — every one mutates record state or hosts an Input. Attr placement after docstring per sweep #6 (`[INV-41]`).
* **Logging events:** `synthesis.*` (new / load / save / rename / feature.add / featlib.insert / featlib.annotate / insert_site). The `app.open.synthesis` action-log fires from `action_open_synthesis` via `@_action_log`.

## [INV-54] Synthesis composer DNA/Protein tabs (2026-05-20)
`SynthesisScreen` is a two-tab workbench (TabbedContent: DNA + Protein) sharing one toolbar. Actions dispatch on the active tab via `_active_tab_id()`. Tested by `tests/test_synthesis.py` (80 cases). Sacred invariants:
* **Per-tab state isolation.** Each tab carries its own `_loaded_id` / `_loaded_name` / `_dirty` / `_saved_snapshot` — DNA tab uses the bare names (back-compat with the v0.9.x single-tab handlers), protein tab carries explicit `_protein_*` variants. Save / Load / Rename / New all dispatch through `_active_tab_id()`. Tab switch fires `TabbedContent.TabActivated` → `_refresh_status()` re-renders the status line for the now-active tab. Close prompts unsaved-changes when EITHER tab is dirty (the modal's "save" decision saves the active tab; "abandon" drops both).
* **`ProteinEditor` mirrors `SynthesisEditor` semantics in AA coords.** `_aa_seq` (single-letter codes, `*` for stop), `_cursor_pos` on AA[N] (reverse-video paint). Direction-aware anchor for shift+arrow (same fix as DNA — first shift+left includes the AA under the cursor). Two render modes selected by `_codon_mode`: **codon-translated** (2 rows: AA centred over 3-bp codon; `_cols_per_aa() = 3`) and **AA-only** (1 row: contiguous letters; `_cols_per_aa() = 1`). Click→AA uses `aa_col // _cols_per_aa()`. Vertical + horizontal centring pads identical to SynthesisEditor.
* **N-/-C polarity markers.** `_FLANK_MARKER_N = "N-"`, `_FLANK_MARKER_C = "-C"`, width 2. Different from DNA's `5'-`/`-3'` (width 3) — the cursor / click math reads the protein editor's own constant, not the DNA editor's. `_FLANK_MARKER_STYLE = "bold #FF6B9D"` (pink) — visually distinct from DNA's `bold #4CC4FF` (cyan).
* **Codon-table dropdown is protein-tab-only.** Hosted in `#syn-protein-controls` row at the top of the protein TabPane. `Select.Changed` → `_apply_codon_table_choice(choice)` → rebuilds `_codon_table_raw` from `_codon_tables_load()` and calls `pe.set_codon_table(raw)` which rebuilds `_codon_cache: dict[aa, most_freq_codon]`. Default seeded from the first registry entry on `_init_codon_table()` (always K12 per `_codon_tables_load` invariant). Unknown AAs fall back to `NNN`; missing stop falls back to `TAA`.
* **Sacred — `Alt+T` toggles codon mode, NOT codon picking.** `action_toggle_codon_mode` only operates on the protein tab; DNA-tab activation just notifies. Button label flips between "Codon-translated" and "AA-only". Bound to **Alt+T** (NOT Ctrl+M) because Ctrl+M is the CR control character ^M at the terminal protocol level — the terminal swallows it as Enter before Textual sees it. Alt+T sends an ESC-prefixed sequence that's unambiguous across every supported terminal.
* **Built-in protein motif library.** `_PROTEIN_MOTIFS` constant (30 entries) covers affinity tags (His6/8/10, FLAG/3xFLAG, HA, Myc, V5, Strep-II, T7), localisation signals (NLS x2, NES), linkers (GSG, GGS, (GGGGS)x3/x4, EAAAK x3), protease sites (TEV, PreScission, Thrombin, Factor Xa, Furin), self-cleaving 2A peptides (P2A, T2A, E2A, F2A), and motifs (Kozak M, Stop, FLAG+Stop). Same shape as `_load_features()` entries so the side-panel renderer can reuse the row-build code. Insert splices the motif's AA sequence at cursor via `pe.insert_at_cursor`.
* **Atomic save lock (`_save_lock: threading.RLock`).** Both `_commit_save` (DNA) and `_commit_protein_save` wrap their SeqRecord construction + `LibraryPanel.add_entry` call in `with self._save_lock:`. Prevents two rapid Ctrl+S presses (or DNA+protein concurrent save races) from interleaving construction. RLock because the inner library save chain re-acquires `_cache_lock` — those are separate locks, but if either becomes re-entrant the RLock is forward-compatible.
* **Protein save round-trip via CDS `translation=` qualifier.** `_commit_protein_save` back-translates AA → DNA using `_codon_cache` (most-frequent codon per AA), builds linear DNA SeqRecord with CDS feature carrying `translation=[aa_seq]`, `label=[name]`, and `note=` describing the source codon table. `_load_protein_entry_by_id` → `_extract_protein_from_record` reads the CDS's translation qualifier FIRST (preserves AA exactly, no codon-table-drift), falls back to BioPython `Seq.translate()` only when no CDS w/ translation found. Active codon table name embedded in the saved CDS `note=` qualifier for audit trail.
* **`Ctrl+F` / `Ctrl+R` on protein tab notify only.** DNA-specific actions surface a friendly notify pointing at the motif library when invoked on the protein tab. Avoids partial-implementation traps.
* **Logging events:** `synthesis.protein.*` (new / load / save / rename / mode_toggle / codon_table_changed / motif_insert).

## [INV-55] Synthesis composer hardening sweep #14 (2026-05-20)
Post-commit audit of the freshly-shipped synthesis composer. Five fixes, tests in `TestUniqueEntryIdHelper`, `TestSaveAsDoublePromptFix`, `TestPartsBinAutoTriggerNewPart`, `TestSynthesisCloneFragmentButton`, `TestSynthesisCloneFragmentFlow`, `TestSynthesisRenderExceptNarrowed`. Key invariants:
* **Save As prompts ONCE.** Pre-fix `_save_as._on_named` set `_loaded_id=None` then called `_do_save`, which saw the cleared id and pushed a SECOND `NamePlasmidModal` (default = the just-typed name). Post-fix the inner callback builds a unique id via `_make_unique_entry_id` and calls `_commit_save` directly — only one prompt ever fires. Same fix in `_protein_save_as`. Regression-guarded by `test_save_as_commits_without_second_prompt` (counts NamePlasmidModal pushes through a stubbed `app.push_screen`).
* **`_make_unique_entry_id(name, *, exclude_id=None)` is the new entry-id factory for fresh-save / Save-As paths.** Sanitises `name` via `_make_entry_id` then suffix-disambiguates against current `_load_library()` ids (`base`, `base_2`, `base_3`, …, capped at 10_000, then random-hex fall-through). `exclude_id` lets re-save paths pass their current id and skip the bump so the document model (id-match silent replace) keeps working. Mirrors the suffix loop `_domesticator_library_mirror_worker` already used for the same problem. Sacred — pre-fix a synthesis save whose sanitised name collided with an existing library id silently clobbered the unrelated entry via `LibraryPanel.add_entry`'s id-match branch.
* **`PartsBinModal(auto_trigger_new_part=True)`** auto-fires `_new_part(None)` from `on_mount` via `call_after_refresh`. Flag self-clears after firing so a screen-resume can't re-trigger. Sole entry path is `SynthesisScreen.action_clone_fragment` — the Clone Fragment workflow's "open Parts Bin pre-armed for the just-saved fragment" step.
* **Clone Fragment button — Synthesis → Domesticator handoff.** `action_clone_fragment` auto-saves the synthesis fragment (or refuses if empty), re-reads the saved entry via `_find_library_entry_by_id`, loads it onto the main canvas via `app._apply_record(rec)`, dismisses the Synthesis screen, then `call_after_refresh` pushes `PartsBinModal(auto_trigger_new_part=True, clone_prefill_seq=full_seq)`. DNA tab only — protein tab notifies (domestication is a DNA operation). Closes the "compose → domesticate → L0 part in bin" loop in one click; pre-sweep was a 5-click chore.
* **Atomic prefill of Domesticator direct-input.** `DomesticatorModal(prefill_direct_seq=...)` writes the full fragment sequence into the `#dom-direct-seq` TextArea via `ta.text = seq` on mount — one atomic write, so the user can't catch the prefill mid-paste. `PartsBinModal._clone_prefill_seq` is a one-shot attribute consumed by the first `_new_part` call and cleared after firing, so a subsequent manual "New Part" click opens an empty form. Sequence sourced from the just-saved `SeqRecord.seq` (not the editor buffer) to guarantee what lands in the Domesticator matches what landed in the library, byte-for-byte. Logged as `domesticator.prefill { source: "clone_fragment" }`.
* **Single-entry lookups use `_find_library_entry_by_id`.** `_load_entry_by_id` (DNA load) and `_load_protein_entry_by_id` (protein load) swapped their full `for e in _load_library():` walk for the optimised helper added in `[INV-51]`. Removes the per-click O(N) deepcopy on libraries with many entries.
* **Render-path `except Exception` narrowed.** Three guards around `txt.split("\n")` calls in `SynthesisEditor` / `ProteinEditor` render paths (the bottom-strand splice, the horizontal-centring pad, the protein editor's pad) tightened to `(AttributeError, TypeError)` per pitfall #1. `TestSynthesisRenderExceptNarrowed` white-box asserts no bare `except Exception:` remains in either editor's class body so the regression can't drift back.

## [INV-56] Synthesis library entries land as pre-colored features + Edit buttons (sweep #15, 2026-05-20)
Both feature libraries (DNA + Protein) now produce colored features on insert AND let the user edit entries in place. Tests in `TestProteinEditorFeatures`, `TestProteinMotifInsertColored`, `TestProteinSaveLoadRoundTrip`, `TestProteinMotifsPersistence`, `TestSynthesisEditButtons`. Key invariants:
* **`ProteinEditor` grew `_aa_feats: list[dict]`** in AA coords (half-open). `load(aa_seq, feats=...)` seeds the list; `get_feats()` returns a deepcopy; `add_feature(feat)` appends with bounds clamping. Render paints feature cells as background color via per-cell `feat_bg` lookup; priority is `cursor (reverse) > selection (#003366) > feature color > default`, so the cursor + selection always stay visually loud over the colored band. Both render modes (codon-translated + AA-only) implement the same overlay.
* **Insert / delete shift AA features.** `insert_at_cursor` shifts features whose `start >= cur` right by the insert length; `end >= cur` extends UNLESS the feature is a zero-length anchor parked at the cursor. `_delete_range` clips overlapping features to the surviving span, shifts entirely-right features left by the delete length, drops zero-survival features. Mirrors `SynthesisEditor` feature shift semantics so behaviour matches across tabs.
* **Motif insert produces a colored feature.** `_motif_insert_selected` captures `pe._cursor_pos` BEFORE the splice (so it has the start of the inserted span), calls `pe.insert_at_cursor(aa_seq)`, then `pe.add_feature(...)` over `[start, start+len(aa_seq))`. Color sourced from the motif's own `color` field when set (user edits), falling back to `_PROTEIN_FEATURE_TYPE_COLORS[feature_type]` — a 6-entry palette for `Tag` / `Signal` / `Linker` / `Cleavage` / `2A peptide` / `Motif`.
* **Save/load round-trip via CDS sub-features.** `_commit_protein_save` writes each AA feature as a `misc_feature` SeqFeature inside the CDS (AA coords × 3 → DNA coords), stashing the AA feature type in `qualifiers["note"]` as `"splicecraft-aa-feature-type=<type>"` so the GenBank vocabulary mismatch (Tag/Signal/Linker aren't standard) round-trips. Color is also written via `ApEinfo_fwdcolor` / `ApEinfo_revcolor` so external viewers (ApE / CommercialSaaS) see the band. `_extract_aa_feats_from_record` reads the note marker back and recovers the original `type` string; falls back to `"Motif"` if missing.
* **`protein_motifs.json` persistence + Edit button.** New `_PROTEIN_MOTIFS_FILE = _DATA_DIR / "protein_motifs.json"` + cache. `_load_protein_motifs()` merges built-in `_PROTEIN_MOTIFS` with user overrides keyed on `name` (user edit replaces the built-in by name; novel user motifs append to the end). `_save_protein_motifs(entries)` writes ONLY user-modified entries; the merge happens on read. Synthesis motif Edit button → `AddFeatureModal(prefill=motif)` → on save, upserts to user file by name (copy-on-write — built-ins stay in code as a fallback). Synthesis feature-library Edit button (DNA tab) does the same against `features.json` — same `(name, feature_type)` key + dedup-on-rename logic `FeatureLibraryScreen.action_edit` uses, so edits made here surface in the standalone Features screen immediately. Conftest fixture seeds `_PROTEIN_MOTIFS_FILE` into the isolated path; `_check_data_files` validates the file at launch.

## [INV-57] Sweep #16 — dithered protein lane art + distinct motif colors + CI lint gate (2026-05-21)
Two visual polish items + one infra fix. Tests in `TestProteinMotifsDistinctColors`, `TestProteinDitherRender`, `TestProteinMotifInsertColorIsUnique`. Key invariants:
* **Each built-in motif carries its own `color`.** Pre-sweep all 10 Tag motifs shared `#3B82F6`, all 5 Linkers shared grey, etc. — the user couldn't tell two His tags apart in the side panel. Now `_PROTEIN_MOTIFS` carries a curated 30-entry palette spread within each family (Tags shift blue→cyan→indigo→violet; Cleavage reds→pinks; 2A peptides through the greens). Motif insert reads `motif.get("color")` first so the user's per-motif distinct color always wins over the family-fallback. User edits via the Edit button persist to `protein_motifs.json` and override these defaults.
* **Palette-key bug fix: `"2A peptide"` → `"2A"`.** `_PROTEIN_FEATURE_TYPE_COLORS` carried `"2A peptide"` as a key while `_PROTEIN_MOTIFS` used `"2A"` for the `feature_type` — silent fallback to `Motif` purple for every 2A insert. Fixed by matching the key to the data; `TestProteinMotifsDistinctColors.test_palette_key_matches_motif_feature_types` regression-guards.
* **ProteinEditor multi-lane dithered feature art (`_build_protein_lane_text`).** Mirrors the main seq panel's `_chunk_lane_groups` + `_paint_feature_bar` + `_paint_feature_label` style. AA-coord 2D greedy pack via `_pack_features_2d`-equivalent inline loop. Each feature occupies 2 rows: row 0 = `▒`-block bar adjacent to AA (with `▶`/`◀` strand-arrowhead glyph at the terminus), row 1 = centred label in `bold {color}`. Overlapping motifs stack into separate lanes; non-overlapping pack into the same lane. Multi-lane art is prepended ONLY when ≥1 feature is present — the empty case keeps the original 2-row (codon mode) / 1-row (AA-only) layout so users who haven't inserted a motif see the same view they always did. Replaces the per-cell background-color paint from sweep #15 (which was visually busy on overlapping motifs and didn't match the seq panel). `_row_count` tracks the actual lane height via `_lane_height()` so vertical centring stays correct. Legacy `_build_dither_row(cols_per_aa=...)` retained as a single-line back-compat alias that returns just the bar row (closest to AA) — used by simple consumers and white-box tests.
* **`release.py` runs `ruff check .` BEFORE pytest.** Pre-sweep, lint failures only surfaced in CI; a local release passed pytest + built wheels + pushed → CI badge went red on the lint job afterward. Now `release.py` runs the same `ruff check .` invocation `.github/workflows/test.yml`'s `lint` job uses — a release that lints clean here also lints clean in CI. Pyright deliberately NOT added (slow + flaky offline JS-pinned binary; stays CI-only via `PYRIGHT_PYTHON_FORCE_VERSION=latest`).
* **`test_delete_*` smoke tests use poll helper.** `TestDeleteFocusRouting._await_row_count(app, target, pilot)` polls the library DataTable for the expected `row_count` with up to 20 × 50 ms ticks, breaking early on convergence. Pre-sweep all four delete tests used a single fixed `await pilot.pause(0.05)` after `modal.dismiss(True)`, which on CI's Python 3.12 runner intermittently observed the row count one tick before the DataTable repopulate landed → red badge. Common case still completes in <100 ms.

## [INV-58] Sweep #17 — insert-at-cursor boundary-extend kwarg (2026-05-21)
`SynthesisEditor.insert_at_cursor` + `ProteinEditor.insert_at_cursor` gained `extend_adjacent_feats: bool = True` kwarg. Tests in `TestProteinMotifNonOverlapAtBoundary`, `TestDnaFeatLibNonOverlapAtBoundary`. Key invariants:
* **Strictly-inside extension is sacred.** A feature spanning `[fs, fe)` with cursor `fs < cur < fe` ALWAYS extends on insert regardless of the kwarg — typing inside a feature's body grows the bar, matching the seq-panel feel. Both kwarg branches preserve this: the only difference is the BOUNDARY case (`cur == fe`).
* **Default `extend_adjacent_feats=True` keeps the legacy typing-at-tail behaviour.** A feature whose `end == cursor` extends to swallow the inserted bases — the "extend the feature I'm appending to" semantics. Used by the user-typing path (`on_key` → `insert_at_cursor(ch)`) and by Ctrl+R restriction-site inserts (the user is adding bases into existing context).
* **`extend_adjacent_feats=False` lands separate library entries cleanly.** `_motif_insert_selected` (protein) and `_featlib_insert_selected` (DNA) pass False so that when the user inserts a second feature at the 3' boundary of an existing one (`cur == prev.end`), the prior feature's `end` stays put and the new feature lands as a sibling in `[cur, cur+n_ins)`. Pre-fix the upstream feature swelled to cover the insert AND the new feature stacked on top — visible overlap in the dithered lane art, two lanes where there should be one.
* **5' boundary (cursor at fs) shifts the existing feature right.** Both branches always shift `fs >= cur` regardless of the kwarg — the insert sits BEFORE the feature's first AA/bp by definition. Verified by `test_motif_insert_at_5p_boundary_shifts_existing`.
* **Delete-from-inside contracts the bar.** `_delete_range` clips overlap to the surviving span; entirely-right features shift left by the delete length; zero-survival features drop. Unchanged by sweep #17 — listed here only because the user's bug report bundled "extend / contract" as paired invariants.

## [INV-59] Sweep #18 — protein-load filter tightening + CI trimmed (2026-05-21)
Audit follow-up plus CI cycle-speed improvement. `SynthesisScreen._extract_aa_feats_from_record` had two sub-issues: the type filter was too loose (`if sf.type == "CDS": continue` — accepted gene/RBS/mat_peptide/etc. inside the CDS) and codon-misaligned coords would silently floor to the wrong AA boundaries. Fixed by:
* **`if sf.type != "misc_feature": continue`** as the type filter. Strict match to what `_commit_protein_save` writes — keeps imported GenBank records with unrelated CDS sub-features from polluting the Protein-tab editor.
* **Codon-alignment guard.** Drop any sub-feature whose `(fs - cds_s) % 3 != 0` or `(fe - cds_s) % 3 != 0`. Save-path always emits `AA × 3` aligned coords, so well-formed sub-features pass; mis-aligned coords (from a hand-edited GenBank import) get dropped instead of mapped to the wrong residue boundaries.
* Tests in `TestProteinLoadFilterStrict` (8 cases) cover misc_feature kept, gene/RBS/mat_peptide dropped, mis-aligned dropped, mixed-batch only-misc-aligned-passes, and the existing round-trip still works.
* **CI trimmed for fast iteration.** Pre-sweep `.github/workflows/test.yml` ran a 4-Python matrix (3.10/3.11/3.12/3.13) + a separate coverage job — ~28 min wall-clock per push. Trimmed to lint + single-Python-3.12 pytest with `-n auto` xdist parallelism on 2-core GitHub runners — ~6-8 min wall-clock. `pyproject.toml` still declares `requires-python = ">=3.10"`; multi-version verification stays the responsibility of `release.py` running locally pre-tag. CI's job is the public canary on master, not the matrix gate.
* **release.py bundles accumulated work.** Pre-sweep `_ensure_clean_tree` refused to proceed with any uncommitted changes — every feature paid a `git commit` toll. Sweep #18: the new `_summarize_pending_changes` prints `git status --short` (informational only); commit step uses `git add -A` to bundle all modified + new tracked files into the single `Release vX.Y.Z` commit. `.gitignore` still gates artefacts. Frees the iteration cycle — work accumulates in the tree until release.

## [INV-60] Sweep #19 — synthesis perf + pyright (2026-05-21)
Two fixes:
* **Protein lane art was O(rows × cells) on `Text.append`.** `ProteinEditor._build_protein_lane_text` allocated a `total_rows × total_cells` grid and called `Text.append` per cell — 354 ms at the 16.6k-AA cap with 30 motifs, 2.5 s in the all-overlap worst case. Replaced with **per-row segment lists**: each feature contributes O(1) `(col_s, col_e, text, style)` segments to its bar + label rows; emission walks each row's segments sorted-by-col_s and `Text.append`s ONE segment per feature plus inter-feature blank padding. Result: ≥10× speedup across realistic scenarios (5k AA × 30 motifs: 84 ms → 5 ms), 31× on the all-overlap worst case (2.5 s → 81 ms). `scripts/perf_probe_synthesis.py` captures the bench; rerun on any future lane-art change.
* **`self.app._apply_record` pyright fix.** `App[Unknown]` (Textual's base type) doesn't declare `_apply_record` — that's PlasmidApp-only. Added `# type: ignore[attr-defined]` at the synthesis Clone-Fragment call site. Same `# type: ignore` pattern other PlasmidApp-only attribute accesses use; pyright's `[tool.pyright]` analysis is limited to `splicecraft.py` + sidecars so this is the only place the warning surfaced in CI's lint job.

## [INV-61] Sweep #20 — agent-API gaps + event logger + invariant retrofit (2026-05-21)
Multi-part audit found drift accumulating in the wake of sweeps #15–#19. `tests/test_sweep20.py` (70 cases). Highlights:
* **21 new agent endpoints across four data systems + codon-table preference that had ZERO side-door access.** Pre-sweep, experiments / experiment_projects / gels / protein_motifs all shipped with full `_load_*` / `_save_*` infrastructure but external agents couldn't list / create / update / delete any of them. Added: `list-experiments` / `get-experiment` / `create-experiment` / `update-experiment` / `delete-experiment`; `list-experiment-projects` / `set-active-experiment-project` / `create-experiment-project` / `rename-experiment-project` / `delete-experiment-project`; `list-gels` / `get-gel` / `create-gel` / `update-gel` / `delete-gel`; `list-protein-motifs` / `set-protein-motif` / `delete-protein-motif`; `auto-detect-entry-vectors` / `clear-entry-vectors-for-grammar`; `set-active-codon-table` (with new `active_codon_table` settings key honored by `SynthesisScreen._init_codon_table`; existing `list-codon-tables` extended with `active_taxid` field). Total handlers 64 → 85. Project-switch endpoint mirrors the GUI's `[INV-43]` atomicity pattern (active-pointer write + `_settings_flush_sync()` + experiments.json write). Protein-motif endpoints round-trip through the user-file ↔ built-in merge in `_load_protein_motifs`; deleting a user override restores the built-in, deleting a built-in directly is rejected 404.
* **`_MASTER_DELETE_CACHE_ATTRS` (`[INV-48]`) missing two caches.** `_primer_usage_cache` (line 27694, added in primer-design refactor) and `_feature_library_index_cache` (line 28264, added in parts-bin perf work) had never been registered. Master Delete would wipe the files but leave the in-memory references alive; a fresh `_load_*` call would have rebuilt them, but a stale cache hit between the wipe and the next read could surface ghost data. Now covered. `TestMasterDeleteCacheAttrsCoverage.test_no_persisted_data_cache_missing` mirrors the conftest data-file list so a future feature can't silently add a `*_cache` global without registering it.
* **`_blocks_undo: bool = True` (`[INV-41]`) missing on five input-bearing modals.** Sweep #12 added the flag to 12 modals; five had been overlooked: `FetchModal` (URL + email Inputs), `OpenFileModal` (filename Input), `ExportGenBankModal` (filename Input), `NcbiTaxonPickerModal` (search-query Input), `FeatureSearchModal` (search-query Input). Pre-fix, a Ctrl+Z inside any of these typing-heavy modals fell through to the canvas record's undo stack instead of the Input's own undo, surprising the user mid-edit. Attr placement AFTER docstring per the invariant.
* **Collision modals (`ExactCopyConfirmModal`, `NameCollisionModal`) dismissed silently.** No structured event for the user's Skip / Keep / Overwrite / Cancel choice — `[INV-42]`'s "user pastes log → AI parses → patch" loop couldn't reconstruct the decision. Now every exit path emits `collision.exact_copy.dismiss` or `collision.name_collision.dismiss` with `{entity, n, choice}` fields where `choice ∈ {skip, keep, overwrite, cancel, esc}`.
* **`_save_protein_motifs` GUI failure path routed through `_notify_save_failure`.** Pre-sweep the GUI motif-edit save did its own `_log.exception` + inline notify, bypassing the `save.failed` structured event the rest of the codebase emits via `_notify_save_failure`. Now consistent with every other GUI save path.
* **`@_action_log` retrofit on 15 must-have actions.** Per `[INV-42]` ("`@_action_log(event_name)` decorates every `action_*`"), an Explore-audit pass found 25 user-visible action methods lacking the decorator. Sweep #20 added to 15 of those — the ones where no body event already captures the action (avoid double-logging): `FeatureLibraryScreen` (export_fasta / close / save / add / edit / rename / remove); `EntryVectorsModal.action_close`; `PartsBinModal.action_delete_selected_parts`; `ExperimentsScreen.action_open_projects`; `ExperimentRenameModal.action_submit`; `AlignmentScreen.action_close`; `HistoryScreen.action_close`; `FeatureSidebar.action_open_feature_at_cursor`; `SequencePanel.action_open_selected_feature`. The 10 SKIPPED actions already emit body events (`synthesis.*` / `experiments.new` / `experiments.save` / `undo.trigger` / `redo.trigger`) or are widget-level delegates whose App-level wrapper already carries the decorator (`PlasmidMap.action_toggle_map_view` is called by `PlasmidApp.action_toggle_map_view` which has `app.toggle.map_view`).
* **Bare-`except` narrowing pass.** 4 of the 286 `except Exception:` clauses outside `@work` bodies tightened to narrow types: 2 in `_build_system_info`/`_create_diagnostic_bundle` (file-system enumeration + JSON serialisation), 2 in the inner `_load_settings` and `_read_log_tail` calls inside `_collect_ui_snapshot` (internal helpers don't go through user-provided `__getattr__`). **The 6 getattr blocks in `_collect_ui_snapshot` that touch the `app` argument STAY broad** with `# noqa: BLE001 — broken-app survival` comments — `tests/test_smoke.py::test_collect_ui_snapshot_handles_broken_app` constructs a `__getattr__`-raises-RuntimeError stub and asserts the snapshot survives. **Sacred lesson**: diagnostic-time helpers that inspect potentially-broken state need broad catches by design. The first narrowing attempt failed the test; reverted with comments. The remaining ~278 `except Exception:` sites split into three legitimate categories: (a) inside `@work(thread=True)` decorated worker bodies — pitfall #1's explicit carve-out; (b) logging helpers (`_repr_for_log`, `_action_log` wrapper, `_notify_save_failure`) marked `# noqa: BLE001 — logging must never raise`; (c) bridging third-party libraries with loose error models (`primer3.calc_tm`, BioPython aligner) where the broad catch immediately re-raises as a clean `ValueError`. These are all correct and were not touched.
* **Documentation freshness.** README + `docs/agent-api.md` + `docs/features.md` updated from "60+ endpoints" to "80+ endpoints" with the new categories enumerated (experiments / projects / gels / protein motifs / entry vectors).

## [INV-62] Sweep #21 — modal-staleness audit (2026-05-21)
Audit pass for the staleness window: a modal opens at T=0 capturing canvas state (feature idx, selection range, record reference); between T=0 and dismiss at T=2, an agent-API endpoint (`_h_load_entry` / `_h_fetch` / `_h_load_file`) can mutate `_current_record`; the modal's dismiss callback then applies user edits to the WRONG molecule at the WRONG idx. `tests/test_sweep21.py` (7 cases). Key fixes:
* **`_open_feature_editor` callback wrapped via `_guard_callback`.** Pre-sweep, FeatureEditModal / PrimerEditModal captured `idx` at T=0 and the dismiss callback applied the edit unconditionally. An agent-driven `_h_load_entry` mid-modal swapped `_current_record` and the user's save landed on the new record's feature[idx] — silent data corruption on the wrong molecule. Now uses the same `_guard_callback` wrapper that `action_edit_seq` already used.
* **`action_add_feature` callback wrapped via `_guard_callback`.** Same risk on the "Annotate" path — AddFeatureModal captured selection `(start, end)` at T=0; applying those coords to a NEW record's bases would silently corrupt the annotation. Wrap-aware location math doesn't help when the underlying bases are entirely different DNA.
* **`_guard_callback` is the canonical helper.** Lives at `PlasmidApp._guard_callback`; captures `_record_load_counter` at wrap time, refuses to fire the wrapped callback if the counter shifted (every `_apply_record` bumps it). Pre-sweep, only `action_edit_seq` used it; sweep #21 brings `_open_feature_editor` + `action_add_feature` to parity. `action_transfer_annotations` uses an inline counter check (lines 73066-73082) because its state spans two modal callbacks; same semantic.
* **What was checked but NOT fixed (verified-safe):** PartEditModal already does identity-based lookup `(name, sequence)` on save and refuses with a friendly notify if the part identity has been removed/modified by another writer — better than counter-based since it handles in-place mutations too. GrammarEditorModal serializes through `_cache_lock` via `_save_custom_grammars`. Both safe.

## [INV-63] Sweep #22 — load-bearing function audit + file-migration futureproofing (2026-05-21)
Four parallel deep audits (I/O atomicity, sequence primitives, concurrency/state, logging plumbing) covering 17 functions across the codebase's most-used helpers. Most verified clean; two real defects fixed, one minor polish. Plus a futureproofing pass to make sure files imported from previous SpliceCraft versions migrate correctly. `tests/test_sweep22.py` (20 cases).
* **`_safe_save_json` ancestor-symlink walk (HIGH).** Pre-sweep, only `path.is_symlink()` was checked. Sweep #10 (`[INV-50]`) added the full ancestor-chain walk to `_check_agent_write_path` but didn't propagate to `_safe_save_json` — a symlink at ANY deeper ancestor (e.g. `~/.local` → `/etc`) could redirect every save under the data dir. Now mirrors the agent-side defense: walks every ancestor up to the filesystem root, refuses on any symlink hit, surfaces a friendly OSError naming the symlinked ancestor. Errors mid-walk also refuse — can't tell if it's safe.
* **`_iupac_pattern` case-fold cache key (LOW polish).** Cache keys preserved input case (`"GAATTC"` vs `"gaattc"` occupied separate slots) but the internal regex was always built from `site.upper()`. Two slots, identical regex. Now normalizes the key to uppercase on lookup AND store. Saves cache slots on mixed-case calls; one-line fix in `splicecraft_biology.py`.
* **File migration framework — test coverage (futureproofing).** `_CURRENT_SCHEMA_VERSION` + `_ENTRY_MIGRATIONS` + `_migrate_entries` + `_extract_entries` (`[INV-36]`) have been in place since ~0.5.x but NEVER exercised in production because no migrations are registered. When the schema finally bumps from v1 to v2, the framework needs to work correctly. `TestMigrationFramework` (11 cases) covers every path: bare-list legacy → v0 migrated; envelope v1 → no-op; envelope future-version → loads with warning; multi-step chain (v0→v1→v2); missing intermediate migrator → no-op pass-through; failed migrator → entry preserved + warning; non-dict garbage → skipped; non-list input → empty list + warning; no-op when at current → fresh list returned. `TestLegacyFileRoundTrip` verifies a bare-list file loaded + re-saved comes back as the envelope format. `TestMigrationCoverageEveryLoadPath` white-box asserts every `_load_*` helper routes through `_safe_load_json` so the framework applies uniformly — a future load helper that bypasses it would silently skip migrations.
* **`.dna` packet XML size-cap (declined-by-design).** The Explore agent flagged that `_iter_commercialsaas_packets` payloads pass to `_safe_xml_parse` without per-packet caps. Verified: the upstream `load_genbank` size check at `_SAFE_LOAD_JSON_MAX_BYTES = 1 GB` bounds the entire `.dna` file. Billion-laughs requires a DTD which `_safe_xml_parse` rejects. The remaining risk (legit-but-large XML packet → memory pressure) is "user opens their own 1 GB file" — not a remote-attacker DoS. Declined as defense-in-depth not worth the complexity.
* **Audit category 1 — I/O atomicity verified clean otherwise:** backup ordering correct (write `.bak` + `.bak.<ts>` BEFORE new content lands), atomic write sequence intact (mkstemp → fsync → close → os.replace → fsync_parent_dir), no quadratic encode patterns in JSON serialise, `_safe_file_size_check` correctly rejects symlinks/FIFOs/devices via lstat, DOCTYPE/ENTITY defense in `_safe_xml_parse` is case-insensitive streaming prologue scan (handles whitespace + comment prefixes).
* **Audit category 2 — sequence primitives verified clean:** all sacred invariants #1–#9 hold. `_rc` IUPAC complement covers all 15 codes via stdlib `str.translate`; `_scan_restriction_sites` palindromic-forward-only + wrap-boundary + recut-modulo math all correct; `_feat_bounds` handles canonical CompoundLocation wrap re-encoding (start=0, end=total signal); `_smallest_enclosing_feature` bisect on `_feats_starts_sorted` correct + wrap-feature fallback bounded.
* **Audit category 3 — concurrency verified clean:** `_apply_record` orders `_current_record =` BEFORE `_record_load_counter += 1` so a worker reading the counter sees the new record (not torn state); every full-record-swap site bumps the counter; `_cache_lock` (RLock) wraps save+mirror in a single critical section across all 13 `_save_*` helpers per sweep #10 `[INV-50]`; `_LIVE_APP_REF` lifecycle clean (set in `on_mount` + `_agent_dispatch`, cleared in `finally`).
* **Audit category 4 — logging plumbing verified clean:** `_repr_for_log` catches `SeqRecord` / `Seq` / `MutableSeq` / `bytes` / `bytearray` via class-name match (no BioPython import); `_log_event` 200-char string truncation + 300-char nested-dict bail; `_action_log` and `_timed` both use `@functools.wraps` + `_stacklevel=3` correctly; `_log.propagate = False` blocks BioPython's child-logger output from inheriting our level. Sacred `[INV-38]` (sequence content never leaked) verified — no `_log_event` callsite passes raw sequence fields.

## [INV-64] Sweep #23 — file-format expansion + agent CRUD parity + deferred-fix landing (2026-05-21)
Multi-week sweep covering format support, agent-API CRUD gaps, modal hardening, and several perf optimizations. New tests in `tests/test_new_formats.py` (17 cases) + `tests/test_new_agent_endpoints.py` (13 cases) + 1 new case in `tests/test_sweep10.py`. Total handler count 89 → 107.

* **Five new file formats accepted by the loader chain.** EMBL (`.embl`), AB1 Sanger trace (`.ab1`/`.abi`), FASTQ multi-read (`.fastq`/`.fq`, capped at `_FASTQ_MAX_READS = 1000` reads per file), GFF3 standalone (`.gff`/`.gff3` with inline `##FASTA`), and GFF3 overlay (sequence-less → applied to currently-loaded record). Dispatch is uniform across the three import paths: UI `OpenFileModal._do_load`, `_bulk_import_folder`, and agent `_h_load_file` all consult `_is_fasta_path` / `_is_ab1_path` / `_is_fastq_path` / `.gff[3]` suffix in the same order before falling through to `load_genbank`. `_detect_plasmid_format` now content-sniffs unknown extensions (LOCUS / ID / CommercialSaaS cookie peek) before defaulting to GenBank. **`_check_export_extension` extended to the three text-format UI export modals** — pre-fix the agent path enforced extension allow-lists but the UI path silently wrote GenBank text to a `.dna` filename.

* **`GFF3 standalone-vs-overlay split` is the canonical pattern.** A GFF3 file with an inline `##FASTA` section is a complete record → `_gff3_path_to_record` builds a fresh `SeqRecord` and the UI `load-file` flow replaces the canvas. A sequence-less GFF3 is a feature overlay → `_gff3_apply_to_loaded_record` mutates the loaded record's `.features` in place after length-mismatch validation. The two paths refuse each other's input (`_gff3_apply_to_loaded_record` rejects `##FASTA` payloads to prevent silent sequence loss; `_gff3_path_to_record` rejects sequence-less files to prevent feature drift onto the wrong canvas). **Wrap features round-trip via same-`ID=` row rejoining** (inverse of pitfall #26's `_record_to_gff3` split-row emit). Agent endpoint `apply-gff3` mirrors `transfer-annotations`'s record-load-counter stale-canvas guard.

* **Bulk export collection feature.** New `_bulk_export_collection(collection_name, fmt, target_dir, progress_cb)` writes every plasmid in a collection to a folder as individual files. GenBank format uses a fast path that atomic-writes the cached `gb_text` directly without a Biopython round-trip (skips ~2–10 s on 500-plasmid exports). Other formats parse-and-emit via the existing per-format writers. **Filename collision policy: case-folded uniqueness set + `_N` suffix bump.** Pre-fix the exact-case check missed `pUC19` / `puc19` collisions on APFS/NTFS → silent overwrite. The case-fold defense mirrors `_dna_sidecar_path`. `_safe_export_filename` scrubs path-traversal characters + Windows reserved device names via the hoisted `_WIN_RESERVED_FILENAMES` + `_is_windows_reserved_stem` helpers (three inline duplicates collapsed to one). UI: `BulkExportCollectionModal` with collection / format / folder pickers + worker-driven progress; menu entry `File → Export collection (bulk)…`. Agent endpoint `bulk-export-collection`.

* **18 new agent endpoints across four data systems** brought to full CRUD parity:
  * **Cloning grammars:** `list-grammars`, `get-grammar`, `create-grammar`, `update-grammar`, `delete-grammar`. Built-in ids (`gb_l0`, `moclo_plant`) refused for create/update/delete — mirrors the UI's `editable=False` gate. Delete also clears the bound entry vector for the grammar.
  * **Primers:** `list-primers`, `get-primer`, `create-primer`, `delete-primer` (update was already present). New `list-primer-collections` + `set-active-primer-collection` for collection-aware workflows. Duplicate sequences (case-insensitive) refused with 409 + the existing entry's name returned for disambiguation.
  * **Parts bin:** `create-part`, `update-part` (list/get/delete were already present). Lookup is `(name, grammar)`; update preserves original date so an edit doesn't masquerade as a fresh add.
  * **Feature library:** `list-feature-library`, `get-feature-library`, `create-feature-library`, `update-feature-library`, `delete-feature-library`. Named distinctly from the record-level `add/delete/update/get-feature` (which operate on the loaded plasmid's features) to prevent agent-side confusion — the record-level handlers were defined first and the name collision was caught by pyright at landing time.

* **`_AGENT_BACKUP_LABELS` parity invariant.** Pre-sweep `_AGENT_BACKUP_LABELS` covered 11 of 16 `_USER_DATA_FILE_ATTRS` and `RestoreFromBackupModal._TARGETS` covered 9 of 16. Users could list/restore Experiments / Gels / Protein motifs / Primer collections via the GUI but not via the agent API; Feature library / Feature colours / Cloning grammars / Entry vectors / Codon tables / Parts-bin collections were unreachable from EITHER path even though their `.bak` files existed on disk. Both maps now mirror `_USER_DATA_FILE_ATTRS` minus `_SETTINGS_FILE` (settings restore is intentionally GUI-only to avoid clobbering live in-session pointer state). `tests/test_sweep10.py::test_agent_backup_labels_parity_with_user_data_files` is the regression guard: asserts every user-data attr except settings is reachable by SOME label AND every label has a cache-bust map entry. **Sacred contract:** any new persisted file added to `_USER_DATA_FILE_ATTRS` must also land in `_AGENT_BACKUP_LABELS` + `RestoreFromBackupModal._TARGETS` + the cache-bust map at `_h_restore_backup:cache_attr`, OR be explicitly excluded with a code comment naming the reason.

* **`_iter_collections_readonly` no-mutation contract.** Callers MUST NOT mutate the returned cache list or its nested dicts. Returns the live cache directly (no `_typed_clone`) for hot read paths — saves ~30–50 ms per call on 5k-plasmid libraries (`_search_collections_library` per-keystroke search, `_blast_collections_fingerprint` on every BLAST submit). For ANY mutation path, use `_load_collections()` (which clones). Mirroring pitfall #17's existing read-side clone rule, but inverted: this helper is the explicit "I won't mutate" handshake. Confirmed safe at all four current callsites by inspection; widening to other typed loaders deferred until a specific caller demonstrates a perf need.

* **BLAST collections fingerprint cache.** `_blast_collections_fingerprint` is memoised by `(id(_collections_cache), frozenset(names))`. Saves a full library walk per `_blast_get_db` call. Auto-invalidates when `_save_collections` reseats the cache (identity flips inside the `_cache_lock` block). Combined with the earlier sweep's content-fingerprint BLAST cache (which dropped the `_blast_clear_cache()` unconditional-clear from `_save_collections`), a 200-plasmid collection rename no longer flushes BLAST dbs — the fingerprint is the same, cache stays warm.

* **`_gb_text_to_record` LRU cache.** 64-entry cache keyed on `hash(text)`. Used by 53 callsites; library-iteration paths (`_classify_part_from_plasmid`, `_assembly_fragment_recover_gb_text`, `_bulk_export_collection` non-GenBank path) repeatedly parse the same `gb_text` during one user action. Cache returns `deepcopy` on hit so callers can mutate freely per pitfall #17.

* **`_h_apply_gff3` apply-record fix (P0 bug).** Pre-fix the handler called nonexistent `app._refresh_canvas_after_edit()` swallowed by an `(AttributeError, Exception)` antipattern — feature counts were returned but the map never repainted. Now follows the canonical `transfer-annotations` pattern: deepcopy `rec` on the worker thread, mutate the copy, hop to UI thread for `_push_undo` + `_apply_record(clear_undo=False)` + `_mark_dirty()`. Record-load-counter captured at entry and re-checked in `_apply` so a concurrent canvas swap surfaces 409 instead of silently corrupting state.

* **`(AttributeError, Exception)` antipattern is bare-except in disguise.** `Exception` already subsumes `AttributeError`, so the tuple is equivalent to `except Exception` — violates pitfall #1. Fixed in `_h_apply_gff3` (two sites) and Sanger handlers (`_sanger_load`, `_sanger_add_to_library`). **Sacred lesson:** when narrowing exception classes, write the tuple AS the union you actually want — pyright won't catch the redundancy and a copy-paste from a worker-thread `except Exception as exc` carve-out propagates as silently-broad.

* **Format-helper size caps.** `_ab1_path_to_record` + `_fastq_path_to_records` + `_gff3_path_to_record` route through `_safe_file_size_check` at entry. Pre-fix `SeqIO.parse(path, "abi")` streamed from disk with no upstream guard — a multi-GB hostile file OOM'd the worker before any packet validation. FASTQ has a second cap (`_FASTQ_MAX_READS = 1000`) — at the 50 MB file cap a typical Illumina lane delivers ≫1000 short reads; the plasmid library wasn't designed to hold raw sequencing reads.

* **`_check_data_files` is validate-only — a seed-from-validate optimisation FAILED.** First attempt at a 2026-05-21 perf win: pre-sweep `_check_data_files` validates every persisted JSON via `_safe_load_json` to surface corruption early; the subsequent first-use `_load_*` call re-reads the same file. The naive optimisation seeded each typed cache with the validated entries to skip the second read. **It broke three loaders whose post-validation logic the generic seed bypassed:** `_load_protein_motifs` merges built-in motifs with user overrides (30 built-ins disappeared), `_load_experiments` runs `_migrate_legacy_tag_format` (pre-2026-05-18 tag tokens leaked into the editor), `_load_entry_vectors` applies a tighter filter requiring `grammar_id` to be a string (drift entries leaked). **Reverted to validate-only.** Future-me: do not re-attempt without isolating each loader's post-load logic into a shared helper that runs on both seed and lazy-load paths. Documented here so the audit recommendation doesn't re-surface as actionable.

* **`_dismiss_once` retrofit scoped to mutating modals.** Sweep #21 found ~75 `ModalScreen` subclasses lacking the `[INV-50]` double-fire guard. Indiscriminately retrofitting all 75 would have added maintenance noise to view-only pickers that have no destructive payload. Scope landed: every modal that **writes to disk on dismiss OR dismisses with a payload that triggers a downstream save** — 13 destructive/quit/export/save-prompt modals (UnsavedQuitModal, QuitConfirmModal, UnsavedNavigateModal, EditGrammarConfirmModal, `_ConfirmDeleteGrammarModal`, LargeFileConfirmModal, ExperimentDeleteConfirmModal, ExperimentUnsavedChangesModal, SynthesisUnsavedChangesModal, ExportGenBankModal, ExportCommercialSaaSModal, GffExportModal, FastaExportModal). View-only pickers (PlasmidPickerModal, NamePickerModal, etc.) left unguarded — they round-trip a selection without mutating state, so a double-dismiss races to the same payload twice with no consequence beyond a redundant `dismiss()` exception that Textual already swallows. The pattern applied is uniform: `self._dismissed: bool = False` in `__init__`, `_dismiss_once(payload)` helper, every exit path (button + Esc + worker-callback) routes through it. **Sacred contract:** any NEW modal that writes on dismiss must add the guard at landing time, not as a follow-up sweep.

* **`_atomic_write_text` + `_atomic_write_bytes` symlink refusal.** `os.replace(tmp, str(path))` on a symlink replaces the *symlink itself* with a regular file — breaks the link chain without writing through to the target. Pre-fix a user opening `~/work/current.gb` (a symlink into `~/projects/foo/v3.gb`) and saving would silently break the symlink. Now both atomic-write helpers `lstat` the target and raise OSError if `S_ISLNK`. NOT a write-into-target vuln (replace doesn't follow symlinks on rename), but real silent breakage. Users who legitimately want to overwrite through a symlink must resolve the path themselves first.

* **`_backup_info` damaged-row schema.** Returns `{n_entries, mtime_str, error}` for every backup — `n_entries: None` and a non-empty `error` string for parse-failed / oversized / non-list-or-envelope-shaped files. Pre-fix, damaged backups returned `None` and were silently dropped from the Restore UI's row list. A user trying to recover from a corrupted-but-existing snapshot would see "no recoverable backups" and have no way to know the file was there. Now the row surfaces with `[red]<kind> [damaged][/red]` in the kind column and `?` in the entries column; the restore button refuses with a clear "Backup is damaged: {error}" message.

* **Sanger AB1 tab in `SequencingScreen`.** New "Sanger (.ab1)" top-level tab alongside Plasmidsaurus. `_AbiAwareDirectoryTree` paints `.ab1`/`.abi` files sky-blue. Pick a trace → preview (name, length, mean Phred, first 200 bases) → "Load into canvas" or "Add to library" buttons (both gated until a valid trace is parsed). Re-clicking the same path short-circuits — `_ab1_path_to_record` is ~50–200 ms per trace.

* **`docs/features.md` rewrite.** Pre-sweep the file stopped at the original feature surface; missing Synthesis (DNA + Protein composers), Experiments lab notebook, Gels, Protein motifs, Master Delete, the full File-formats matrix. Now mirrors the README's feature taxonomy + adds a dedicated "Recovery + data safety" section covering the four-layer JSON safety net, Restore-from-backup damaged-row tagging, Master Delete, and pre-update snapshots. README endpoint count bumped to "90+"; `docs/agent-api.md` body updated.

* **Performance hot-path fixes.** `_label_at` row-indexed lookup (`splicecraft.py:12509`) — per-hover linear scan replaced with `{ly: [(x0, x1, idx), ...]}` cache invalidated by `id(self._label_bboxes)`. `_update_hover_status` widget-ref cache (`splicecraft.py:17574`) — DOM walk at 60 fps hover collapsed to a one-time `query_one` cached on the panel instance.

* **Settings schema bumps.** `crash_recovery_seen` added to `_SETTINGS_SCHEMA` with `((list,), [])` so a corrupted settings.json with `crash_recovery_seen: 42` can't crash the post-mount recovery scan. Forward-compat unknown-key passthrough already handled it but the schema is the documented contract for "what's persisted."

* **`BulkExportCollectionModal` added to `tests/test_modal_boundaries.py::_MODAL_CASES`.** The 160×48 fit guarantee now covers the new collection-export modal.

---

## [INV-65] Sweep #24 — custom enzymes + enzyme collections hardening (2026-05-22)
Audit-driven cleanup for the in-flight "custom enzymes + enzyme collections" feature (~2000 LOC added before v1.0.0). The new `_save_*` helpers landed but the cross-cutting contracts ([INV-50] `_dismiss_once`, [INV-51] save-failure notify, [INV-64] `_AGENT_BACKUP_LABELS` parity, the `_NEB_ENZYMES`-vs-`_all_enzymes()` consumer audit) were not all wired through. New tests: 13 cases in `tests/test_enzyme_collections.py` + 9 cases in `tests/test_new_agent_endpoints.py`.

* **`_AGENT_BACKUP_LABELS` parity (P0 ship-blocker).** `_USER_DATA_FILE_ATTRS` got `_CUSTOM_ENZYMES_FILE` + `_ENZYME_COLLECTIONS_FILE` and `RestoreFromBackupModal._TARGETS` covered both, but `_AGENT_BACKUP_LABELS` (`splicecraft.py:73145-73166`) was missed — the agent restore-backup endpoint couldn't list/restore the two new files. The `_h_restore_backup` cache-bust map and the bulk-restore cache tuple in `_h_restore_pre_update_snapshot` had the same gap. All three maps now mirror `_USER_DATA_FILE_ATTRS` minus `_SETTINGS_FILE`. Regression guard: existing `tests/test_sweep10.py::test_agent_backup_labels_parity_with_user_data_files` + new `test_agent_backup_labels_include_enzyme_files` in `tests/test_enzyme_collections.py`.

* **Modal `_dismiss_once` retrofit ([INV-50]).** `AddCustomEnzymeModal`, `EnzymeCollectionsModal`, and `_EnzymeNamePromptModal` all write to disk on dismiss (or carry payloads that trigger downstream saves) but lacked the double-fire guard. A double-click on Save in `AddCustomEnzymeModal` appended the same custom enzyme twice; the second pass bypassed the duplicate-name check because the FIRST pass had already persisted. Pattern uniform across all three: `_dismissed: bool = False` in `__init__` (AFTER docstring per [INV-41]), `_dismiss_once(payload)` helper, every exit path (button + Esc + worker callback) routed through it.

* **`_save_or_status` modal helper.** `EnzymeCollectionsModal` had six bare `_save_enzyme_collections(entries)` callsites in the CRUD button handlers (New / Rename / Duplicate / Delete / Add / Remove). Sacred invariant #7: `_safe_save_json` re-raises on OSError; without a wrap the exception would crash the modal mid-CRUD instead of routing through `_notify_save_failure`. New `EnzymeCollectionsModal._save_or_status(entries) -> bool` helper handles try/except + `_notify_save_failure` + inline `_set_status` red toast; each callsite becomes `if not self._save_or_status(entries): return`. Mirrors the pattern from `AddCustomEnzymeModal._save_btn` which wraps correctly.

* **Scan/cloning path routing to `_all_enzymes()` (P0).** Custom enzymes are visible to the restriction-overlay scan via `_SCAN_CATALOG` (rebuilt from `_all_enzymes()` after every `_save_custom_enzymes`), but **seven downstream consumers still keyed on `_NEB_ENZYMES` directly** — a user could add `MyEnzI` but couldn't use it in Domesticator, fragment excision, cloning grammars, or via the agent. Fixed:
  - `_enzyme_cuts_impl` (the underlying scanner used by all cloning paths via `_enzyme_cuts`) — local `catalog = _all_enzymes()` at function entry, all membership tests + indexing route through it.
  - `_excise_fragment_pair` (synthetic 2-enzyme digest used by traditional cloning UI) — same pattern.
  - `_site_for_enzyme` (cloning-grammar site lookup) — `_all_enzymes().get(enzyme)`.
  - `_clone_part_into_entry_vector` + IIS-conflict guard + second-pass clone helper (3 sites) — grammar's enzyme can now be a custom one.
  - `_design_cloning_primers` (cloning primer design) — local `catalog = _all_enzymes()`.
  - `_settings_validator_custom_enzymes_csv` (legacy CSV settings validator).
  - `_h_list_restriction_sites` agent endpoint — new `respect_active_collection: bool = true` parameter; when no explicit `enzymes` list is provided, defaults to the user's active enzyme collection so the agent mirrors UI overlay semantics. Explicit `enzymes` always wins.

* **`(IndexError|NoMatches, Exception)` antipattern cleanup.** 7 sites used the redundant tuple-with-Exception pattern that [INV-64] flagged as bare-except in disguise. `HistoryViewerModal` (19586) + `HistoryScreen` (19799) narrowed to `except IndexError:`. 5 sites in `ExperimentEditor` / `SequencingScreen` / `action_layout_*` narrowed to `except NoMatches:`. Sacred lesson: `Exception` already subsumes `IndexError` / `NoMatches`, so the tuple form is the same as `except Exception` — pyright won't catch it, only grep will.

* **12 new agent endpoints (CRUD parity per [INV-64] sweep #23 pattern).**
  - **Custom enzymes:** `list-custom-enzymes`, `get-custom-enzyme`, `create-custom-enzyme`, `update-custom-enzyme`, `delete-custom-enzyme`. Built-in NEB names refused for create (409) — agent must add a custom enzyme with the SAME name to override (matches the `_all_enzymes()` override-by-name contract). Shared `_agent_validate_custom_enzyme_payload` helper mirrors `AddCustomEnzymeModal._validate` so UI + agent accept the same shape.
  - **Enzyme collections:** `list-enzyme-collections`, `get-enzyme-collection`, `create-enzyme-collection`, `update-enzyme-collection`, `delete-enzyme-collection`. Update supports rename via `new_name` + enzyme-list replacement; rename onto an existing name returns 409. Delete clears the active-pointer if the deleted collection was active. Unknown enzyme names accepted at create/update time — `_active_enzyme_allowed_set` filters them at scan time so a custom enzyme added later participates retroactively.
  - **Active pointer:** `get-active-enzyme-collection`, `set-active-enzyme-collection`. `set` refuses unknown names (404); pass `name: null` to clear.

* **`_save_custom_enzymes` cache invalidation (correctness, not perf).** After `_save_custom_enzymes`, `_SCAN_CATALOG` is rebuilt, but three other caches that key on enzyme NAMES (not definitions) held stale results: `_ENZYME_CUTS_CACHE` (LRU on `(hash(seq), tuple(sorted enzymes), circular)`), `_RESTR_SCAN_CACHE` (LRU on the scan params), and `_ASSEMBLY_FRAGMENT_CACHE` (which caches `None` for unresolved enzymes — a newly-added custom enzyme would forever hit the cached `None`). All three now `.clear()` at the end of `_save_custom_enzymes`. `_save_enzyme_collections` does NOT need this fix — `_RESTR_SCAN_CACHE`'s key already includes `allowed_enzymes`, so it self-invalidates on collection change.

* **`AddCustomEnzymeModal` agent-payload validation hardening.** Pyright caught `int(payload.get("fwd_cut"))` accepting `None` and raising at runtime via `TypeError`. Refactored to extract the raw values, explicit `None` check ("missing 'fwd_cut' or 'rev_cut'" → 400), then `int(raw)` inside a `(TypeError, ValueError)` catch — cleaner error message + appeases pyright.

* **`_EnzymeNamePromptModal` fit guarantee.** Added to `tests/test_modal_boundaries.py::_MODAL_CASES` so the 160×48 baseline-terminal fit test exercises the prompt modal alongside `EnzymeCollectionsModal` + `AddCustomEnzymeModal` + `SettingsModal` (all already covered from initial landing).

* **Deferred (P2 follow-up candidates).** UI palette dropdowns (`_CLONING_RE_OPTIONS` at 32355, restriction-insert modal at 51199, traditional-cloning enzyme picker at 55246, primer-design dialog at 62776) still enumerate `_NEB_ENZYMES.keys()` directly. Custom enzymes appear in the scan + cloning paths but NOT in these dropdown widgets — a user can type a custom-enzyme name in a custom field but can't pick from the picker. Lower priority since the cloning path resolution is correct; UX gap, not a robustness gap. `EditGrammarDialog` enzyme validation (38808) similarly accepts only NEB enzymes — defer until a user requests custom-enzyme-keyed grammars. Library-walk perf items from audit #3 (BulkExportCollectionModal compose, full-library entry-id lookup at 3 sites) deferred — they're optimisations not correctness fixes, and benchmarking them is more cost than the 1.0.0 deadline allows.

## [INV-66] Sweep #25 — full adversarial audit (2026-05-23)
Four parallel sub-agents covered footguns/correctness, performance, resource utilization, and hardening; each briefed with INV-36…INV-65 to avoid duplicating prior sweeps. Combined surfaced ~40 findings; this sweep landed all CRITICAL/HIGH and most MEDIUM/LOW. New tests: 37 cases in `tests/test_sweep25.py`. Several MEDIUM items (M7 export TOCTOU via O_EXCL, M8 export-endpoint symlink ancestor walk, M12 clipboard-dir retention, M15/M16/M17/M18/M19 additional iterator-conversion + cache caps, M21 stale-canvas guard on Plasmidsaurus alignment, H13 `_draw_linear_flag` wrap-feat enumerate, H7 fd-pass into zipfile) and LOW items (L2/L4/L5/L7/L8/L10) were assessed lower-value or larger-than-sweep and deferred to a future sweep.

* **Cache-bust enumeration drift CLOSED (P0).** `_MASTER_DELETE_CACHE_ATTRS` is now the single source of truth for both restore paths (`RestoreFromBackupModal._do_restore` + `_h_restore_pre_update_snapshot`). Pre-sweep both maintained their own hand-listed tuples that drifted: sweep #9 added experiments/projects/gels (missed in modal); sweep #20 added `_primer_usage_cache` + `_feature_library_index_cache` (missed in both); sweep #24 added `_custom_enzymes_cache` + `_enzyme_collections_cache` (missed in modal). Future caches now enroll automatically. `RestoreFromBackupModal` continues to skip `_settings_cache` (intentional — UI restore would clobber live pointer state mid-session); the agent path includes it (invoked outside the running app). Regression guards in `TestCacheBustParity`.

* **`_iter_library_readonly` + `_iter_parts_bin_readonly` + `_find_library_entry_by_name` helpers.** Mirror the sweep-#23 `_iter_collections_readonly` pattern: read-only view returns cached list directly (no `_typed_clone`). Pre-sweep ~40 library-iteration sites paid the full multi-MB deepcopy on every call (every primer-usage scan, every plasmid pick, every collection sync). Converted 13 highest-impact sites in this sweep: agent endpoints (`_h_list_library`, `_h_list_collections`), id-set construction for save-time disambiguation (4 sites), primer-usage scanners (`_index_primer_usage_in_collections` + `_find_primer_plasmid_usages`), assembly fragment recover, single-id agent endpoints (`_h_transfer_annotations`, `_h_diff_plasmid`, `_h_align_plasmidsaurus_zip` — id+name via new sibling helper), and `LibraryPanel._row_selected`. Remaining ~70 callsites (mostly write-paths that legitimately need `_load_library()`) audited as safe-as-is.

* **Lock-discipline gap widened (`_cache_lock`).** INV-50/51 closed cross-group worker RMW races for Constructor/Gibson/Domesticator/Traditional; this sweep extended:
  - **Custom-enzyme + enzyme-collection agent endpoints** (`_h_create_custom_enzyme`, `_h_update_custom_enzyme`, `_h_delete_custom_enzyme`, `_h_create_enzyme_collection`, `_h_update_enzyme_collection`, `_h_delete_enzyme_collection`) — full collision-check + load + mutate + save under `_cache_lock`. INV-65 added these endpoints but missed the lock; two concurrent agent calls could both pass the collision check then both append, silently dropping the first writer.
  - **App-level Ctrl+S `_save_worker` library RMW** (the canonical save path that the lock was originally built for!). Pre-fix `_load_library()` happened outside the lock; a Gibson/Constructor save landing between load and `_save_library` would be silently dropped.
  - **Five disk-writer workers** (`_add_save_to_disk`, `_delete_save_to_disk`, `_collections_save_to_disk`, `_trad_save_to_disk`, `_rename_save_to_disk`) now wrap their `_safe_save_json` calls in `with _cache_lock:`. Pre-fix cross-group workers could race the disk writes. The current design (UI thread updates cache; worker writes disk) is preserved — the lock only serialises the writes. Remaining race: cross-group out-of-order writes can still land a stale payload over a newer one; mitigation deferred to a future sweep (clean fix is "worker writes the latest cache state, not the dispatch-time payload" — bigger refactor).
  - **`_sync_active_collection_plasmids` sync path** — full RMW under `_cache_lock`, using `_iter_collections_readonly` + targeted `_typed_clone` of only the modified entry (pre-fix paid 2× ~160 MB deepcopies per save on main thread; now reads by reference, clones only the target).

* **`_SCAN_CATALOG` atomic global reassign.** `_rebuild_scan_catalog` (called after every `_save_custom_enzymes` per INV-65) was clearing+appending the global list in-place. A concurrent `_restr_scan_worker` iterating the list could see a half-built catalog → missed cut sites or `IndexError` mid-iteration (visible as flickering restriction overlay mid-enzyme-add). Now builds local list then `globals()["_SCAN_CATALOG"] = new_list` (atomic GIL-protected).

* **Agent server hardening (H5/H8/H9/M4/M5/M6).** Pre-sweep `if write and not self._check_token()` — read endpoints were unauthenticated, despite several doing real work (hmmscan, blast, list-library, zip parse) that leaks filesystem state or consumes CPU/RAM. Now:
  - **All endpoints require bearer token** (except `""` / `tools` which short-circuit for client self-describe before token-acquisition).
  - **Token check fires BEFORE handler lookup** so unknown paths uniformly 401 (don't leak endpoint-list oracle via 404).
  - **Write endpoints require POST** (`GET /delete-from-library` pasted into a browser no longer silently fires); read endpoints accept both GET/POST (queries via body stay ergonomic).
  - **`_AgentAPIServer.timeout = 30` + handler `timeout = 30` + `request_queue_size = 16`** for slow-loris mitigation.
  - **`_read_body` returns `"__bad_body__"` sentinel** on malformed JSON / UTF-8 errors → dispatcher converts to 400 with `"malformed JSON body"`. Pre-fix silent `{}` masked caller serialisation bugs.
  - **Exception fall-through scrubs `_DATA_DIR` paths from response body** via `_scrub_path`. Pre-fix `OSError`/`FileNotFoundError`/`PermissionError` `str(exc)` carried the full offending path → leaked data-dir layout.
  - **`_h_load_file` + `_h_list_plasmidsaurus_members` + `_h_align_plasmidsaurus_zip` errors collapsed to uniform 400** with `"file rejected (see splicecraft log)"`. Pre-fix differentiated 404/400/413 responses (with size info) acted as a filesystem-state oracle for unauthenticated callers. Logs still carry the local-user diagnostic detail.
  - **`_h_gibson_assemble`** now wraps `_agent_refresh_library_panel(app)` in `app.call_from_thread(...)` — every other library-mutating endpoint had this; Gibson was the one site missed. Pre-fix touched DataTable widgets from the HTTP worker thread.
  - **Token via `secrets.token_urlsafe(32)`** (256-bit) — was `uuid.uuid4().hex` (122-bit). `secrets` is the stdlib's documented bearer-token primitive.

* **`_get_setting` cache-direct read.** 59 callsites; pre-fix every call did `_load_settings().get(key, default)` which fully deepcopies the settings dict. `EditCustomizeModal` open paid 4 clones; `_get_active_collection_name()` paid 24. Now reads `_settings_cache.get(key, default)` directly under `_cache_lock`, `_typed_clone`s only the matched value (negligible cost for the JSON primitives that cover ~99% of keys; still defensive for the rare container values like `experiments_custom_dict`).

* **`_rc(tgt_seq)` hoist in `_find_annotation_transfers`.** Pre-fix the RC call was inside the per-feature loop, paying O(F × N_tgt) — for a 100 kb source × 200 features, ~20 MB of RC work that should be ~100 kb. Now hoisted above the loop.

* **`_GB_PARSE_CACHE` cap 64 → 16.** A parsed SeqRecord is 5–15× its source text in memory; at 64 × 64 MB worst case the cache could hold ~40 GB resident (no byte-aware eviction). 16 entries still covers observed hot paths.

* **`_STACKS_LOG_FD` startup-time rotation.** `faulthandler.register` holds a borrowed FD so we can't rotate mid-session; truncate at startup if prior sessions accumulated > 10 MB. Pre-fix unbounded growth (a user hitting SIGUSR1 once a week accumulated MBs over a year).

* **`_safe_identifier` validator on `active_*` settings.** A hand-edited `settings.json` with `"active_project": "../../../etc/foo"` passed `_validate_settings` because only string-type was checked. Currently looked up by name (safe), but a future refactor joining the name into a path would silently inherit a traversal vuln. New `_is_safe_identifier` helper rejects `..`, `/`, `\\`, NUL on the 6 `active_*` keys.

* **Snapshot directory-restore 5 GB cap.** Pre-fix the file branch of `_restore_pre_update_snapshot` had mandatory sha256 verify; the directory branch (`shutil.copytree`) had no size check. Tampered manifest could declare `n_files=0` for a multi-GB dir and exhaust user disk.

* **`_VECTOR_MATCH_CACHE` key on `hash(gb_text)` not raw string.** Pre-fix 64 × 5 MB worst case = 320 MB just in tuple keys. `hash(str)` is deterministic per-process; collisions functionally indistinguishable from cache miss.

* **GFF3 silent feature drop now logs.** `_gff3_features_to_biopython` drops out-of-range or zero-length features; pre-fix the drop was silent (one bad row in a multi-part feature destroyed the entire feature with no warning). Now `_log.info` per drop with the `gff_id`.

* **`RestoreFromBackupModal._dismiss_once` retrofit.** INV-64 covered other writes-on-dismiss modals; this one was missed. Button + Esc race could double-fire callback.

* **Bare-except narrowing.** `except (OSError, Exception)` → `(OSError, RuntimeError, ValueError, KeyError, TypeError)` in `_h_design_primers`'s Primer3 catch (INV-65's grep for `(AttributeError, Exception)` shape missed `(OSError, Exception)`). NCBI/Kazusa network fetches → `(OSError, _urllib_error.URLError [, ET.ParseError])`. `target_seq = ""` alignment fallback → `(AttributeError, TypeError)`. Sentinel test added in `TestBareExceptNarrowing`.

* **`AddCustomEnzymeModal._save_btn` wrapped properly.** INV-65 said "wraps correctly" but only the in-modal status line was wrapped, not the structured event. Now uses `_cache_lock` for the full RMW AND calls `_notify_save_failure` so failures emit the `save.failed` structured event Alt+D bug bundles pick up.

* **Crash-recovery prune (30-day age + 50-file cap).** Pre-fix `_CRASH_RECOVERY_DIR` accumulated forever; a user hitting one crash per week for a year accumulated ~50 unnoticed `.gb` files (each a full plasmid dump, ~10–500 KB). Prune fires on every `_check_crash_recovery` startup pass.

* **`_atomic_copy` / `_atomic_marker_write` / agent token file write fd-close.** Pre-fix `open(src, "rb")` or `os.fdopen` raising before the `with` construct leaked `fd`. New `fd_owned` flag tracks ownership; except path closes the raw fd if `_os.fdopen` never took it.

* **`splicecraft_cli._read_session` lstat token file.** Pre-fix `f.stat()` followed symlinks; a pre-placed symlink at `~/.local/share/splicecraft/agent_token` pointing to `/etc/passwd` would be silently followed and leak first 1 KB via the malformed-port stderr error. Now `lstat` + `S_ISLNK` refusal.

* **Lockfile unlink on graceful exit.** `_release_data_dir_lock` now best-effort-unlinks `splicecraft.lock` after the `LOCK_UN`. Cosmetic on local filesystems (PID-alive recheck handles stale lockfiles), but cleaner on NFS/SMB shares.

* **Architectural recommendation deferred to v1.1.** The recurring cache-bust enumeration drift (caught 4 times: INV-43, INV-50, INV-64, here) and `_AGENT_BACKUP_LABELS` parity drift (INV-64) suggest a `_PERSISTENT_CACHE_REGISTRY: dict[str, CacheSpec]` with `attr_name`, `file_attr`, `backup_label`, `restore_handler`. The single source of truth would eliminate the entire bug class. Not landed this sweep — touches 5+ subsystems; defer until v1.0.0 ships.

### Sweep #26 follow-up (same INV-66; 2026-05-23 same-day)

User asked for "fix all deferred" after sweep #25's first pass landed v0.9.20. This second pass closed every deferred-list item with concrete value, leaving only the v1.1 architectural refactor unaddressed. Test count grew 37 → 61 in `tests/test_sweep25.py`; the file's docstring documents both batches.

* **L2 — PyPI HTTPS-by-default.** `_resolve_pypi_url` now refuses `http://` overrides unless `SPLICECRAFT_PYPI_INSECURE=1` is explicitly set. The update-check response feeds version-comparison logic; an in-path attacker on a corporate LAN could downgrade-attack via plain-http manipulation. Tests in `TestSweep26PypiHttpDowngrade`.

* **L4 — `_check_data_files` driven by `_USER_DATA_FILE_ATTRS` registry.** Pre-fix the 17-entry list was hand-maintained — same parity-drift class as INV-64 caught for `_AGENT_BACKUP_LABELS`. Now derives labels from `_AGENT_BACKUP_LABELS` inversion (with special-case overrides for 3 non-standard hand-labels) plus an attr-name fallback. New files added to `_USER_DATA_FILE_ATTRS` auto-enroll in startup validation. Tests in `TestSweep26CheckDataFilesRegistry`.

* **L5 — `_ENZYME_CUT_RANGE = 30` module constant.** Replaces hardcoded `±30` in `AddCustomEnzymeModal._validate` (UI) and `_agent_validate_custom_enzyme_payload` (agent). Single point of adjustment. Tests in `TestSweep26EnzymeCutRangeConstant`.

* **L6 — NCBI / Kazusa narrow excepts use `urllib.error.URLError`.** `_ncbi_taxid_search` and `_codon_fetch_kazusa` now `import urllib.error as _urllib_error` and catch `(OSError, _urllib_error.URLError [, ET.ParseError])` instead of bare `Exception`. Real bugs in the parse paths (KeyError on missing XML field, etc.) now propagate instead of disguising as "Network error".

* **L7 — `agent.write.ok` log fires AFTER `_send` succeeds.** A broken-socket `_send` failure now emits a distinct `agent.write.send_failed` event so forensics can distinguish "handler succeeded but client never saw the response" from "handler succeeded and response delivered". Pre-sweep the log fired before the response landed; a `_send` failure could leave the audit log saying success while the client retried. Tests in `TestSweep26LogEventTimingAfterSend`.

* **L8 — `_ACCEPTOR_TU_PAIRS_CACHE` FIFO cap = 64.** Pre-sweep unbounded (only invalidated on `_save_entry_vectors`). Bounded in practice by `(grammar_id × enzyme)` tuples, but a user with many custom grammars could grow this. Mirrors `_VECTOR_MATCH_CACHE`'s 64-entry cap.

* **L9 — already landed in sweep #25's first pass** (lockfile unlink on graceful exit).

* **L10 — `_build_system_info` rglob cap at 10k files.** Pre-sweep unbounded `rglob('*')` count over user-data dirs. On heavy installs (thousands of attachments / crash recovery files / .dna sidecars) this was a CPU burst per diagnostic bundle / log-startup. The exact count past 10k isn't useful for triage — `"≥10000"` tells the same story.

* **M3 — already landed in sweep #25** (snapshot dir restore 5 GB cap).

* **M7 — assessed already-mitigated.** Audit suggested O_EXCL on export writes. `_atomic_write_text`'s inner `lstat` check catches symlink-mid-flight; the remaining "regular file created between agent's check and atomic-replace" race isn't a security gap (just a different overwrite than expected). Skipped intentionally.

* **M8 — `_check_agent_read_path_ancestors` helper + applied to Plasmidsaurus paths.** New helper mirrors `_check_agent_write_path`'s ancestor-walk pattern for read endpoints. Applied to `_h_list_plasmidsaurus_members` + `_h_align_plasmidsaurus_zip` so a parent symlink (`~/Documents` → `/etc`) doesn't silently redirect the downstream `os.open`. Tests in `TestSweep26AncestorReadCheck`.

* **M12 — clipboard fallback dir prune.** `_DATA_DIR/clipboard/` tier-3 tmpfiles now prune on every write: 7-day age cap OR 100-file count cap (oldest first). Pre-sweep the dir accumulated forever; a long-running session that frequently hit tier-3 (broken X11 / SSH / WSL-no-tty) could leave hundreds of MB of sequence-shaped tmpfiles around.

* **M16 — `ConstructorModal._build_palette_rows` + 5 more parts-bin readers** use `_iter_parts_bin_readonly`. Loop bodies only `.get()` fields then build fresh row dicts.

* **M17 — `_iter_all_grammars_readonly` helper added.** Skips the per-call `deepcopy` of every built-in grammar dict that `_all_grammars` pays. Built-ins returned by reference (callers MUST NOT mutate); custom grammars get a shallow clone for `editable=True` injection. Callsite conversions deferred to a future sweep — adding the helper is the gating change. Tests in `TestSweep26GrammarsReadonly`.

* **M18 — `_gb_text_to_record(cache=False)` for batch parses.** New keyword arg lets one-shot batch consumers skip the `_GB_PARSE_CACHE` populate. Used in Plasmidsaurus `_batch_extract_gbk_meta` where 50+ samples × multi-MB assemblies would absorb the entire batch into the cache (~250 MB pressure in one click). Tests in `TestSweep26GBParseCacheFlag`.

* **M15 — 4 more library-iter conversions** (entry-vector picker, linear-library picker, traditional-cloning lib-by-name index, add-record id-set). All read-only patterns, no mutation.

* **M19 — assessed lower-value.** Per-cache count cap of 16 is a reasonable bound; aggregate-bytes tracking adds complexity for a worst-case scenario that requires multiple multi-MB plasmids open in series. Documented as-is.

* **M21 — `_h_align_plasmidsaurus_zip` post-alignment target-drift detection.** Captures `resolved_target_id` at handler entry; after the (possibly multi-second) `_pairwise_align` completes, re-looks up by id. Returns 410 Gone if the target was deleted mid-flight (alignment result is technically valid but the named target no longer exists for downstream `set-active`/`load-entry`). Surfaces `_target_renamed_to` flag in the result payload if the target was renamed (alignment still ships; agent's follow-ups use the current name). Tests in `TestSweep26AlignmentDriftDetection`.

* **H7 — TOCTOU-safe zip open via `os.open` + `fileobj=`.** Both `_list_gbk_members_in_zip` and `_extract_gbk_member` now open the zip path via `os.open(path, O_RDONLY)` (which dereferences the symlink ONCE), `fstat` on the fd (immune to a concurrent path swap), then `zipfile.ZipFile(fileobj=os.fdopen(...))`. Closes the TOCTOU window that pre-sweep let a hostile local process swap the file between the path-based stat and the path-based `ZipFile(str(p))`. Explicit `fobj.close()` in `finally` since `ZipFile.close()` doesn't close caller-supplied fileobj. Tests in `TestSweep26ZipFdPass`.

* **H13 — `_wrap_feats_idx` precomputed in `load_record`.** `_draw_linear_flag` no longer enumerates every feature on every render frame just to filter for wrap features. Maintains `_wrap_feats_idx: list[int]` alongside `_feats_by_start` per the established precompute pattern. On dense plasmids (1000+ features) the per-frame O(F) wrap scan was a noticeable tax on rotation + zoom interaction. Tests in `TestSweep26WrapFeatsIdx`.

* **Removed unused `uuid` import** (left over from sweep #25 L3 switching to `secrets.token_urlsafe(32)`). Caught by `ruff check` in `release.py`'s gate.

* **Architectural cache-registry refactor remains v1.1.** Same conclusion as sweep #25's first pass.

---

## [PIT-01]…[PIT-39] Known pitfalls (moved from CLAUDE.md)

Pitfalls discovered during regular development and codified as rules. Each has at least one regression test. **`[PIT-NN]` numbering is stable** — old grep recipes like `grep -ni 'pitfall #17' docs/` still work because the numbering matches the historic CLAUDE.md ordering.

1. **[PIT-01] Bare `except` forbidden.** Use narrow types (`NoMatches`, `ET.ParseError`, `(OSError, json.JSONDecodeError)`). Bare `except Exception` reserved for `@work` thread bodies — always `_log.exception` there.
2. **[PIT-02] User-facing errors:** `self.notify(...)` or `Static.update("[red]...[/]")`. Never raw tracebacks. Diagnostic detail → `_log.exception`.
3. **[PIT-03] Wrapped features (`end < start`) first-class.** Use `_bp_in()` / `_feat_len()` for any distance, midpoint, or membership check.
4. **[PIT-04] Cache keys use `id(...)` of feature lists.** Correct only because lists are *reassigned* on load, not mutated. Don't start mutating `self._feats` in-place.
5. **[PIT-05] Textual reactive auto-invalidation requires assignment, not mutation.** `self._feats = new_list` triggers refresh; `.append(x)` does not.
6. **[PIT-06] Primer3 is linear-only.** For wrap regions, rotate template to `seq[start:] + seq[:start]`, then unrotate via `(coord + rotation) % total`. See `_design_detection_primers`.
7. **[PIT-07] `_source_path` survives in-place edits.** Cleared only when `clear_undo=True` (fresh loads). `_discard_changes` explicitly stashes/restores `_source_path`.
8. **[PIT-08] NCBI XML routes through `_safe_xml_parse`.** Rejects DOCTYPE/ENTITY before `ET.fromstring`.
9. **[PIT-09] Migration runs in `App.compose()`, not `on_mount`.** Textual mount fires leaves→root; `App.on_mount` runs AFTER `LibraryPanel.on_mount`. Collections / active-collection setup must happen before children mount.
10. **[PIT-10] `_save_library` mirrors to active collection.** Every panel CRUD writes BOTH `plasmid_library.json` and `collections.json`. Routing around `_save_library` (e.g. `_restore_library_from_active_collection`) bypasses the mirror; do that only when the collection IS the source.
11. **[PIT-11] Wrap-CDS rendering uses `_orig_start`/`_orig_end`.** `_feats_in_chunk` splits wrap features into linear half-features; CDS halves carry original coords as `_orig_start` / `_orig_end`. Codon-midpoint math, AA translation, AA-click detection must read `f.get("_orig_start", f["start"])`. Half-local `f["start"]` (= 0 for head halves) gives wrong reading frame.
12. **[PIT-12] `_re_highlight` schema (0.4.5+):** `start, end, top_cut_bp, bottom_cut_bp, color, name`. Legacy `fwd_cut_bp`/`rev_cut_bp` gone. Resites with `cut == -1` fall back to plain `black on white`.
13. **[PIT-13] Map rotation keys live on `PlasmidMap.BINDINGS`.** Not `App.BINDINGS` — rotations would fire from modals. App-level `on_key` skips arrow / Enter when `DataTable`, `Input`, or `PlasmidMap` is focused.
14. **[PIT-14] Ctrl+Shift+C is functionally an alias for Ctrl+C** (both ETX, 0x03). Alt+C is the actual RC-copy trigger.
15. **[PIT-15] `PlasmidApp.on_key`/`on_click` early-return when `len(screen_stack) > 1`** so seq-panel cursor / RE-highlight clears can't fire under modal. Ctrl+Z / Ctrl+Y above this guard.
16. **[PIT-16] `_blast_get_db` LRU invalidated by `_save_collections`** via `globals().get("_blast_clear_cache")()`. Any new collection-mutation path not going through `_save_collections` must call `_blast_clear_cache()` manually.
17. **[PIT-17] Cache contracts (deepcopy on BOTH read AND save).** `_load_library` / `_load_collections` / `_load_features` / `_load_custom_grammars` / `_load_parts_bin` / `_load_primers` deepcopy on read; corresponding `_save_*` deepcopy when re-seating the cache. Without both halves, a caller editing the list it just saved leaks post-save mutations into the next reader.
18. **[PIT-18] Trademark scrub.** `.dna` is the popular commercial plasmid editor's binary format. Code identifiers use `CommercialSaaS` / `commercialsaas` / `_BIOPYTHON_DNA_FMT`. BioPython API string and 8-byte cookie magic stored hex-encoded as `_BIOPYTHON_DNA_FMT` and `_COMMERCIALSAAS_COOKIE_MAGIC` so trademarked text never appears verbatim. User-facing prose says "popular commercial plasmid editor file format".
19. **[PIT-19] Untrusted XML routes through `_safe_xml_parse`.** Includes NCBI responses AND `.dna` history packets (`_parse_commercialsaas_history`).
20. **[PIT-20] Network reads size-capped.** PyPI (`_PYPI_MAX_RESPONSE_BYTES`), NCBI (`_NCBI_MAX_RESPONSE_BYTES`), Kazusa (`_KAZUSA_MAX_RESPONSE_BYTES`). Any new HTTP fetch must follow `resp.read(MAX + 1)` + bail-if-exceeded.
21. **[PIT-21] `_extract_commercialsaas_history_xml` uses streaming LZMA decompress** with `max_length=cap+1`.
22. **[PIT-22] `_dna_sidecar_path` strips `..`/dot-only/NUL** via `Path(...).name` after replacing separators. Don't loosen — `entry_id` is user-controlled.
23. **[PIT-23] `_safe_load_json` size-capped at `_SAFE_LOAD_JSON_MAX_BYTES` (1 GB).** Distinct from the 50 MB `_BULK_IMPORT_MAX_BYTES` agent-API cap (different threat models).
24. **[PIT-24] `_h_load_file` agent endpoint size-capped at `_BULK_IMPORT_MAX_BYTES` (50 MB)** with `force=true` override.
25. **[PIT-25] `_excise_fragment_pair` enforces exactly-2 cuts on circular plasmids.** ≥3 cuts surfaces error rather than ambiguous fragments. Sacred — restriction-cloning correctness depends on this.
26. **[PIT-26] GFF3 export off-by-one.** `_record_to_gff3` converts 0-based half-open to 1-based inclusive: `start+1`, `end` (unchanged because GFF3 end is inclusive). Wrap features emit two rows sharing one `ID=`; circular records carry `Is_circular=true` on synthesised `region` row. Source features filtered.
27. **[PIT-27] Annotation transfer exact-match only.** `_find_annotation_transfers` does verbatim substring on both strands; no fuzzy / BLAST. Skips below `_ANNOT_TRANSFER_MIN_LEN` (30 bp). Wrap-aware; whole-plasmid case (`feat_len == n_tgt`) special-cased to single `[0, n)` transfer.
28. **[PIT-28] Pairwise alignment cap + cancellability.** `_pairwise_align` caps at `_PAIRWISE_MAX_LEN = 200_000` bp per side. PairwiseAligner C loop **cannot be cancelled mid-flight** — `_diff_align_worker` uses `exclusive=True`. Workers capture `_record_load_counter` at entry and refuse if canvas moved on (mirrors `_restr_scan_worker`, `_seed_default_library`).
29. **[PIT-29] Cross-collection search skips id-less entries.** `_search_collections_library` filters entries lacking `id` to avoid aliasing dismiss payload `(collection, "")`. Same reason `LibrarySearchModal` row keys carry the `(collection, id)` pair.
30. **[PIT-30] Agent endpoints `transfer-annotations`/`diff-plasmid` look up against active library only** (via `_load_library()`). Cross-collection lookup is the `search-library` endpoint's job; agents call that first, then `set-active-collection`, then transfer/diff.
31. **[PIT-31] Four-layer JSON data-safety net.** Every `_safe_save_json` write produces: (a) `<file>.bak` single-gen (back-compat with `_safe_load_json` recovery); (b) timestamped `<file>.bak.YYYYMMDD-HHMMSS` (`_BACKUP_RETENTION_COUNT = 10`); (c) daily `<DATA_DIR>/snapshots/<stem>-YYYY-MM-DD.json` (`_SNAPSHOT_RETENTION_DAYS = 30`, via `_snapshot_data_files` at launch); (d) suspicious-shrink guard (>50% loss + ≥5 prior entries) spills to `<DATA_DIR>/lost_entries/` BEFORE overwrite. Restore UI: `Settings → Restore … from backup…` (`RestoreFromBackupModal`); helpers `_list_recoverable_backups` + `_restore_from_backup` reusable from agent path.
32. **[PIT-32] `_skip_snapshot: bool = True`** on `PlasmidApp` (test default); `main()` flips False. Same pattern as `_skip_seed`, `_skip_update_check`.
33. **[PIT-33] Natural-sort row mapping symmetric.** Every screen sorting a `DataTable` for display (`LibraryPanel`, `FeatureLibraryScreen`, `PartsBinModal`, `MutagenizeModal`, `PrimerDesignScreen`, `PlasmidPickerModal`, `TraditionalCloningPane`, `_palette_rows_for_grammar`) MUST resolve every `cursor_row` lookup against the SAME sort. Mismatched sort/lookup is the 0.7.4.5 bug class. `FeatureLibraryScreen` uses `_row_to_entry_idx` + `_entry_idx_to_row`; `PrimerDesignScreen` uses `_row_to_primer_idx`. `PlasmidPickerModal` sidesteps via `key=e.get("id")` on `add_row` (preferred pattern for new pickers).
34. **[PIT-34] `_classify_part_from_plasmid` is grammar-by-grammar Type IIS digest.** Loops `_all_grammars()`, runs `_excise_fragment_pair`, picks first 2-fragment digest whose smaller fragment's `(left.overhang_seq, right.overhang_seq)` matches a position. Smaller = insert; larger = vector. Linear records skipped. Parts Bin "Load Part" runs in `@work` thread (sync froze UI 200–500 ms on plasmids with many grammars).
35. **[PIT-35] CommercialSaaS `.dna` writer emits the editor's full default packet inventory.** `_write_commercialsaas_dna_bytes` writes 0x00 (sequence) + 0x0A (features) + 0x06 (notes) + 0x08 (`AdditionalSequenceProperties`, 289 bytes) + 0x05 (`Primers` with `HybridizationParams`, 217 bytes) + optional history. Defaults match real CommercialSaaS files even when no user primers / no meaningful end-stickiness on circular plasmids — Viewer's panels fall back to "(empty)" if missing. Byte-for-byte assertions in `tests/test_commercialsaas_io.py::TestWriteCommercialSaaSDnaBytes`.
36. **[PIT-36] Rename keeps id in sync with sanitised display name; legacy libraries are backfilled on first load. Seven one-shot backfills run on first cache populate.** `_rename_library_entry` sets `e["name"] = new_name.strip()` AND `e["id"] = sanitize(new_name)` (with `_2`/`_3` disambiguation against other entries' ids), then re-serialises gb_text so the LOCUS line + `rec.id` agree with the new name. The display name can carry whitespace and `+`/`·` but the GenBank LOCUS line forbids whitespace, so `rec.name` is sanitised via `re.sub(r"[^A-Za-z0-9_-]+", "_", new_name).strip("_")[:_GB_LOCUS_NAME_MAX]`. Pre-fix the SeqIO writer raised `ValueError("Invalid whitespace in '...' for LOCUS line")`, the caller swallowed it, and gb_text drifted away from `e["name"]` — the next reader saw the OLD LOCUS while the row showed the NEW display name. **Invariant (2026-05-24):** `e["id"] == sanitize(e["name"])` AND `e["name"] == e["name"].strip()` for every library entry. Pre-2026-05-24 rename kept `id` immutable; after `MAV 34 → MAV 33` the entry carried `id="MAV_34"` forever, which made `NamePlasmidModal` reject any attempt to save a NEW "MAV 34" (id-collision check). The new rename flow updates id alongside name; the one-shot `_backfill_library_ids_match_names` runs on first cache populate to enforce BOTH invariants on legacy libraries (gated by `_id_name_backfill_done`; rewrites in place + re-saves once). **Migration MUST funnel through `_ensure_library_cache_populated_and_migrated`:** initial attempt put the backfill only in `_load_library`, but `_iter_library_readonly` / `_find_library_entry_by_id` / `_find_library_entry_by_name` each populate `_library_cache` independently — if any of them ran first at startup (LibraryPanel state restore, primer-usage indexing, etc.), the cache warmed pre-migration and the subsequent `_load_library` short-circuited on the populated cache, skipping the backfill forever for that process. The helper centralises the populate + backfill so the migration runs exactly once on the first cache-populating call regardless of entry point. **Whitespace trim invariant + the .dna trailing-space trap:** `.dna` filenames sometimes carry a trailing space before the extension (`'MAV 27 ….dna'`) and the import flow used to seed `e["name"]` with that space. The delete-cascade in `_request_plasmid_delete` and the rename-cascade in `_rename_library_entry` do strict `==` on `(b.get("name") or "")` against the library entry's `name`, so a stale trailing space silently MISSED the parts_bin row (the parts_bin entry was saved without the space). Three lines of defence: (a) every library-entry creation site now calls `.strip()` on `display_name` and `id` (`_import_and_persist`, the bulk-import path); (b) the backfill above also trims; (c) the cascade match itself was made `.strip()`-tolerant on both sides. **NamePlasmidModal warning:** the modal's status line now surfaces a dedicated `[yellow]⚠ leading/trailing whitespace will be stripped[/yellow]` chip when the typed name has whitespace on either end, separate from the generic "Will save as" hint — so the user notices BEFORE submitting that what they typed isn't what they'll get. Display-name collisions in `NamePlasmidModal` remain a hard block; id-collisions remain a hard block (now they should be virtually impossible). **Trade-off:** rename of a plasmid that's referenced as a parent in another entry's `history_xml` breaks the cite-by-id link — history nodes use display `name` for the user-visible label and the integer `node_id` for tree structure, so the visible damage is small. **Cascade:** rename mirrors the new name into matching parts-bin rows (same `(name, grammar)` match the library-delete cascade uses, both strip-tolerant) — parts_bin entries have no `id` field so no id cascade needed there. **Seven backfills total (2026-05-24 sweep):** (1) `_backfill_library_ids_match_names` on `plasmid_library.json` — id == sanitize(name), name trim; (2) `_backfill_parts_bin_sequences` on `parts_bin.json` — populate empty `sequence` on L1+ entries by re-digesting gb_text (with overhang + length sanity); (3-4) the same two backfills applied to embedded `plasmids` / `parts` lists inside `collections.json` / `parts_bin_collections.json` so non-active collections aren't left stale; (5) `_backfill_entry_vector_names` — whitespace trim; (6) `_backfill_primer_names` on `primers.json` — whitespace trim; (7) embedded-primer name trim across `primer_collections.json`. Each is gated by a per-cache `_*_backfill_done` flag and idempotent. **Test-suite contract:** the `_protect_user_data` conftest fixture sets ALL backfill flags to `True` per test (skip), because test data usually deliberately deviates from the invariants for setup convenience. Tests that exercise a backfill must opt in by flipping the relevant flag to False before triggering the load — see `TestLibraryIdBackfill::test_load_library_triggers_backfill_once` for the pattern. **Hardening:** every backfill has per-entry try/except so one corrupt row doesn't abort the batch; never deletes entries; name-trim variants never ADD a `name` field where missing; parts_bin sequence backfill skips on overhang mismatch (grammar-drift detector) or body-larger-than-gb_text (malformed digest catcher). Regressions: `test_existing_ids_maps_to_display_name`, `test_rename_updates_id_to_sanitised_new_name`, `test_backfill_rewrites_id_to_match_name`, `test_load_library_triggers_backfill_once`, `test_backfill_trims_leading_and_trailing_whitespace`, `test_backfill_handles_corrupt_entries_without_aborting`, `test_parts_bin_sequence_backfill_skips_on_overhang_mismatch`, `test_parts_bin_sequence_backfill_skips_on_huge_body`, `test_entry_vectors_name_trim_does_not_add_name_field`, `test_entry_vectors_name_trim_skips_non_string_name`, `TestNamePlasmidModalWhitespaceWarning` (`test_trailing_space_shows_warning`, `test_leading_space_shows_warning`, `test_no_warning_when_already_trimmed`).
37. **[PIT-37] Non-modular Constructor tabs (Traditional + Gibson) have no parts-bin mirror; modular tabs (GB / MoClo) always do.** Hard rule clarified 2026-05-24: only Constructor tabs that carry a grammar association write to the parts_bin. `TraditionalCloningPane._commit_product_to_library` and `GibsonAssemblyPane._gibson_save_worker` both write a library row but never call `_save_parts_bin` — those tabs assemble full plasmids (often blunt / overlap-driven joins), not reusable modular parts, so a bin entry would clutter the palette with non-pickable junk. Modular L0/TU/MOD parts come from the Constructor's GB / MoClo tabs through `_persist_assembly`, which calls `_save_parts_bin` unconditionally + tags the bin entry with the active `grammar: gid` (so a GB save lands in the GB palette, a MoClo save in the MoClo palette — grammar-filtered via `_palette_rows_for_grammar`). The save sequence is library-then-parts-bin: a library save success followed by a parts_bin save failure raises `RuntimeError` with a partial-commit message naming the orphaned `unique_id` so the user can recover. Sequence field on L1+ bin entries (`bin_entry["sequence"]`): post-2026-05-24 carries the released insert body (overhangs stripped) computed from the level-up digest probe (`level_up_frag["sequence"]`), matching the Load Part convention — pre-fix it was hardcoded to `""` which made constructor-saved TUs/MODs look incomplete next to Load-Part-imported TUs. **Traditional engine + UI** (kept here since the pane's behaviour is part of the same Constructor surface): `_simulate_traditional_cloning(insert, vector)` is the 2-fragment wrapper around `_simulate_traditional_cloning_multi([insert], vector)`; the multi engine pre-chains 1..N inserts in lane order before joining the vector. UI (2026-05-23 master/detail rebuild): lane row = donor plasmid spec (`source_entry_id` + `donor_frag_idx`, NOT a pre-digested fragment). "→ Add donor to Lane" parks an unconfigured row; the master/detail editor below (auto-parks on the newly-added row) sets enzymes via `#trad-edit-enz-1/2` and toggles the donor radio (`#trad-edit-frag-0/1`) — fragment 0/1 overrides the feature-aware auto-pick (`_pick_insert_fragment`). `donor_frag_idx = -1` falls back to auto-pick at Simulate time. Vector enzymes on `#trad-vec-enz-1/2`. Save buttons push `NamePlasmidModal` with default `{vector} · {fragments} ({suffix})` (GB-parity dup-check). **Gel preview** (read-only) sits above the editor and re-renders on every lane / enzyme change; backed by `_cached_digest((entry_id, sorted_enzymes_tuple)) → frags` with FIFO-bounded `_record_cache` (32 SeqRecords) + `_digest_cache` (64 digests) so a 5-donor lane doesn't re-scan every plasmid on every cursor tick. Reuses `_render_gel_image`'s visual language (bp ticks on left, well at top, dye-front at bottom) via the inline `_paint_gel` helper which takes pre-computed per-lane bands (each donor digests its own template, unlike SimulatorScreen's single-template gel).
38. **[PIT-38] Trad pane's edit-panel programmatic writes need `prevent` to break feedback loops.** `_refresh_edit_panel` programmatically sets `Select.value` (enzyme dropdowns) and `RadioButton.value` (donor fragment radios) on every cursor move; `_refresh_lane` programmatically calls `move_cursor`. Each of these posts a `Changed` / `RowHighlighted` event asynchronously that would otherwise route back into `_on_edit_enzyme_changed` → `_refresh_lane` → `RowHighlighted` → `_refresh_edit_panel` in an infinite loop (test_lane_add_remove_reorder hung silently for 36 min during initial rollout). Every programmatic widget write must be wrapped in `with self.prevent(MessageType):` AND **the prevent must cover ALL widget writes that fire the event, including `DataTable.clear()` / `add_row()` (both fire synthetic `RowHighlighted` as the cursor relocates) — wrap the entire refresh body, not just `move_cursor`**. A sync `_suppress_edit_events` flag is NOT enough because Textual events fire after the function that sets the flag has already returned and cleared it.
39. **[PIT-39] `Select.BLANK` is a `False` bool, NOT the no-selection sentinel — use `Select.clear()` or `Select.NULL`.** Textual's `Select.BLANK` (class attr) literally `== False`, and the Select widget's `value` setter validates inputs against the options list — assigning `False` (i.e. `Select.BLANK`) is silently REJECTED, leaving the previous value in place. Symptom: after switching the edit panel from a row whose enzyme was set to one whose enzyme is unset, the Select widget shows the OLD row's enzyme; the user then re-selects that same enzyme and no `Select.Changed` fires (value already matches), so the new row's spec never updates. Fix in `_refresh_edit_panel`: use `sel.clear()` (drops to `Select.NULL`, the real no-selection sentinel) when the target value isn't in the options list — never set to `Select.BLANK`.

---

## [PREFS] Persistent user preferences

`settings.json` via `_get_setting`/`_set_setting`. To add a new toggle:

1. Class-level annotation on `PlasmidApp` with default (e.g. `_my_setting: bool = True`).
2. Hydrate in `PlasmidApp.compose()` — `self._my_setting = bool(_get_setting("my_setting", True))`. **`compose()` not `on_mount`** (mount fires leaves→root, so by `on_mount` children read stale defaults).
3. `action_toggle_my_setting` calls `_set_setting("my_setting", self._my_setting)` after flipping.
4. Surface in Settings menu (`MenuBar.MENUS` between File and BLAST; `Settings` entry in `PlasmidApp.open_menu`'s `menus` dict).

**Persisted toggles:** `show_feature_tooltips`, `click_debug`, `check_updates`, `show_restr`, `restr_unique_only`, `restr_min_len`, `min_primer_binding`, `show_connectors`, `constructor_filter_by_grammar`, `restr_use_custom_list`, `restr_custom_enzymes`, `active_collection`, `active_grammar`, `active_primer_collection`, `active_enzyme_collection`, `active_parts_bin`, `active_codon_table`, `seq_panel_height`, `hmm_db_active_id`, `hmm_db_allow_http`. (`linear_layout` was retired 2026-05-08 — layout is flag-only now; a dead schema slot lingers for back-compat.) `map_mode` is **per-plasmid** on each library entry's `map_mode` field. `_library_load` stashes as `_tui_map_mode`; `pm.load_record` honours over topology default; `action_toggle_map_view` + `_register_alignment` write through `_persist_map_mode_for_active`. Sequencing-aligned plasmids auto-tag `linear`. `show_connectors`/`linear_layout` need deferred apply via `_pending_show_connectors`/`_pending_linear_layout` (targets not composed yet in `compose()`).

**Persisted infrastructure:** `last_seen_version` (What's New auto-push), `last_known_latest` + `last_update_check_ts` (24 h PyPI cache), `hmm_db_path`, `active_parts_bin`, `active_project`, `experiments_custom_dict`.

See also: `[INV-43]` settings schema, `[INV-37]` `_SETTINGS_SCHEMA` + `_validate_settings`.

---

## [ARCH] Architecture pointers

`splicecraft.py` top-to-bottom: imports + persistence helpers → enzyme catalog + IUPAC + scanner + 2D feature packer + seq-panel renderer → GenBank I/O → `_Canvas` / `_BrailleCanvas` / `PlasmidMap` / `FeatureSidebar` → `LibraryPanel` → `SequencePanel` → core modals → grammars + settings → codon registry + Kazusa + mutagenesis → feature-library → parts bin → domesticator + constructor → mutagenize → primer design → small modals → `PlasmidApp` → `main()`.

Use `grep -n "^class \|^def " splicecraft.py` for live map. Test files 1:1 named after the subsystem.

See `docs/architecture.md` for the long-form rationale (single-file rule, test pyramid, concurrency model, observability).

---

## [CONV] Conventions

- **Workers:** `@work(thread=True)`, `try / except Exception as exc / _log.exception`, friendly message via `call_from_thread`. Stale-record guard: capture `self._current_record` identity at entry, compare in callback.
- **JSON libraries:** envelope schema v1. Filter `isinstance(entry, dict)` after load. Add new files to `_protect_user_data` in `tests/conftest.py` and `_check_data_files`. Cover corruption recovery in `test_data_safety.py`.
- **Modals:** subclass `ModalScreen[ReturnType]`. Add row to `test_modal_boundaries.py::_MODAL_CASES` (fits 160×48).
- **Tests:** cross-validate against Biopython where biological. No network, no real files (autouse `_protect_user_data` monkeypatches every `_*_FILE` path). Async: `async with app.run_test(size=...)` + double `await pilot.pause()` for `call_after_refresh`.
- **Regression guards** cite date in docstring (`# Regression guard for 2026-MM-DD fix`).

---

## [SISTER] Sister project (ScriptoScope)

`/home/seb/proteoscope/scriptoscope.py` (~8,600 lines) — same author, same single-file convention. Patterns to crib if seq-panel renders blow 33 ms/frame: thread-local `Console` for `_text_to_content`; two-level render cache (`_seq_render_cache` + `_content_cache`, LRU via `OrderedDict.move_to_end`); `@lru_cache(1)` availability probes.

User is undecided whether to merge SpliceCraft / ScriptoScope / MitoShift / RefHunter / molCalc into one Textual app with modes. Single-file convention keeps the option open.

---

## [RECIPE] Borrow before respinning

When building a new feature, look at this map FIRST. Almost every category has a working sibling in-tree whose patterns + invariants are already debugged. Respinning from scratch re-discovers bug classes that earlier sweeps already fixed. **Before writing new code: `grep -ni '<area>' docs/invariants.md docs/subsystems.md`** — the relevant `[INV-NN]` / `[PIT-NN]` / `[SUB-xxx]` likely already encodes the bug class.

**New modal:**
* Subclass `ModalScreen[ReturnType]` (Textual base).
* Hosts an `Input` / `TextArea`? → `_blocks_undo: bool = True` AFTER docstring (`[INV-41]`).
* Mutates `_current_record`? → wrap dismiss callback via `self._guard_callback(cb, "Label")` so a canvas reload mid-modal drops the edit (`[INV-62]`).
* Looks up an item by idx on dismiss? → use **identity-based lookup** like `PartEditModal._on_result` does with `(name, sequence)` tuple — refuses + notifies on miss. Better than counter-based for in-place mutations.
* Double-click race? → `_dismissed: bool` flag + `_dismiss_once(payload)` helper, applied to every exit path (`[INV-50]`).
* Add a row to `tests/test_modal_boundaries.py::_MODAL_CASES` (must fit 160×48).

**New persisted JSON file (cache + reload semantics):**
1. `_<NAME>_FILE = _DATA_DIR / "<name>.json"` constant.
2. `_<name>_cache: "list | dict | None" = None` module-level global.
3. `_load_<name>()` returns `_typed_clone(_<name>_cache)` (`[PIT-17]`, deepcopy on read).
4. `_save_<name>()` wraps `_safe_save_json` + cache reseat inside `with _cache_lock:` (`[INV-41]` — concurrency).
5. Register cache name in `_MASTER_DELETE_CACHE_ATTRS` (`[INV-48]`).
6. Register file attr in `_USER_DATA_FILE_ATTRS` (`[INV-39]`).
7. Add the `(file_attr, cache_attr)` tuple to `tests/conftest.py::_protect_user_data::_DATA_FILES`.
8. Add to `_check_data_files` launch-check.
9. Add the attr name to `RestoreFromBackupModal._TARGETS` so the Restore-from-backup UI covers it (`[INV-43]`).
10. Settings keys → add to `_SETTINGS_SCHEMA` with explicit type tuple + default (`[INV-43]`).

**New save action:**
* All writes go through `_safe_save_json` (sacred #7) — never raw `json.dump`.
* On failure call `_notify_save_failure(app, label, exc)` — fires the `save.failed` structured event AND surfaces a user toast (`[INV-41]`).
* Agent endpoints use `_agent_save_or_500(save_fn, label)` for uniform 500 shape.
* If the save has a downstream mirror (active collection, active project, etc.), the mirror call MUST live inside the `_cache_lock` block (`[INV-50]`).

**New `action_*` method:**
* Decorate `@_action_log("app.<area>.<verb>")` for the user-intent event (`[INV-42]`). Decorator AND body events can co-exist — intent vs outcome are different signals.
* Destructive? Use a confirm modal with default-focus on `No` (mirror `LibraryDeleteConfirmModal`). Stray Enter should never delete.

**New `@work` worker:**
* `@work(thread=True, exclusive=True, group="<name>")` for heavy / cancellable ops.
* Capture `entry_counter = self._record_load_counter` at entry; bail in `_apply` callback if it shifted (`[PIT-28]`).
* `except Exception as exc / _log.exception("...")` — `[PIT-01]`'s explicit carve-out for worker bodies.
* `try / finally` to drop any "in-flight" sentinels so an exception can't wedge the worker permanently (`[INV-41]` example: `_settings_flush_running`).

**New agent endpoint:**
* `@_agent_endpoint("name", write=True/False)` — `_AGENT_HANDLERS` registry is auto-populated.
* Write endpoints: route through `_agent_save_or_500`; check `_agent_dirty_guard(app, payload)` if the canvas dirty state matters.
* Inputs use `_sanitize_label`, `_sanitize_bases`, `_sanitize_accession`, etc. — never trust raw payload values.
* Idx-based payloads: capture `_record_load_counter`, check in `_apply` (`[PIT-28]`).
* Listing endpoints with large payloads (library, search): hard-cap at endpoint-specific limit; `_AGENT_RESPONSE_MAX_BYTES = 50 MB` is the global backstop.
* Add a row to `test_agent_api.py` covering happy + error paths.

**New picker / DataTable:**
* Same sort for display AND for cursor → idx resolution (`[PIT-33]`). Easiest: `key=` parameter on `add_row` (pre-empts the bug class — `PlasmidPickerModal` is the reference).
* If using `cursor_row` int + sort, build explicit `_row_to_entry_idx` + `_entry_idx_to_row` maps.

**New feature-list iteration:**
* Use `_feat_bounds(feat, total) → (start, end, strand)` for wrap-aware extraction (`[INV-41]`).
* Use `_smallest_enclosing_feature(bp)` not O(N) `_feat_at` for bp-lookup (`[INV-41]`).
* `_feat_len(start, end, total)` for wrap-aware distance — naive `end - start` is wrong on origin-spanning features (sacred #8).

**New HTTP fetch:**
* `resp.read(MAX + 1)` + bail-if-exceeded — never `resp.read()` raw (`[PIT-20]`).
* Existing caps: `_PYPI_MAX_RESPONSE_BYTES`, `_NCBI_MAX_RESPONSE_BYTES`, `_KAZUSA_MAX_RESPONSE_BYTES`, `_PLASMIDSAURUS_*_MAX_BYTES`.
* Retry pattern: 1 try + 250 ms backoff (mirrors `_fetch_latest_pypi_version`, `fetch_genbank`).

**New XML parsing:**
* Route through `_safe_xml_parse` — rejects DOCTYPE / ENTITY (`[PIT-19]`). Includes NCBI responses AND `.dna` history packets.

**New logging point:**
* User actions: `@_action_log("app.area.verb")` decorator.
* State changes: `_log_event("<noun>.<verb>", **fields)`.
* Heavy ops: `@_timed("op.area.name", threshold_ms=50)` wrapper.
* **SACRED: never log sequence content** — `_repr_for_log` truncates/summarises automatically (`[INV-38]`). `seq.chunk_dump` and similar route through structured events that hash or length-only the payload.

**New file-system traversal:**
* `path.lstat()` + `stat.S_ISREG(st.st_mode)` — refuses symlinks outright (`[INV-50]`).
* `_safe_save_json` already covers symlink refusal for writes.
* Bulk imports: walk via `path.iterdir()` not `os.walk(followlinks=True)`.
