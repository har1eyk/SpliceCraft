[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
# Suppress DeprecationWarnings from upstream libs during the test run.
filterwarnings = [
    "ignore::DeprecationWarning:Bio.*",
    "ignore::PendingDeprecationWarning:Bio.*",
]
